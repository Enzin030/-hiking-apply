# 階段 4 刪除清單（第 2 版・2026-09-09）

> **本輪未執行任何刪除、移動或改名。** 這份是執行計畫。

第 1 版放在 `.scratch/outputs/`，審核 agent 讀不到（`.scratch` 是 junction 且已列入
`.gitignore`）。第 2 版改放 repo 根目錄，與 `decisions.md`、`CONTEXT.md` 同層。

第 1 版只有盤點，沒有逐條的刪／移拆分，也沒有執行順序——那兩項是這一版補的。
死規則的數字也全部重算過，理由見 §2.1。


## 0. 與第 1 版的差異（先看這節）

| 項目 | 第 1 版 | 第 2 版 | 為什麼變 |
|---|---|---|---|
| 死規則判定方法 | 文字比對（grep class token） | **瀏覽器 `querySelectorAll` ∧ 原始碼字面** 兩個獨立方法取交集 | 文字法被騙過 3 次，見 §2.1 |
| 判定為可刪 | 246 → 270 → 333 → 325（反覆修正） | **402 條**（A 級） | 同上 |
| 保留待確認 | 無此分級 | **52 條**（B 級，§2.4） | 「沒渲染到」不等於「沒人用」 |
| 第 3、4 節 | 只列規則，未分刪／移 | **逐條標刪或移**（§3、§4） | 缺口 2 |
| shared.css 退場 | 無 | **三步驟＋剩餘 456 條的落點**（§8） | 缺口 4 |
| 執行順序 | 無 | **§9 十一步，附每步的驗證與回滾** | 審核指出 |
| 範本改寫契約 | 只說「要先改寫」 | **§10 逐項對照使用者列的 5 點** | 缺口 1 |
| 產生器 | 「保留輸入所以延後刪 .jsx」 | **逐支實測能否重跑**（§7.2），不能重跑的不構成延後理由 | 缺口 3 |
| 慣例檔 | 只點名 `template/page.html` | **CLAUDE.md／CONTEXT.md／`_README.md` 已改寫**（§10.1） | 缺口 1 |


## 1. 35 個 `.html` 的 link／script 盤點

| 頁面 | shared.css | index.css | React/Babel | `.jsx` |
|---|---|---|---|---|
| `_island-test.html` | 0 | 1 | 3 | 0 |
| `_stage2-test.html` | 1 | 1 | 0 | 0 |
| `apply-1.html` | 1 | 1 | 0 | 0 |
| `apply-2.html` | 1 | 1 | 0 | 0 |
| `apply-3.html` | 1 | 1 | 0 | 0 |
| `apply_teach.html` | 1 | 1 | 0 | 0 |
| `applySearch.html` | 1 | 1 | 0 | 0 |
| `campsite.html` | 1 | 1 | 0 | 0 |
| `forest-camp-1.html` | 1 | 1 | 0 | 0 |
| `forest-camp-2.html` | 1 | 1 | 0 | 0 |
| `index-b.html` | 1 | 1 | 0 | 0 |
| `index.html` | 1 | 1 | 0 | 0 |
| `information_6.html` | 1 | 1 | 0 | 0 |
| `news.html` | 1 | 1 | 0 | 0 |
| `notice.html` | 1 | 1 | 0 | 0 |
| `notice_a1.html` | 1 | 1 | 0 | 0 |
| `notice_a10.html` | 1 | 1 | 0 | 0 |
| `notice_a2.html` | 1 | 1 | 0 | 0 |
| `notice_a3.html` | 1 | 1 | 0 | 0 |
| `notice_a4.html` | 1 | 1 | 0 | 0 |
| `notice_a5.html` | 1 | 1 | 0 | 0 |
| `notice_a6.html` | 1 | 1 | 0 | 0 |
| `notice_a7.html` | 1 | 1 | 0 | 0 |
| `notice_a8.html` | 1 | 1 | 0 | 0 |
| `notice_a9.html` | 1 | 1 | 0 | 0 |
| `notice_b1.html` | 1 | 1 | 0 | 0 |
| `notice_b2.html` | 1 | 1 | 0 | 0 |
| `notice_b3.html` | 1 | 1 | 0 | 0 |
| `notice_b4.html` | 1 | 1 | 0 | 0 |
| `notice_b5.html` | 1 | 1 | 0 | 0 |
| `notice_b6.html` | 1 | 1 | 0 | 0 |
| `notice_b7.html` | 1 | 1 | 0 | 0 |
| `open.html` | 1 | 1 | 0 | 0 |
| `rss.html` | 1 | 1 | 0 | 0 |
| `web_illustrate.html` | 1 | 1 | 0 | 0 |

- 仍載入 React／Babel 或 `.jsx` 的頁面：**['_island-test.html']**
- 34/35 link `shared.css`；35/35 link `index.css`
- **`template/page.html` 不在上表**（在子資料夾）。它原本仍載 React／Babel／`Shared.jsx`／
  `PageName.jsx`，是 `Shared.jsx` 唯一的 `<script src>` 引用來源。
  **2026-09-10 已改寫為 Vue 版**（§10.2）：四行靜態順序與已遷移頁面一致、
  `data-page-init` 指向 `template/PageName.js`、零 React，實測 22 項驗證全過。
  ⇒ **`Shared.jsx` 的引用來源已消失，§9 步驟 2 的前置條件解除。**
- `_island-test.html`、`_stage2-test.html` 是使用者確認保留的驗收頁。`_stage2-test.html`
  沿用 13 條 `.camp-*` 舊 class，是 §8 中「暫留 shared.css」那 14 條的唯一成因。


## 2. 死規則

### 2.1 判定方法（第 1 版的數字為什麼不能用）

第 1 版用「規則的每個 class token 是否出現在語料中」判定，那個方法在本專案被騙過**三次**：

| # | 情形 | 具體例子 | 錯的方向 |
|---|---|---|---|
| 1 | 後代選擇器 | `.p3-members-row .th-input`：祖先不存在就選不到，但 `th-input` 還活著 | 漏判（看起來活著） |
| 2 | 執行期組出的 class | `'th-level lv-' + n`、`'bulletin-badge agency-' + id`、`'is-' + align` 是字串串接 | 誤判（看起來死了） |
| 3 | 詞邊界 | `th-header` 會命中 `th-header-bar`（`-` 本身就是詞邊界） | 漏判 |

改成**兩個獨立方法取交集**：

```
A 級（可刪）＝ 瀏覽器 querySelectorAll 選不到任何元素
            ∧ 原始碼裡連完整 class token 的字面都沒有
B 級（保留）＝ 瀏覽器選不到，但原始碼有字面 → 條件渲染分支或涵蓋不足
```
兩個方法的錯誤方向剛好相反（瀏覽器法會把未渲染的分支誤判為死，文字法會把
執行期組合誤判為死），取交集才能同時避開兩種。

**瀏覽器法本身也踩到 4 個坑，都已修**：

| 坑 | 症狀 | 修法 |
|---|---|---|
| `:hover`／`::before` | `querySelectorAll('.a:hover')` 永遠回 0，整批誤判為死 | 測存在性前剝掉互動偽類與偽元素（884→810 個可查詢選擇器）；`:not`／`:nth-*` 等結構性偽類保留 |
| 語法被拒 | JS 端 `catch` 回 −1，Python 端 `if n > 0` 當成沒命中，**全程不出聲** | 另跑 `probe_invalid.py` 空白頁測語法：實測 810 個**全部合法**，此坑本專案未觸發 |
| 元件名撞 class 名 | `<th-header active=…>` 與 `window.thComponents["th-header"]` 讓 `.th-header` 看起來有 52 處消費端，實際 0 處 | 取證時排除 `<tok`／`</tok` 與註冊字串位置 |
| 多行註解 | `.th-faq` 只出現在一段 `<!-- … -->` 中間的行，該行行首不是註解符號 | 先整段剝註解，不只看行首 |

**`@keyframes` 的 `from`／`to`／百分比不是選擇器**，規則掃描會把它們當規則收進來
（2 條，L[6825, 6829]），已從死規則扣除。

### 2.2 覆蓋範圍（判定的效力邊界）

- **35 個頁面** × 兩個狀態（初始／互動後）
- **5 個帶查詢字串的分支**：`apply-2.html?unit=police`、`apply-2.html?unit=forestry-area`、`forest-camp-1.html?route=guigu`、`forest-camp-2.html?route=guigu&start=2026-11-01&nights=3&headcount=6`、`apply-3.html?route=yushan-main`
- **5 頁在 390px 寬**（`news`／`campsite`／`apply-3`／`open`／`index-b`），涵蓋行動版 `@media`
- 互動後的動作：切分頁、切子選、點膠囊、開月曆彈窗、按查詢、開快捷選單、填文字欄位
- **未涵蓋**：手機版選單面板（要先縮寬再點開啟鈕）、語言下拉、forest-camp-2 的逐晚填妥、
  依資料才出現的分支（`o.built === false`、附件副檔名）。這些正是 B 級的來源。

### 2.3 A 級：可刪（402 條）

| 族 | 條數 | 來由 |
|---|---|---|
| `.p2-*` | 97 | apply-2 已改名 `.p-apply2-*` |
| `.p1-*` | 78 | apply-1 已改名 `.p-apply1-*` |
| `.th-*` | 73 | 元件化後由 components.css 接手，或原本就無人使用 |
| `.p3-*` | 73 | apply-3 已改名 `.p-apply3-*` |
| `.open-*` | 46 | open 已改名 `.p-open-*` |
| `.camp-*` | 19 | campsite 已改名 `.p-campsite-*` |
| `.rss-*` | 10 | rss 已改名 `.p-rss-*` |
| `.bulletin-*` | 4 | news／campsite 改名後的殘留 |
| `.fc-*` | 2 | forest-camp 的殘留 |

<details><summary>A 級逐條（點開）</summary>

| 行號 | 選擇器 | @media |
|---|---|---|
| 51 | `.th-header-inner` |  |
| 61 | `.th-brand` |  |
| 68 | `.th-logo` |  |
| 73 | `.th-wordmark` |  |
| 83 | `.th-wordmark-sub` |  |
| 96 | `.th-header-right` |  |
| 103 | `.th-utility` |  |
| 111 | `.th-utility a, .th-utility-btn` |  |
| 124 | `.th-utility-btn:hover, .th-utility a:hover` |  |
| 129 | `.th-divider` |  |
| 135 | `.th-nav` |  |
| 143 | `.th-nav a` |  |
| 150 | `.th-nav a:hover` |  |
| 154 | `.th-nav a.active` |  |
| 158 | `.th-nav a.active::after` |  |
| 252 | `.th-step-label .lbl-sub` |  |
| 448 | `.th-label-en` |  |
| 840 | `.th-footer-inner` |  |
| 851 | `.th-footer-brand` |  |
| 858 | `.th-footer-brand img` |  |
| 862 | `.th-footer-band` |  |
| 872 | `.th-header-inner` | (max-width: 900px) |
| 877 | `.th-nav` | (max-width: 900px) |
| 904 | `.th-header-right` | (max-width: 600px) |
| 1483 | `.bulletin-file.odt, .bulletin-file.doc, .bulletin-file.docx` |  |
| 1491 | `.bulletin-file.odt:hover, .bulletin-file.doc:hover, .bulletin-file.docx:hover` |  |
| 1497 | `.bulletin-file.xlsx, .bulletin-file.xls, .bulletin-file.csv` |  |
| 1505 | `.bulletin-file.xlsx:hover, .bulletin-file.xls:hover, .bulletin-file.csv:hover` |  |
| 2059 | `.th-expcard-step` |  |
| 2067 | `.th-expcard-head` |  |
| 2075 | `.th-expcard-icon` |  |
| 2088 | `.th-expcard-title` |  |
| 2095 | `.th-expcard-sub` |  |
| 2509 | `.th-table .is-right` |  |
| 2778 | `.th-faq` |  |
| 2783 | `.th-faq details` |  |
| 2787 | `.th-faq details:first-child` |  |
| 2791 | `.th-faq summary` |  |
| 2805 | `.th-faq summary::-webkit-details-marker` |  |
| 2809 | `.th-faq summary:hover` |  |
| 2813 | `.th-faq details[open] summary` |  |
| 2817 | `.th-faq summary i` |  |
| 2823 | `.th-faq details[open] summary i` |  |
| 2828 | `.th-faq-body` |  |
| 2836 | `.th-faq-body p` |  |
| 2840 | `.th-faq-body p:last-child` |  |
| 2844 | `.th-faq-body a` |  |
| 2849 | `.th-faq-body a i` |  |
| 2897 | `.th-faq-links` |  |
| 2905 | `.th-faq-link` |  |
| 2913 | `.th-faq-link.is-todo` |  |
| 2919 | `.th-faq-link i` |  |
| 2924 | `.th-page-cta` |  |
| 2936 | `.th-page-cta-text` |  |
| 2969 | `.th-faq summary` | (max-width: 640px) |
| 2973 | `.th-faq-body` | (max-width: 640px) |
| 2977 | `.th-page-cta` | (max-width: 640px) |
| 3060 | `.th-table.tbl-rss th:nth-child(1)` |  |
| 3064 | `.th-table.tbl-rss th:nth-child(2)` |  |
| 3068 | `.th-table.tbl-rss th:nth-child(4)` |  |
| 3072 | `.rss-channel` |  |
| 3080 | `.rss-channel i` |  |
| 3085 | `.rss-url` |  |
| 3092 | `.rss-url-text` |  |
| 3101 | `.rss-url-text:hover` |  |
| 3105 | `.rss-copy` |  |
| 3109 | `.rss-url.is-todo` |  |
| 3116 | `.rss-url-note` |  |
| 3126 | `.rss-url-note i` |  |
| 3131 | `.rss-offscreen` |  |
| 3141 | `.th-table.tbl-rss` | (max-width: 640px) |
| 3145 | `.th-table.tbl-rss thead` | (max-width: 640px) |
| 3149 | `.th-table.tbl-rss tbody, .th-table.tbl-rss tbody tr` | (max-width: 640px) |
| 3154 | `.th-table.tbl-rss th:nth-child(1), .th-table.tbl-rss th:nth-child(2), .th-tabl` | (max-width: 640px) |
| 3160 | `.th-table.tbl-rss tbody tr` | (max-width: 640px) |
| 3165 | `.th-table.tbl-rss tbody tr:last-child` | (max-width: 640px) |
| 3169 | `.th-table.tbl-rss tbody td` | (max-width: 640px) |
| 3176 | `.th-table.tbl-rss tbody td+td` | (max-width: 640px) |
| 3180 | `.th-table.tbl-rss tbody td:not(:first-child)::before` | (max-width: 640px) |
| 3224 | `.th-notice-actions` |  |
| 3229 | `.th-video-btn` |  |
| 3243 | `.th-video-btn:hover` |  |
| 3248 | `.th-video-btn i` |  |
| 3265 | `.th-notice-actions` | (max-width: 640px) |
| 3269 | `.th-video-btn` | (max-width: 640px) |
| 3436 | `.p1-layout` |  |
| 3444 | `.p1-layout` | (max-width: 1000px) |
| 3450 | `.p1-filters` |  |
| 3461 | `.p1-filters` | (max-width: 1000px) |
| 3466 | `.p1-filter-head` |  |
| 3474 | `.p1-filter-head h3` |  |
| 3485 | `.p1-filter-head h3 i` |  |
| 3490 | `.p1-filter-reset` |  |
| 3500 | `.p1-filter-reset:hover` |  |
| 3504 | `.p1-filter-group` |  |
| 3509 | `.p1-filter-group:last-child` |  |
| 3513 | `.p1-filter-label` |  |
| 3523 | `.p1-filter-label i` |  |
| 3528 | `.p1-difficulty-row` |  |
| 3534 | `.p1-diff` |  |
| 3547 | `.p1-diff:hover` |  |
| 3552 | `.p1-diff.is-active` |  |
| 3558 | `.p1-diff-legend` |  |
| 3567 | `.p1-toggle` |  |
| 3577 | `.p1-toggle input` |  |
| 3581 | `.p1-toggle-track` |  |
| 3591 | `.p1-toggle-track::after` |  |
| 3604 | `.p1-toggle input:checked+.p1-toggle-track` |  |
| 3608 | `.p1-toggle input:checked+.p1-toggle-track::after` |  |
| 3613 | `.p1-search-bar` |  |
| 3622 | `.p1-search-row` |  |
| 3628 | `.p1-search-hint` |  |
| 3638 | `.p1-search-hint .lbl` |  |
| 3642 | `.p1-search-hint .tag` |  |
| 3652 | `.p1-search-hint .tag:hover` |  |
| 3658 | `.p1-results-head` |  |
| 3666 | `.p1-results-count` |  |
| 3671 | `.p1-results-count strong` |  |
| 3678 | `.p1-sort` |  |
| 3687 | `.p1-group` |  |
| 3691 | `.p1-group-head` |  |
| 3700 | `.p1-group-head-left` |  |
| 3706 | `.p1-group-tag` |  |
| 3717 | `.p1-group-tag.tag-yushan` |  |
| 3721 | `.p1-group-tag.tag-shei-pa` |  |
| 3725 | `.p1-group-tag.tag-taroko` |  |
| 3729 | `.p1-group-tag.tag-forestry` |  |
| 3733 | `.p1-group-tag.tag-police` |  |
| 3737 | `.p1-group-meta` |  |
| 3743 | `.p1-route-list` |  |
| 3750 | `.p1-route-list` | (max-width: 1200px) |
| 3755 | `.p1-route` |  |
| 3767 | `.p1-route:hover` |  |
| 3773 | `.p1-route-thumb` |  |
| 3780 | `.p1-route-thumb img` |  |
| 3786 | `.p1-route-diff` |  |
| 3800 | `.p1-route-body` |  |
| 3809 | `.p1-route-title-row` |  |
| 3816 | `.p1-route-title` |  |
| 3826 | `.p1-route-badges` |  |
| 3835 | `.p1-route-badges .badge-hot` |  |
| 3848 | `.p1-route-badges .badge-lottery` |  |
| 3861 | `.p1-route-sub` |  |
| 3869 | `.p1-route-meta` |  |
| 3878 | `.p1-route-meta span` |  |
| 3884 | `.p1-route-meta i` |  |
| 3889 | `.p1-route-foot` |  |
| 3898 | `.p1-status-pill` |  |
| 3908 | `.p1-status-pill.s-open` |  |
| 3914 | `.p1-status-pill.s-lottery` |  |
| 3920 | `.p1-status-pill.s-closed` |  |
| 3926 | `.p1-status-pill i` |  |
| 3930 | `.p1-route-go` |  |
| 3939 | `.p1-route:hover .p1-route-go i` |  |
| 3943 | `.p1-route-go i` |  |
| 3947 | `.p1-route-go.go-muted` |  |
| 3951 | `.p1-route-badges .badge-closed` |  |
| 3964 | `.p1-route.is-suspended` |  |
| 3968 | `.p1-route.is-suspended:hover` |  |
| 3973 | `.p1-route` | (max-width: 600px) |
| 3977 | `.p1-route-thumb` | (max-width: 600px) |
| 3982 | `.p1-route-title-row` | (max-width: 600px) |
| 3986 | `.p1-route-badges` | (max-width: 600px) |
| 4000 | `.p2-layout` |  |
| 4008 | `.p2-layout` | (max-width: 1100px) |
| 4013 | `.p2-route-brief` |  |
| 4020 | `.p2-route-brief>div` |  |
| 4033 | `.p2-route-brief-label` |  |
| 4039 | `.p2-route-brief strong` |  |
| 4044 | `.p2-route-brief span:not(.p2-route-brief-label)` |  |
| 4049 | `.p2-route-brief code` |  |
| 4060 | `.p2-route-brief` | (max-width: 820px) |
| 4066 | `.p2-intro` |  |
| 4078 | `.p2-intro-icon` |  |
| 4091 | `.p2-intro h2` |  |
| 4100 | `.p2-intro p` |  |
| 4107 | `.p2-intro-meta` |  |
| 4115 | `.p2-intro-meta span` |  |
| 4121 | `.p2-intro-meta i` |  |
| 4126 | `.p2-sec` |  |
| 4136 | `.p2-sec.is-acked` |  |
| 4141 | `.p2-sec-head` |  |
| 4153 | `.p2-sec-head-left` |  |
| 4161 | `.p2-sec-num` |  |
| 4176 | `.p2-sec.is-acked .p2-sec-num` |  |
| 4181 | `.p2-sec-num i` |  |
| 4185 | `.p2-sec-titles` |  |
| 4192 | `.p2-sec-kicker` |  |
| 4199 | `.p2-sec-title` |  |
| 4208 | `.p2-sec-head-right` |  |
| 4215 | `.p2-sec-count` |  |
| 4224 | `.p2-sec.is-acked .p2-sec-count` |  |
| 4229 | `.p2-sec-chev` |  |
| 4236 | `.p2-sec-head` | (max-width: 640px) |
| 4241 | `.p2-sec-head-right` | (max-width: 640px) |
| 4248 | `.p2-sec-body` |  |
| 4253 | `.p2-consent-html` |  |
| 4259 | `.p2-consent-html, .p2-consent-html *` |  |
| 4266 | `.p2-consent-html p` |  |
| 4270 | `.p2-consent-html p:last-child` |  |
| 4274 | `.p2-consent-html ol, .p2-consent-html ul` |  |
| 4280 | `.p2-consent-html li` |  |
| 4284 | `.p2-consent-html a` |  |
| 4290 | `.p2-consent-html strong` |  |
| 4295 | `.p2-clause` |  |
| 4305 | `.p2-clause:last-child` |  |
| 4309 | `.p2-clause-num` |  |
| 4325 | `.p2-clause-body` |  |
| 4330 | `.p2-clause-body strong` |  |
| 4335 | `.p2-clause-body em` |  |
| 4341 | `.p2-clause-body a` |  |
| 4347 | `.p2-clause-body ul` |  |
| 4354 | `.p2-clause-body ul li` |  |
| 4358 | `.p2-clause-body .small` |  |
| 4365 | `.p2-sec-check` |  |
| 4382 | `.p2-sec-check:hover` |  |
| 4387 | `.p2-sec.is-acked .p2-sec-check` |  |
| 4393 | `.p2-sec-check input` |  |
| 4397 | `.p2-sec-ack-box` |  |
| 4410 | `.p2-sec-check input:checked+.p2-sec-ack-box` |  |
| 4415 | `.p2-sec-check input:checked+.p2-sec-ack-box::after` |  |
| 4424 | `.p2-toc` |  |
| 4435 | `.p2-toc` | (max-width: 1100px) |
| 4440 | `.p2-toc h3` |  |
| 4451 | `.p2-toc h3 i` |  |
| 4455 | `.p2-reading-meter` |  |
| 4463 | `.p2-reading-label` |  |
| 4473 | `.p2-reading-label strong` |  |
| 4479 | `.p2-reading-track` |  |
| 4487 | `.p2-reading-bar` |  |
| 4494 | `.p2-confirm-meter` |  |
| 4498 | `.p2-progress` |  |
| 4506 | `.p2-progress-bar` |  |
| 4513 | `.p2-progress-label` |  |
| 4521 | `.p2-progress-label strong` |  |
| 4526 | `.p2-toc-list` |  |
| 4539 | `.p2-toc-list li button` |  |
| 4555 | `.p2-toc-list li button:hover` |  |
| 4560 | `.p2-toc-list li.is-active button` |  |
| 4566 | `.p2-toc-list li.is-active .p2-toc-dot` |  |
| 4571 | `.p2-toc-dot` |  |
| 4586 | `.p2-toc-list li.is-acked .p2-toc-dot` |  |
| 4592 | `.p2-toc-list li.is-acked .p2-toc-dot::after` |  |
| 4598 | `.p2-toc-list li.is-acked .p2-toc-dot span` |  |
| 4602 | `.p2-toc-list li.is-acked button` |  |
| 4606 | `.p2-toc-text` |  |
| 4615 | `.p2-toc-kicker` |  |
| 4623 | `.p2-footbar` |  |
| 4640 | `.p2-footbar` | (max-width: 700px) |
| 4646 | `.p2-footbar-status` |  |
| 4654 | `.p2-footbar-status .num` |  |
| 4660 | `.p2-footbar-icon` |  |
| 4672 | `.p2-footbar.is-ready .p2-footbar-icon` |  |
| 4677 | `.p2-footbar-actions` |  |
| 4683 | `.p2-callout` |  |
| 4698 | `.p2-callout i` |  |
| 4705 | `.p2-callout strong` |  |
| 4709 | `.p2-info-box` |  |
| 4720 | `.p2-info-box .lbl` |  |
| 4726 | `.p2-info-box code` |  |
| 4743 | `.p3-layout` |  |
| 4751 | `.p3-layout` | (max-width: 1100px) |
| 4757 | `.p3-route-card` |  |
| 4766 | `.p3-route-head` |  |
| 4774 | `.p3-route-thumb` |  |
| 4784 | `.p3-route-thumb img` |  |
| 4790 | `.p3-route-thumb-tag` |  |
| 4802 | `.p3-route-info` |  |
| 4807 | `.p3-route-info h3` |  |
| 4815 | `.p3-route-info-sub` |  |
| 4821 | `.p3-route-tags` |  |
| 4828 | `.p3-route-tag` |  |
| 4840 | `.p3-route-tag i` |  |
| 4845 | `.p3-route-tag.tag-warn` |  |
| 4851 | `.p3-route-tag.tag-warn i` |  |
| 4855 | `.p3-route-change` |  |
| 4867 | `.p3-route-change:hover` |  |
| 4872 | `.p3-route-warn` |  |
| 4884 | `.p3-route-warn i` |  |
| 4890 | `.p3-route-warn strong` |  |
| 4896 | `.p3-section` |  |
| 4905 | `.p3-section-head` |  |
| 4914 | `.p3-section-num` |  |
| 4928 | `.p3-section-titles` |  |
| 4933 | `.p3-section-title` |  |
| 4944 | `.p3-section-title-en` |  |
| 4951 | `.p3-section-sub` |  |
| 4957 | `.p3-section-body` |  |
| 4962 | `.p3-grid` |  |
| 4968 | `.p3-grid-3` |  |
| 4972 | `.p3-grid-1` |  |
| 4976 | `.p3-grid-full` |  |
| 4982 | `.p3-grid, .p3-grid-3` | (max-width: 700px) |
| 4988 | `.p3-radio-row` |  |
| 4994 | `.p3-radio` |  |
| 5011 | `.p3-radio:hover` |  |
| 5015 | `.p3-radio input` |  |
| 5020 | `.p3-radio.is-active` |  |
| 5028 | `.p3-date-row` |  |
| 5034 | `.p3-date-input` |  |
| 5038 | `.p3-date-arrow` |  |
| 5044 | `.p3-date-result` |  |
| 5052 | `.p3-date-result span` |  |
| 5059 | `.p3-date-result span strong` |  |
| 5065 | `.p3-members` |  |
| 5071 | `.p3-members-head` |  |
| 5083 | `.p3-members-row` |  |
| 5092 | `.p3-members-row:last-child` |  |
| 5096 | `.p3-members-row .leader-tag` |  |
| 5109 | `.p3-members-row .row-num` |  |
| 5123 | `.p3-members-row .row-del` |  |
| 5132 | `.p3-members-row .row-del:hover` |  |
| 5136 | `.p3-members-row .row-del:disabled` |  |
| 5141 | `.p3-members-foot` |  |
| 5151 | `.p3-members-foot strong` |  |
| 5156 | `.p3-add-member` |  |
| 5171 | `.p3-add-member:hover` |  |
| 5176 | `.p3-members-head` | (max-width: 900px) |
| 5180 | `.p3-members-row` | (max-width: 900px) |
| 5196 | `.p3-members-row>div::before` | (max-width: 900px) |
| 5207 | `.p3-day-row` |  |
| 5218 | `.p3-day-pill` |  |
| 5232 | `.p3-day-row .th-input` |  |
| 5238 | `.p3-submit` |  |
| 5242 | `.p3-submit-info` |  |
| 5246 | `.p3-summary` |  |
| 5250 | `.p3-summary-head` |  |
| 5254 | `.p3-summary-body` |  |
| 5258 | `.p3-summary-row` |  |
| 5262 | `.p3-summary-checklist` |  |
| 5267 | `.p3-police-note` |  |
| 5282 | `.p3-police-note i` |  |
| 5289 | `.p3-police-note strong` |  |
| 5672 | `.fc-notice-head, .fc-price-head` |  |
| 5685 | `.fc-notice-head i, .fc-price-head i` |  |
| 6125 | `.camp-notice` |  |
| 6131 | `.camp-notice li+li` |  |
| 6136 | `.camp-kindrow` |  |
| 6159 | `.camp-monthbar` |  |
| 6171 | `.camp-month` |  |
| 6178 | `.camp-monthbar-btn` |  |
| 6300 | `.camp-legend` |  |
| 6308 | `.camp-legend-hint` |  |
| 6346 | `.camp-sub` |  |
| 6362 | `.camp-intro` |  |
| 6368 | `.camp-intro-pic` |  |
| 6373 | `.camp-intro-pic img` |  |
| 6379 | `.camp-intro-body` |  |
| 6383 | `.camp-intro-lines` |  |
| 6391 | `.camp-intro-tags` |  |
| 6398 | `.camp-tag` |  |
| 6411 | `.camp-select` |  |
| 6448 | `.camp-intro-pic` | (max-width: 768px) |
| 6452 | `.camp-monthbar` | (max-width: 768px) |
| 6475 | `.open-legend` |  |
| 6486 | `.open-legend-row` |  |
| 6493 | `.open-legend-label` |  |
| 6502 | `.open-table-wrap` |  |
| 6507 | `.open-table` |  |
| 6511 | `.open-table thead th` |  |
| 6517 | `.open-table tbody td` |  |
| 6522 | `.open-table .col-org` |  |
| 6526 | `.open-table .col-main` |  |
| 6531 | `.open-table .col-name2` |  |
| 6536 | `.open-table .col-can` |  |
| 6541 | `.open-table .col-status` |  |
| 6546 | `.open-table .col-needs` |  |
| 6550 | `.open-table .col-need` |  |
| 6555 | `.open-table thead tr+tr th:nth-child(1)` |  |
| 6559 | `.open-table thead tr+tr th:nth-child(2)` |  |
| 6563 | `.open-table thead tr+tr th:nth-child(3)` |  |
| 6567 | `.open-table thead tr+tr th:nth-child(4)` |  |
| 6571 | `.open-table .col-level` |  |
| 6577 | `.open-table .col-fn` |  |
| 6582 | `.open-table thead tr+tr th` |  |
| 6588 | `.open-need-yes` |  |
| 6593 | `.open-need-no` |  |
| 6598 | `.open-level-btn` |  |
| 6603 | `.open-level-btn:hover` |  |
| 6608 | `.open-actions` |  |
| 6615 | `.open-act` |  |
| 6629 | `.open-act i` |  |
| 6633 | `.open-act:hover` |  |
| 6639 | `.open-act.is-primary` |  |
| 6646 | `.open-act.is-primary:hover` |  |
| 6653 | `.open-act.th-todo-link:hover` |  |
| 6660 | `.open-act.th-todo-link::after` |  |
| 6666 | `.open-select` |  |
| 6672 | `.open-modal-def dl` |  |
| 6676 | `.open-modal-def dt` |  |
| 6683 | `.open-modal-def dd` |  |
| 6687 | `.open-modal-def dd:last-child` |  |
| 6692 | `.open-closures` |  |
| 6698 | `.open-closures-title` |  |
| 6705 | `.open-closures ul` |  |
| 6713 | `.open-table-wrap` | (max-width: 768px) |
| 6717 | `.open-table` | (max-width: 768px) |
| 6722 | `.open-table tbody td::before` | (max-width: 768px) |
| 6728 | `.open-table tbody td.col-org, .open-table tbody td.col-main, .open-table tbody` | (max-width: 768px) |
| 6740 | `.open-legend-label` | (max-width: 768px) |
| 6906 | `.th-home-nav-active` |  |
| 6911 | `.th-home-nav-active::after` |  |

</details>

### 2.4 B 級：保留待確認（52 條）

**這批不刪。** 瀏覽器選不到，但原始碼有實際的消費程式碼——多半是條件渲染分支。
典型例子：`campsite.html` 的 `<span v-else class="th-chip is-todo">` 只在
`o.built === false` 時渲染，現行資料全是 `true`，所以永遠選不到，但規則有人用，
刪掉就是等資料一改就破。使用者已就這一條裁決為「移」。

| 行號 | 選擇器 | @media | 消費端（首處） |
|---|---|---|---|
| 41 | `.th-header` |  | `app-boot.js:36` |
| 774 | `.th-sidebar-row:last-child` |  | `apply-3.html:282` |
| 835 | `.th-footer` |  | `head.html:23` |
| 1043 | `.th-menumask` |  | `th-header.js:201` |
| 1052 | `.th-menupanel` |  | `th-header.js:202` |
| 1062 | `.th-menupanel-head` |  | `th-header.js:203` |
| 1072 | `.th-menupanel-head button` |  | `th-header.js:203` |
| 1077 | `.th-menupanel-head button:hover` |  | `th-header.js:203` |
| 1081 | `.th-menunav ul` |  | `th-header.js:210` |
| 1087 | `.th-menunav li` |  | `th-header.js:210` |
| 1091 | `.th-menunav a, .th-menunav .th-menunav-todo` |  | `th-header.js:210` |
| 1103 | `.th-menunav a:hover` |  | `th-header.js:210` |
| 1108 | `.th-menunav a.is-active` |  | `apply-1.html:47` |
| 1112 | `.th-menunav a i` |  | `th-header.js:210` |
| 1117 | `.th-menunav .th-menunav-todo` |  | `th-header.js:210` |
| 1121 | `.th-menuutil` |  | `th-header.js:236` |
| 1131 | `.th-menuutil button` |  | `th-header.js:236` |
| 1137 | `.th-menuutil button:hover` |  | `th-header.js:236` |
| 1333 | `.th-table .col-daterange` |  | `news.js:403` |
| 1343 | `.th-table .col-name` |  | `news.js:401` |
| 1353 | `.th-table tbody td.col-name` |  | `news.js:401` |
| 1451 | `.bulletin-tag--warn` |  | `news.html:133` |
| 1458 | `.bulletin-file` |  | `news.html:139` |
| 1473 | `.bulletin-file.pdf` |  | `news.html:139` |
| 1479 | `.bulletin-file.pdf:hover` |  | `news.html:139` |
| 1511 | `.bulletin-file.png, .bulletin-file.jpg, .bulletin-file.jpeg` |  | `news.html:139` |
| 1519 | `.bulletin-file.png:hover, .bulletin-file.jpg:hover, .bulletin-` |  | `news.html:139` |
| 1751 | `.th-lang-dropdown` |  | `th-header.js:140` |
| 1767 | `.th-lang-item` |  | `th-header.js:142` |
| 1783 | `.th-lang-item:hover` |  | `th-header.js:142` |
| 1788 | `.th-lang-item.is-active` |  | `apply-1.html:47` |
| 1794 | `.th-menu-lang-options` |  | `th-header.js:247` |
| 1802 | `.th-menu-lang-btn` |  | `th-header.js:249` |
| 1815 | `.th-menu-lang-btn.is-active` |  | `apply-1.html:47` |
| 2180 | `.th-expcard ul.th-expcard-nested a:not(.is-external)::before, ` |  | `th-quick-nav.js:132` |
| 2430 | `.th-linkrow.is-todo` |  | `campsite.html:33` |
| 2435 | `.th-linkrow.is-todo:hover` |  | `campsite.html:33` |
| 2513 | `.th-table-empty` |  | `th-data-table.js:33` |
| 2520 | `.th-table-empty i` |  | `th-data-table.js:33` |
| 2618 | `.th-legacy-tag` |  | `campsite.js:74` |
| 2759 | `.th-shot-placeholder` |  | `th-step-list.js:21` |
| 2770 | `.th-shot-placeholder i` |  | `th-step-list.js:21` |
| 5726 | `.fc-price-row:last-of-type` |  | `forest-camp-1.html:138` |
| 5816 | `.fc2-night-block.is-filled` |  | `forest-camp-2.html:79` |
| 5829 | `.fc2-night-block.is-filled .fc2-night-head` |  | `forest-camp-2.html:79` |
| 5851 | `.fc2-night-ok` |  | `forest-camp-2.html:83` |
| 5908 | `.fc2-facility-subtotal` |  | `forest-camp-2.html:95` |
| 5947 | `.fc2-payment-card.is-selected` |  | `forest-camp-2.html:113` |
| 5965 | `.fc2-payment-card.is-selected .fc2-payment-icon` |  | `forest-camp-2.html:113` |
| 5982 | `.fc2-payment-check` |  | `forest-camp-2.html:123` |
| 6038 | `.fc2-cost-detail` |  | `forest-camp-2.html:147` |
| 6044 | `.fc2-cost-detail-row` |  | `forest-camp-2.html:149` |

**處置建議**：階段 4 不動它們。要清掉的話得先補一次針對性互動探針
（開手機選單、開語言下拉、填 forest-camp-2 的每一晚、造出 `built: false` 的資料），
能觸發到的就是活規則直接留下，仍觸發不到的再逐條人工判斷。這件事與刪除無關，
可以排在階段 4 之後。


## 3. 第三類樣式（32 條）——逐條刪／移

`.th-chip`／`.th-select`／`.th-btn*`／`.th-section-title*`／`.th-table-note`。
**審核指出「移動」是一種操作講不通**，正確：

```
刪 ＝ 目的檔（components.css／pages.css）已有同選擇器且宣告等價 → 確認存在後刪 shared.css 那份
移 ＝ 四層 CSS 尚未接手 → 連同完整祖先限定與 @media 條件一起搬過去
```
判定順序是**目的檔存在性優先，死規則其次**。反過來排會把 L1296／L1665／L6143／L6151
誤算成「刪」——那四條的目的檔根本還沒接手，刪掉就沒人負責這些樣式了。

| 行號 | 動作 | 選擇器 | @media | 依據 |
|---|---|---|---|---|
| 309 | **移** | `.th-section-title` |  | 四層 CSS 尚未接手 |
| 320 | **移** | `.th-section-title-sub` |  | 四層 CSS 尚未接手 |
| 335 | **移** | `.th-btn` |  | 四層 CSS 尚未接手 |
| 353 | **移** | `.th-btn i` |  | 四層 CSS 尚未接手 |
| 358 | **移** | `.th-btn-primary` |  | 四層 CSS 尚未接手 |
| 365 | **移** | `.th-btn-primary:hover` |  | 四層 CSS 尚未接手 |
| 370 | **移** | `.th-btn-primary:disabled` |  | 四層 CSS 尚未接手 |
| 377 | **移** | `.th-btn-secondary` |  | 四層 CSS 尚未接手 |
| 382 | **移** | `.th-btn-secondary:hover` |  | 四層 CSS 尚未接手 |
| 386 | **移** | `.th-btn-ghost` |  | 四層 CSS 尚未接手 |
| 392 | **移** | `.th-btn-ghost:hover` |  | 四層 CSS 尚未接手 |
| 398 | **移** | `.th-btn-lg` |  | 四層 CSS 尚未接手 |
| 402 | **移** | `.th-btn-lg` |  | 四層 CSS 尚未接手 |
| 407 | **移** | `.th-btn-sm` |  | 四層 CSS 尚未接手 |
| 412 | **移** | `.th-btn-block` |  | 四層 CSS 尚未接手 |
| 460 | **刪** | `.th-input, .th-select, .th-textarea` |  | components.css L83 已有同選擇器，宣告等價 |
| 481 | **刪** | `.th-select` |  | components.css L101 已有同選擇器，宣告等價 |
| 485 | **刪** | `.th-field .th-select` |  | components.css L104 已有同選擇器，宣告等價 |
| 489 | **刪** | `.th-input:hover, .th-select:hover` |  | components.css L107 已有同選擇器，宣告等價 |
| 494 | **刪** | `.th-input:focus, .th-select:focus, .th-textarea:focus` |  | components.css L111 已有同選擇器，宣告等價 |
| 547 | **移** | `.th-chip-row` |  | 四層 CSS 尚未接手 |
| 553 | **移** | `.th-chip` |  | 四層 CSS 尚未接手 |
| 565 | **移** | `.th-chip:hover` |  | 四層 CSS 尚未接手 |
| 570 | **移** | `.th-chip.is-active` |  | 四層 CSS 尚未接手 |
| 1296 | **移** | `.bulletin-section-head .th-section-title` |  | 四層 CSS 尚未接手 |
| 1659 | **刪** | `.th-select, .th-input--date` | (max-width: 768px) | components.css L1520 已有同選擇器，宣告等價 |
| 1665 | **移** | `.bulletin-filter-row .th-btn` | (max-width: 768px) | 四層 CSS 尚未接手 |
| 3051 | **移** | `.th-table-note` |  | 四層 CSS 尚未接手 |
| 5116 | **刪** | `.p3-members-row .th-input, .p3-members-row .th-select` |  | pages.css L2504（已改名） 已有同選擇器，宣告等價 |
| 5187 | **刪** | `.p3-members-row .th-input::before, .p3-members-row .th-s` | (max-width: 900px) | pages.css L2673（已改名） 已有同選擇器，宣告等價 |
| 6143 | **移** | `.bulletin-tab.is-todo, .th-chip.is-todo` |  | 四層 CSS 尚未接手 |
| 6151 | **移** | `.bulletin-tab.is-todo:hover, .th-chip.is-todo:hover` |  | 四層 CSS 尚未接手 |

→ **刪 8 條、移 24 條**

### 3.1 與審核分類的逐項核對

| 行號 | 審核判定 | 本清單 | 核對結果 |
|---|---|---|---|
| 460 | 刪 | **刪** | 一致｜components.css L83 已有同選擇器，宣告等價 |
| 485 | 刪 | **刪** | 一致｜components.css L104 已有同選擇器，宣告等價 |
| 489 | 刪 | **刪** | 一致｜components.css L107 已有同選擇器，宣告等價 |
| 494 | 刪 | **刪** | 一致｜components.css L111 已有同選擇器，宣告等價 |
| 1659 | 刪 | **刪** | 一致｜components.css L1520 已有同選擇器，宣告等價 |
| 5116 | 刪 | **刪** | 一致｜pages.css L2504（已改名） 已有同選擇器，宣告等價 |
| 5187 | 刪 | **刪** | 一致｜pages.css L2673（已改名） 已有同選擇器，宣告等價 |
| 1296 | 移 | **移** | 一致｜四層 CSS 尚未接手 |
| 1665 | 移 | **移** | 一致｜四層 CSS 尚未接手 |
| 6143 | 移 | **移** | 一致｜四層 CSS 尚未接手 |
| 6151 | 移 | **移** | 一致｜四層 CSS 尚未接手 |
| 570 | 移 | **移** | 一致｜四層 CSS 尚未接手 |

**兩處要補正審核的說法**（都不影響刪／移結論）：

1. 審核把 L460／485／489／494／1659／5116／5187 歸為「確認已存在後直接刪」——正確，
   而且我逐條比對過宣告內容，**七條全部完全相同**（分號拆開後排序比對），
   不是只有選擇器相同。可以放心刪。
2. **`.th-chip` 在四層 CSS 裡一條都沒有**（`grep` 過 `components.css` 與 `pages.css`）。
   所以 L570 `.th-chip.is-active` 不只是「`.is-active` 要跟 `.th-chip` 綁在一起」，
   而是整個 chip 家族（L547 `.th-chip-row`、L553 `.th-chip`、L565 `:hover`、L570 `.is-active`）
   都得一起移。`.th-btn*`（13 條）、`.th-section-title*`（2 條）、`.th-table-note`（1 條）同理，
   目的檔皆為 0 條。
3. L6143／L6151 `.bulletin-tab.is-todo, .th-chip.is-todo` 落在 §2.4 的 B 級
   （瀏覽器選不到、但 `campsite.html:33` 有 `v-else` 分支在用）。**裁決「移」是對的**，
   我第一版自動分類曾把它算成「刪」，已修正為「已裁決優先於自動分類」。


## 4. `.fc-*`／`.fc2-*` 兩頁共用規則（14 條）——逐條刪／移

forest-camp-1／2 共用的 9 個 class，實際規則 14 條（含 `@media` 變體）。
**14 條全部是「移」**，四層 CSS 一條都沒接手。

| 行號 | 動作 | 選擇器 | @media | 依據 |
|---|---|---|---|---|
| 5385 | **移** | `.fc-layout` |  | 四層 CSS 尚未接手 |
| 5393 | **移** | `.fc-layout` | (max-width: 860px) | 四層 CSS 尚未接手 |
| 5397 | **移** | `.fc-aside` | (max-width: 860px) | 四層 CSS 尚未接手 |
| 5473 | **移** | `.fc-section` |  | 四層 CSS 尚未接手 |
| 5482 | **移** | `.fc-section-title` |  | 四層 CSS 尚未接手 |
| 5494 | **移** | `.fc-section-title i` |  | 四層 CSS 尚未接手 |
| 5514 | **移** | `.fc-stepper-input` |  | 四層 CSS 尚未接手 |
| 5523 | **移** | `.fc-stepper-btn` |  | 四層 CSS 尚未接手 |
| 5537 | **移** | `.fc-stepper-btn:hover:not(:disabled)` |  | 四層 CSS 尚未接手 |
| 5542 | **移** | `.fc-stepper-btn:disabled` |  | 四層 CSS 尚未接手 |
| 5547 | **移** | `.fc-stepper-val` |  | 四層 CSS 尚未接手 |
| 5568 | **移** | `.fc-avail-section .fc-section-title` |  | **落點改為 pages.css**：含要改名的 `.fc-avail-section`，必須與祖先同落點 |
| 5657 | **移** | `.fc-aside` |  | 四層 CSS 尚未接手 |
| 5738 | **移** | `.fc-footbar` |  | 四層 CSS 尚未接手 |
### 4.1 L5568 的處理（步驟 3a 已解決落點分裂）\n
`.fc-avail-section .fc-section-title` 同時含 `.fc-avail-section`（**forest-camp-1 專屬**，
markup 在 `forest-camp-1.html:123`）與 `.fc-section-title`（FC1／FC2 兩頁共用）。
**第 2 版誤標為「forest-camp-2 專屬」，本版更正。**\n
原本的處置自相矛盾：祖先 L5563 `.fc-avail-section` 歸 `pages.css` 並依 §6.2 改名，
子規則 L5568 卻保留舊祖先名搬去 `components.css` ——
**祖先一改名，子規則就再也命中不到任何元素，而且完全安靜**
（屬「驗收抓不到的那一類」：改名連 markup 一起改，三件驗收全過）。\n
**統一規則**：涉及改名的 class，其所有以該 class 為祖先或同一複合選擇器成員的規則，
**必須同一步處理、落點一致**。\n
⇒ **L5568 的落點由 `components.css` 改為 `pages.css`**，與祖先 L5563 同一步搬、同時改名。
整條一起搬，**不按 class 拆開**（維持裁決）—— 拆開會讓「在 avail-section 裡的
section-title」這個更具體的樣式失去限定，套到 forest-camp-1 的所有 section-title 上。\n
`.fc-section-title` 的**基底**規則（L5482、L5494）不受影響，仍照 §4 搬去 `components.css`
—— 那是 FC1／FC2 共用的元件樣式；L5568 是單頁對它的微調，
依「`pages.css` 的界線是這一頁的外觀」歸 `pages.css`。\n
**不變式已寫成斷言**（`split_check.py`）：65 個要改名的 token，
其全部存活規則必須落在同一個目的檔，違反就當場失敗。實測分裂數 **0**。\n

### 4.2 移動時的三個必要條件

1. **`@media` 條件一起帶**：14 條裡有 2 條在 `(max-width: 860px)` 內（L5393、L5397）。
   `components.css` 裡要建對應的 `@media` 區塊，不能把宣告攤平到外層。
2. **同一 class 的多條規則要維持相對順序**：`.fc-layout` 有 L5385（外層）與 L5393
   （`@media` 內）兩條，`.fc-aside` 有 L5397（`@media` 內）與 L5657（外層）兩條——
   **L5657 在 L5397 之後**，搬過去必須保持這個先後，否則 `@media` 內的宣告會被外層蓋掉。
3. **搬完當場驗證**：forest-camp-1／2 兩頁的像素與 computed 比對，加上 860px 寬各一次。


## 5. `.th-notice-grid` 改名

使用者已於裁決 2 解除改名封鎖，排進階段 4。**這是改名，不是刪除**：
`shared.css` 的 4 條刪掉、`pages.css` 已有的 4 條保留、markup 7 處改字。

### 5.1 `shared.css`（4 條，動作＝刪）

- L3207 `.th-notice-grid`
- L3214 `.th-notice-grid.is-secondary`
- L3218 `.th-notice-grid>*`
- L3261 `.th-notice-grid`　@(max-width: 640px)

### 5.2 `assets/css/pages.css`（4 條，已改名的副本，動作＝保留）

- L54 `.th-notice-grid`
- L60 `.th-notice-grid.is-secondary`
- L63 `.th-notice-grid>*`
- L92 `.th-notice-grid`　@(max-width: 640px)

### 5.3 markup（7 處，動作＝改 class 名）

| 檔案 | 行號 | 內容 |
|---|---|---|
| `applySearch.html` | 15 | `.th-chip-row／.th-notice-grid 與 Tailwind utility 組成，所以 pages.css 沒有` |
| `applySearch.html` | 18 | `.th-notice-grid 沿用原名（§6.6 白名單）：notice.html 也在用，改名會弄壞那頁。` |
| `applySearch.html` | 46 | `<!-- items-stretch 覆蓋 .th-notice-grid 的 align-items: start，` |
| `applySearch.html` | 48 | `<div class="th-notice-grid items-stretch">` |
| `notice.html` | 69 | `.th-notice-grid 維持原名：applySearch.html 也在用且尚未遷移，改名會弄壞那頁。` |
| `notice.html` | 72 | `<div class="th-notice-grid">` |
| `notice.html` | 85 | `<div class="th-notice-grid is-secondary">` |

**改名時 markup 與 CSS 必須同一個 commit。** 階段 3 犯過這個錯：
`.camp-daytable` 的 CSS 改了名、markup 還是 `camp-*`，`components.css` 的 `.th-table`
就因為同特異性但檔案在後而勝出，modal 列高 43→51px、166,104 個像素差。


## 6. 待刪 `.jsx`（28 支）

**數量是 28 支。** 第 1 版寫 26 支是漏掉 `template/`；後來一度是 29 支（含 `template/PageName.jsx`），2026-09-10 範本改寫時該檔已改為 `template/PageName.js` 並刪除，所以現在是 `components/` 的 28 支。
第 1 版寫 26 是因為只掃了根目錄 `*.html`，漏掉 `template/page.html`。

### 6.1 零引用的驗證方法

在語料中搜尋 `src="…<檔名>"` 形式的 `<script>` 標籤，**只出現在註解或說明文字裡的
檔名不算引用**——先前用純檔名 grep 曾誤判 17 頁公告仍在引用 `NoticeDetail.jsx`，
實際只是註解提到。

| 檔名 | `<script src>` 引用 | 僅文字提及 | DEPRECATED 檔頭 | 大小 |
|---|---|---|---|---|
| `Apply1.jsx` | 無 | 1 處 | 有 | 15,065 |
| `Apply2.jsx` | 無 | 1 處 | 有 | 28,976 |
| `Apply3.jsx` | 無 | 2 處 | 有 | 21,282 |
| `ApplySearch.jsx` | 無 | 1 處 | 有 | 15,342 |
| `ApplyTeach.jsx` | 無 | 1 處 | 有 | 5,049 |
| `Campsite.jsx` | 無 | 2 處 | 有 | 48,500 |
| `CampsiteData.jsx` | 無 | 1 處 | 有 | 93,109 |
| `CampsiteForestryData.jsx` | 無 | 1 處 | 有 | 87,538 |
| `CampsiteRouteData.jsx` | 無 | 1 處 | 有 | 18,524 |
| `CampsiteTarokoData.jsx` | 無 | 1 處 | 有 | 64,317 |
| `CampsiteYushanData.jsx` | 無 | 1 處 | 有 | 89,699 |
| `ConsentData.generated.jsx` | 無 | 1 處 | 有 | 98,106 |
| `ForestCamp1.jsx` | 無 | 4 處 | 有 | 10,942 |
| `ForestCamp2.jsx` | 無 | 1 處 | 有 | 14,340 |
| `ForestCampShared.jsx` | 無 | 2 處 | 有 | 4,651 |
| `Index.jsx` | 無 | 2 處 | 有 | 75,759 |
| `Information6.jsx` | 無 | 2 處 | 有 | 2,736 |
| `News.jsx` | 無 | 3 處 | 有 | 35,288 |
| `Notice.jsx` | 無 | 1 處 | 有 | 9,061 |
| `NoticeDetail.jsx` | 無 | 20 處 | 有 | 5,301 |
| `NoticeDetailData.jsx` | 無 | 1 處 | 有 | 208,057 |
| `OpenStatus.jsx` | 無 | 1 處 | 有 | 16,546 |
| `OpenStatusData.jsx` | 無 | 1 處 | 有 | 225,634 |
| `RouteData.jsx` | 無 | 3 處 | 有 | 112,834 |
| `Rss.jsx` | 無 | 1 處 | 有 | 8,777 |
| `Shared.jsx` | 無 | 36 處 | 有 | 35,023 |
| `TrailLevelData.jsx` | 無 | 1 處 | 有 | 3,410 |
| `WebIllustrate.jsx` | 無 | 2 處 | 有 | 7,916 |

### 6.2 兩件要更正第 1 版的事

1. **`Shared.jsx` 不是零引用。** `template/page.html` 仍以 `<script src>` 載入它
   （還有 `PageName.jsx`）。第 1 版說它零引用是錯的，因為我只掃了根目錄 `*.html`。
   **2026-09-10 範本改寫後這個前置條件已解除**：新的 `template/page.html` 不載入任何 `.jsx`，
   實測 28 支的 `<script src>` 引用數全部為 **0**。
   ⇒ **刪 `Shared.jsx` 的前置條件是先改寫 `template/page.html`**（§10.2）。
2. ~~`PageName.jsx` 沒有 DEPRECATED 檔頭~~ —— 該檔已於 2026-09-10 隨範本改寫刪除，
   改為 `template/PageName.js`（Vue 版）。剩下的 28 支檔頭皆有 `// DEPRECATED`。

### 6.3 資料檔的孿生現象（不要誤刪）

`RouteData`／`ConsentData.generated`／`CampsiteRouteData`／`NoticeDetailData` 四組都是
**`.js` 與 `.jsx` 同名並存**，現行頁面載入的是 `.js`（階段 0 轉的）。`.jsx` 那份是停用的孿生檔，
可以刪；但 `.js` 那份是活的，**不在本清單內**。


## 7. 產生器與驗收工具

### 7.1 所有這類腳本都在 scratchpad，不在 repo、不進版控

```
C:\Users\enzin.GIS\AppData\Local\Temp\claude\D--OneDrive…\72c0d700-…\scratchpad\ 
```
審核 agent 找不到它們是對的。**這一點本身就決定了缺口 3 的結論**：
scratchpad 會隨 session 消失，所以「留著 `.jsx` 好讓產生器能重跑」這個理由
在產生器本身留不住的情況下不成立。

repo 內另有兩支舊產生器在 `.scratch/outputs/`（`.scratch` 是 junction 且已 gitignore，
同樣讀不到、同樣不進版控）。

### 7.2 逐支實測能否重跑（缺口 3）

方法：確認工作區乾淨 → 跑該支 → `git status` 看有沒有改到任何已遷移的頁面 →
有改動就記下 diff 再 `git checkout` 還原。**不是讀程式碼推測，是實跑。**

| 產生器 | 讀取的 `.jsx` | 重跑結果 |
|---|---|---|
| `gen_apply1.py` | `Apply1.jsx` | ✔ 位元組相同 |
| `gen_index.py` | `Index.jsx` | **✘ 已失效**：會把 `index.html` 的 `v-cloak` 與 whitespace 規則註解退回去（6 插入／9 刪除） |
| `gen_news.py` | `News.jsx` | ✔ 位元組相同 |
| `gen_applysearch.py` | `ApplySearch.jsx` | ✔ 位元組相同 |
| `gen_applysearch2.py` | `ApplySearch.jsx、Shared.jsx` | ✔ 位元組相同 |
| `gen_fc1.py` | `ForestCamp1.jsx` | ✔ 位元組相同 |
| `gen_fc2.py` | `ForestCamp2.jsx` | ✔ 位元組相同 |
| `gen_apply2.py` | `Apply2.jsx` | ✔ 位元組相同 |
| `gen_apply2b.py` | `Apply2.jsx` | ✔ 位元組相同 |
| `gen_apply3.py` | `Apply3.jsx` | ✔ 位元組相同 |
| `gen_apply3b.py` | `Apply3.jsx` | ✔ 位元組相同 |
| `gen_campsite.py` | `Campsite.jsx` | ✔ 位元組相同 |
| `gen_campsite2.py` | `Campsite.jsx` | ✔ 位元組相同 |
| `gen_indexb2.py` | `Shared.jsx` | **✘ 已失效**：`ValueError: substring not found`，是一次性補丁腳本 |
| `.scratch/outputs/gen-notice-pages.py` | `NoticeDetailData.jsx` | **✘ 已失效**：找 `<div id="root" data-notice=…>`，現行頁面是 `#th-app`，替換命中 0 |
| `.scratch/outputs/build-notice-detail.py` | `（寫入）NoticeDetailData.jsx` | **✘ 已失效**：寫的是已停用的 `.jsx` 孿生檔（現行載入 `.js`），且需 `bs4` ＋ 連外網 |

**⇒ 12 支重跑位元組相同、4 支已失效。**
`gen_index.py` 是最值得記的一支：它「跑得起來」但輸出是**退化**的，
只看有沒有拋錯會誤判為有效。這正是審核那句「保留輸入不等於產生器仍有效」的實例。

### 7.3 結論與處置

- **不為產生器延後刪 `.jsx`。** 產生器不在版控裡，留著輸入沒有意義。
- 改為在刪除的 commit message 裡寫清楚：哪些頁是由哪支 `.jsx` 轉出來的、
  轉換方式（逐段切文字 ＋ `interp()` 轉插值 ＋ 中文覆蓋率斷言）。
  要回溯的人用 `git log` 就能找到 `.jsx` 的最後版本。
- 若使用者希望產生器本身留下來，那是另一件事：把還有效的 12 支整理進 `scripts/`
  並提交。**本輪不做**（未在任務範圍內）。
### 7.4 問題 a：哪幾支驗收工具會讀 `.jsx` 或 `shared.css`\n
**第 1 版這張表是錯的，本輪逐支 grep 重查。** 錯在兩處：說 `audit_copy.py`
讀 `.jsx`（它一行 `.jsx` 都沒有），說 `scan_66.py`／`tokens_check.py` 讀 `shared.css`
（前者讀 `pages.css`，後者讀 `tokens.css` 與 `design_system/colors_and_type.css`）。
\n
| 工具 | 讀 `.jsx` | 讀 `shared.css` | 實際讀什麼 | 刪除後 |
|---|---|---|---|---|
| `verify1.py`（像素＋computed） | 否 | 否 | 兩個 HTTP 埠：8081（參考 worktree）與 8080（工作樹） | **不受影響** |
| `audit_copy.py`（複製稽核） | 否 | **是**（5 處） | `shared.css` ＋ `components.css`／`pages.css`，比對「多選擇器規則被複製後，是否漏掉 shared.css 裡更後面的細化規則」 | **會失效**，但那正是它的任務結束 |
| `audit_media.py`（圖檔／連結 4xx） | 否 | **是**（4 處） | 三支 CSS 的 `url()` | 改讀兩支即可 |
| `scan_66.py`（§6.2 命名掃描） | 否 | 否 | `assets/css/pages.css` | **不受影響** |
| `tokens_check.py`（色票對照） | 否 | 否 | `tokens.css`、`design_system/colors_and_type.css` | 後者隨 `shared.css` 的 `@import` 一起退場後要改指向 `tokens.css` 單邊 |
| `cascade_check.py`（層疊共現） | 否 | 否 | 各頁面 URL，走瀏覽器 | **不受影響** |
| `dead_all.py`／`dead_split.py`／`remain.py`／`stage4_inventory.py` | `stage4_inventory` 掃檔名 | **是** | `shared.css` | **會失效**，階段 4 專用的一次性工具，用完即棄 |
| 12 支仍有效的 `gen_*.py` | **是** | 部分 | 各自的 `.jsx` 來源 | **會失效**，見 §7.3：不為此延後 |
\n
**⇒ 沒有任何「驗收」工具會因為刪 `.jsx` 而失效。** 三件驗收（像素／computed／console）
全部走瀏覽器，與 `.jsx` 無關。第 1 版提出的「刪 `.jsx` 前先跑最後一次中文覆蓋率」
這個建議**前提不成立**——中文覆蓋率不是獨立工具，是各支 `gen_*.py` 檔尾的內建斷言，
在階段 3 遷移當下就已經跑過並通過了，沒有「再跑一次」這回事。
\n
`shared.css` 刪除後真的要改的只有兩支：`audit_media.py`（拿掉一個路徑）與
`tokens_check.py`（拿掉 `design_system` 那一邊）。都是改一行。
\n
⚠ 另外查到一個與刪除無關、但會咬人的問題：`audit_copy.py` 從**另一個 session 的
scratchpad** import `css_split`
（`…\6f99e8dd-7040-…\scratchpad`）。那個目錄目前還在，但它隨時可能消失。
要繼續用這支工具的話得先把 `css_split` 併進來。


## 8. `shared.css` 的退場（缺口 4）

### 8.1 958 條的完整歸戶（先把帳算平）

| 分類 | 條數 | 動作 |
|---|---|---|
| A 級死規則 | 402 | **刪**（§2.3） |
| B 級待確認 | 52 | **保留不動**（§2.4） |
| `@keyframes` 步進 | 2 | 不是規則，隨所屬 `@keyframes` 一起處理 |
| 第三類 ＋ `.fc-*`（已逐條裁決） | 46 | 刪 8 ／移 38（§3、§4） |
| 其餘 → components.css | 362 | **移** |
| 其餘 → pages.css | 69 | **移** |
| 其餘 → **暫留 shared.css** | 13 | **移** |
| 其餘 → base.css | 7 | **移** |
| 其餘 → **暫緩（與 B 級同批）** | 6 | **移** |
| **合計** | **959** | 與 `shared.css` 解析出的 959 條相符 |

這張表是**窮盡且互斥**的（腳本裡有 assert 檢查總數），沒有任何一條規則沒有落點。

### 8.2 「暫留 shared.css」那 14 條是什麼

唯一消費端是 `_stage2-test.html`——使用者已確認保留的階段 2 驗收頁。
它沿用 campsite 改名前的 `.camp-*` 舊 class：

| 行號 | 選擇器 |
|---|---|
| 6242 | `.camp-cal-remain` |
| 6250 | `.camp-cal-unit` |
| 6255 | `.camp-cal-none` |
| 6266 | `.camp-cal-cell.is-open .camp-cal-remain` |
| 6281 | `.camp-cal-cell.is-full .camp-cal-remain` |
| 6314 | `.camp-daybody` |
| 6318 | `.camp-daytable` |
| 6322 | `.camp-daytable th` |
| 6333 | `.camp-daytable td` |
| 6340 | `.camp-daytable .camp-strong` |
| 6351 | `.camp-daynote` |
| 6425 | `.camp-cal-remain` |
| 6444 | `.camp-cal-unit` |

**這批既不能刪（有人用）、也不該進 `pages.css`（campsite 已改名為 `.p-campsite-*`，
再放一份 `.camp-*` 就是製造第二個真源）。** 兩個選項：

- (a) 把 `_stage2-test.html` 的 markup 改用 `.p-campsite-*`，這 14 條就變成可刪；
- (b) 保留 `shared.css` 成為只剩這 14 條的殘骸檔。

**建議 (a)**：驗收頁的用途是證明共用元件能獨立運作，改 class 名不影響那個用途，
而 (b) 會讓「shared.css 已退場」這件事永遠差最後一步。⇒ 待確認清單第 1 項。

### 8.3 三個退場步驟

#### 步驟 1：剩餘有效內容的處置

順序**必須是「先移、後刪 link」**，不能顛倒——link 一移掉，還沒搬走的規則立刻失效。

1. **`base.css`（6 條）** — 純元素選擇器 `html, body` / `body` / `button` /
   `input, select, textarea` / `img` / `a`。`base.css` 目前是空的，這 6 條是它的第一批內容。
   ⚠ 這是**唯一會影響全部 35 頁**的一步，改完要跑全頁比對，不能只驗抽樣。
2. **`components.css`（362 條）** — 共用元件在用的。
   分批搬，一個 class 家族一個 commit，每批搬完跑「用到該家族的頁面」的比對。
3. **`pages.css`（69 條）** — 只有單一頁面在用的，
   依 §6.2 一併改名 `.p-<頁名>-*`。**改名必須連 markup 一起改，同一個 commit**（見 §5 的教訓）。
4. **第三類 ＋ `.fc-*` 的 38 條「移」** — 照 §3、§4 的逐條表，
   帶完整祖先限定與 `@media` 條件。
5. **A 級 402 條 ＋ 第三類／`.fc-*` 的 8 條「刪」** — 直接刪，不需要搬。
6. **B 級 52 條 ＋ 暫留 13 條** — 不動，留到 §9 步驟 9 依裁決處理。\n
#### 步驟 2：移除 35 頁的舊 `<link>` 後驗證\n
**前置條件以 §9 為唯一來源。** 第 2 版在這裡另寫了一套（「步驟 1 的 1–5 全部完成，
且 `shared.css` 只剩 B 級 ＋ 暫留」），與 §9 步驟 10 的「**步驟 9 處理完 B 級與暫留之後**」
不一致 —— 前者允許那兩批尚未處置就移除 link，而本節 §8.2 自己又認定暫留那批
**仍有保留中的消費端**（`_stage2-test.html`）。兩者不能並存。\n
⇒ **移除 link 的前置條件＝§9 步驟 1–9 全部完成**，不在本節重述細節。
本節只描述「怎麼做」與「怎麼驗」，**條件一律看 §9**。
```html
<!-- 每頁 <head> 的靜態四行，目前： -->
Tailwind → tailwind.config.js → styles/shared.css → assets/css/index.css
<!-- 移除第三行後： -->
Tailwind → tailwind.config.js → assets/css/index.css
```
**驗證要三件事，逐頁都做**（與階段 3 的每頁驗收同一套）：

1. **全頁像素比對，1440 與 900 兩個寬度**。參考端是 git worktree at `8db90de`。
   ⚠ **比對前要把工作樹的 `shared.css` 複製進參考 worktree**（裁決 5）——
   但這一步之後 `shared.css` 已經幾乎空了，所以改成：**參考端固定為「移除 link 前
   的最後一個 commit」**，用 `git stash` 或第二個 worktree，不再用 `8db90de`。
2. **32 項 computed 屬性比對**，被列出的項目要附實際內容（裁決 5）。
3. **console 零錯誤，含 4xx**——移掉 link 之後最可能出現的就是 `shared.css` 404。

**層疊順序的風險**：目前的實測結論是「`shared.css` 在前、`index.css` 在後」。
移掉前者之後，`index.css` 變成唯一的靜態樣式表，Tailwind 的 runtime `<style>` 仍在最後。
這個順序沒有改變，**但要用 `cascade_check.py` 實測確認一次**，不要靠推論。

**回滾**：一頁一個 commit。任一頁比對失敗就 `git revert` 那一個 commit，
其餘已完成的頁不受影響。

#### 步驟 3：實際刪檔

前置條件（**全部滿足才能刪**）：

1. 35 頁的 `<link>` 都移除且驗證通過；
2. `template/page.html` 已改寫（§10.2）——它是唯一還只 link `shared.css` 的檔；
3. B 級 52 條有結論（§2.4 的針對性探針跑完，或使用者裁決「一律保留」→ 那就得搬去 `components.css`）；
4. `_stage2-test.html` 的 14 條有結論（§8.2 的 (a) 或 (b)）；
5. `styles/shared.css` **是使用者的未提交工作**（`git status` 顯示 ` M`）——
   **刪檔前必須經使用者確認**，不可自行處理。

刪檔本身：

```
git rm 05_Prototype/styles/shared.css
# 連帶檢查：styles/ 底下還有沒有別的檔（design_system/colors_and_type.css 是 @import 來源）
```
### 8.4 `@import design_system/colors_and_type.css` 的處置（已查證）

`shared.css` 第 1 行是 `@import url("../design_system/colors_and_type.css")`，
刪掉 `shared.css` 就等於同時抽掉這條 `@import`。該檔內容分兩部分：

| 內容 | 數量 | 是否已被四層接手 |
|---|---|---|
| CSS 自訂屬性（`--*`） | 92 個 | **是**。`tokens.css` 有 107 個，那 92 個**全部**都在裡面（差集為空） |
| 一般規則 | 12 條 | **否**。`body`／`h1, .h1`～`h4, .h4`／`p`／`a`／`a:hover`／`.label`／`.meta`／`code, pre, .mono`／`.section-title` |

⇒ **那 12 條就是 `base.css` 該有的內容**（`base.css` 目前是空的）。
與 §8.3 步驟 1 的 6 條純元素選擇器有重疊（`body`／`a`），搬過去要**合併**，不是各放一份。
`h1, .h1` 這種「元素 ＋ 同名 class」的寫法要整條帶，不能只留元素選擇器。

第 1 版漏掉整支 `design_system/`，因為它在 `styles/` 的**上一層**
（`05_Prototype/design_system/`），`@import` 路徑是 `../design_system/`。

## 9. 執行順序（十一步）

每步一個 commit，每 5 步一個 checkpoint。**任一步驗證失敗就 revert 該步，不往下走。**

| # | 動作 | 影響範圍 | 驗證 | 回滾 |
|---|---|---|---|---|
| 1 | ~~改寫 `template/`~~ **（2026-09-10 已完成）** | 只有範本，零頁面 | **§16 S6 的七條命題全部通過**（三件驗收在這一步不適用，理由見 S6） | revert 一個 commit |
| 2 | 刪 **28** 支 `.jsx`（§6） | 零（步驟 1 後已無 `<script src>` 引用） | 全 35 頁 console 零 4xx | revert |
| 3 | 刪 A 級 402 條 ＋ 第三類／`.fc-*` 的 8 條「刪」 | `shared.css` 只減不增 | 全 35 頁 × 1440／900 像素 ＋ computed | revert |
| 4 | `.th-notice-grid` 改名：刪 `shared.css` 4 條 ＋ 改 7 處 markup（§5） | 用到該 class 的頁 | 那些頁的三件驗收 | revert |
| 5 | 移 `design_system/colors_and_type.css` 的 12 條 ＋ 6 條純元素選擇器 → `base.css`（合併，§8.4） | **全部 35 頁** | 全 35 頁 × 兩寬度，**不可抽樣** | revert |
| — | **checkpoint 1** | | | |
| 6 | 移 300 條 → `components.css`，**一個 class 家族一個 commit** | 用到該家族的頁 | 每批：相關頁的三件驗收 | revert 該批 |
| 7 | 移 38 條（第三類 ＋ `.fc-*`）→ `components.css`，帶祖先限定與 `@media`（§3、§4） | chip／btn／section-title／table-note／fc 家族 | forest-camp-1／2 另加 860px | revert |
| 7b | **`.th-expcard*` 17 條重新複製回 `components.css`** | 快捷選單（全 36 頁） | `th-quick-nav` 的展開／收合、`.is-todo` 徽章、外部連結圖示，1440 ＋ 390 兩寬度 | revert |
| 8 | 移 136 條 → `pages.css` 並依 §6.2 改名 `.p-<頁名>-*`，**markup 同 commit** | 單一頁面 | 該頁三件驗收 | revert |
| 9 | 處理 B 級 52 條（§2.4）與 `_stage2-test.html` 的 14 條（§8.2） | 依裁決 | 依裁決 | revert |
| — | **checkpoint 2** | | | |
| 10 | 移除 35 頁的 `<link ... shared.css>`（§8.3 步驟 2） | 全部 35 頁，一頁一個 commit | 每頁三件驗收 ＋ `cascade_check.py` | revert 該頁 |
| 11 | `git rm styles/shared.css`（§8.3 步驟 3，**須使用者確認**） | 全部 | 全 35 頁 × 兩寬度 | revert |

**順序的三個硬約束**：

1. ~~步驟 1 必須在步驟 2 之前~~ —— **步驟 1 已於 2026-09-10 完成**，範本不再引用 `Shared.jsx`，實測 28 支的 `<script src>` 引用數全部為 0，此約束已解除。
2. **步驟 6／7／8（移）必須在步驟 10（移除 link）之前**——link 一移掉，還沒搬走的規則立刻失效。
4. **步驟 7b 的來由**：3.0b 曾把 `.th-expcard*` 複製進 `components.css`，
   但因為當時一併複製了**使用者尚未提交**的修改，於 2026-09-08 依裁決整批撤除，
   註記「等使用者提交那份修改後再複製回來」（`components.css` L1862–1877）。
   **該條件已於 2026-09-10 的 `0811527` 滿足。**
   現況是 `components.css` 缺這 17 條、`shared.css` 有——`shared.css` 一刪就沒了。
   **歸屬依現行判準重新確認過，不是「當初在那裡所以放回去」**：
   `.th-expcard` 家族 17 條**全部由 `th-quick-nav.js` 的 template 渲染**
   （`th-quick-nav.js:151-154`），**0 個頁面直接寫**，
   所以依「元件 template 實際渲染什麼」歸 `components.css`。
   複製時照 3.0b 的判準：**先確認每一條都存在於 HEAD**，只在工作樹裡的跳過並回報。
5. **步驟 5 單獨成一步且不與別的合併**——它是唯一影響全部 35 頁的樣式改動，混在別的 commit 裡就無法定位回歸來源。
## 10. 會把未來工作導回舊架構的檔（缺口 1）

### 10.1 已改寫的三支（本輪完成）

掃描範圍：repo 內所有 `*.md` ＋ `template/`，排除使用者的未提交工作
（`.agents/`、`04_Handover/`、`02_Spec/`、`.scratch/`）。**共 4 支承載慣例，3 支已改寫。**

| 檔案 | 原本說什麼 | 已改成 |
|---|---|---|
| `CLAUDE.md` | 「React 元件（Babel 轉譯的 JSX）」、「新頁一律從 `template/` 起手」、「頁面外殼用 `Shared.jsx` 的 `PageShell`」、「只有 `shared.css` 一支」 | Vue 3 全域建構 ＋ `thPage()` ＋ `app-boot.js` 契約；`.jsx` 標明已停用且**不要從它們起手**；四層 CSS ＋ `shared.css` 正在退場；**範本仍是 React 版，改寫完成前不要從 `template/` 起手**，新頁拿 `notice.html` 當範本；`<th-page-shell>` 取代 `PageShell`；`RouteData`／`ConsentData` 已轉 `.js` |
| `CONTEXT.md` | L31「React 18 UMD ＋ Babel」 | Vue 3 global build ＋ 四層 CSS |
| `assets/components/_README.md` | 「`components.css` 仍為空」 | 3.0b 已填入內容；`shared.css` 的重複規則待階段 4 處理 |

`CLAUDE.md` 另加了一條警告，因為它害我在階段 3 debug 過三次：

```
**`vue.global.prod.js` 不發任何 warning**：樣板編譯失敗與元件解析失敗在 console 上
完全安靜，只能靠 DOM 元素數或像素比對發現。改元件或樣板後不要只看 console。
```
`CLAUDE.md` 另加了兩條硬規則（2026-09-09 訂，見該檔「兩條硬規則」節）：

- **R1：工具的有效性用「輸出是否與現況一致」判定，不用「能不能執行」判定。**
  判準是跑完之後 `git diff` 是空的。依據就是 §7.2 的 `gen_index.py`。
- **R2：路徑層級的 `git checkout` / `git clean` / `git reset --hard` 一律禁止。**
  依據是 2026-09-09 21:08 的事故：為了實測產生器跑了 `git checkout -- 05_Prototype/`，
  把該目錄下全部未提交改動丟掉。計數邏輯有排除那些路徑，還原指令沒有。
  `shared.css` 靠裁決 5 留下的參考 worktree 副本救回（958 條規則、md5 核對一致），
  `hiking-site/` 那兩支無副本——經確認移植時讀的是工作樹（`gen_indexb.py:7-9` 用絕對路徑、
  `verify_indexb.py` 的驗收基準是 8080 工作樹），改動已進 `index-b.html`，無實質損失。
### 10.2 `template/` 的改寫契約（逐項對照使用者列的 5 點）

**尚未執行**（是 §9 步驟 1）。逐項核對：

| # | 使用者列的要求 | 現況 | 改寫後 | 核對 |
|---|---|---|---|---|
| 1 | 靜態順序 Tailwind → `tailwind.config.js` → `index.css` → `head-loader.js`；過渡期若仍載 `shared.css`，它在 `index.css` **前** | `page.html` 只 link `shared.css`，載 React／Babel | 四行靜態：Tailwind → `tailwind.config.js` → `styles/shared.css` → `assets/css/index.css`，再 `head-loader.js` | ✔ 與已遷移頁面一致。**順序有實測依據**：`index.css` 必須在 `shared.css` 後（理由見 `assets/css/index.css` 檔頭） |
| 2 | `head.html` 經 loader 載入，含 Vue、共用元件、`app-boot` | 無 loader，React／Babel 寫死在 `<head>` | `<script src="assets/includes/head-loader.js">`，Vue／`th-*.js`／`app-boot.js` 都由 `head.html` 帶 | ✔ |
| 3 | 單一 `#th-app` 加 `v-cloak`，涵蓋 header／內容／footer／modal | `<div id="root">` | `<div id="th-app" v-cloak>`，內含 `<th-header>`、`<th-page-shell>`、`<th-footer>`、modal | ✔ 契約：`th-header`／`th-footer` 在 `th-page-shell` **之外** |
| 4 | 頁面初始化交給 `thPage()` | `PageName.jsx` 用 `ReactDOM.render` | `template/PageName.js` 呼叫 `thPage({ data, computed, methods })` | ✔ **`.jsx` 換成 `.js`**，不是留一支 `.jsx` |
| 5 | 原 `PageName.jsx` 承載的頁面骨架也要接手，不能只換 `head` | 骨架（trail／title／lead／stepper）寫在 `PageName.jsx` 的 JSX 裡 | 骨架移到 `page.html` 的 in-DOM template；`PageName.js` 只留狀態與方法 | ✔ 這是 Vue 3 in-DOM template 的必然結果：版面留在 `.html`，只有共用元件的 template 是 JS 字串 |

**驗收方式見 §16 S6。** 範本是本清單裡唯一「執行後無法用三件驗收判定對錯」的一步：
它不是頁面（不被任何頁載入），也沒有「改版前」可比對。
所以改成七條可機械檢查的命題，全部通過才算完成。### 10.3 掃描方法與完整結果（缺口 1）

**定義**（使用者給的）：會被人或 agent 讀取當慣例、**但不影響任何現有頁面**的檔案。
這一類的危險在於「沒有任何驗收會抓到」——像素、computed、console 都不會動。

**掃描方法：用瀏覽器的實際請求紀錄，不用靜態解析。**
判準是「35 頁全部載入一遍，這個檔有沒有被要走」。
不用 grep 的理由：引用形式至少五種，靜態 regex 每漏一種就多一批誤判——
本輪連錯兩次（先漏了 `data-page-init="components/x.js"`，把 20 支活的頁面腳本
判成孤兒；再漏了 `fetch("assets/includes/head.html")`）。

**這支掃描自己也踩了一次坑**：8080 中途停掉，35 頁全部 `goto` 逾時，
於是它宣稱「每一個檔都是孤兒」而且完全不出聲。已加上「任一頁載入失敗就當場中止」
的斷言（一開始用「請求路徑數 ≥ 200」當門檻是錯的——185 個不重複路徑其實正常，
35 頁大量共用元件，那個門檻只是憑感覺訂的，會把正確結果擋掉）。

**結果：35 頁共載入 185 個路徑，沒有任何頁面載入的檔共 38 支。**

| 類別 | 支數 | 內容 | 處置 |
|---|---|---|---|
| A. 已停用的 React 版 | **28** | `components/*.jsx`（`template/PageName.jsx` 已隨範本改寫刪除） | §9 步驟 2 刪除 |
| B. 說明文件 | 5 | `CLAUDE.md`、`CONTEXT.md`、`assets/components/_README.md`、`decisions.md`、`階段4-刪除清單.md` | 前三支已改寫（§10.1）；`decisions.md` 是決策紀錄不承載慣例；本清單就是這一份 |
| C. 範本 | 1 | `template/page.html` | §9 步驟 1 改寫 |
| D. 移植來源 | 3 | `hiking-site/index.html`、`style.css`、`app.js` | 移植已完成，整個目錄在階段 4 刪除（見 §10.4） |
| **F. 其他（要逐支確認）** | **0** | —— | 沒有漏網的 |

**沒有第五類。** `design_system/colors_and_type.css` **不在孤兒清單裡**，
因為 `shared.css` 第 1 行 `@import` 它、瀏覽器確實會去要——這也反過來證明
這個掃描方法抓得到 `@import` 這種靜態 regex 容易漏的引用形式。

以下四支承載慣例但**確實影響頁面**，所以不在上表，改壞了驗收會抓到，
現況都已正確：

| 檔案 | 承載什麼慣例 |
|---|---|
| `assets/css/index.css` 檔頭 | 四層順序、`shared.css` 必須在 `index.css` 前的實測結論 |
| `assets/app-boot.js` 檔頭 | 掛載契約六條 |
| `assets/includes/head-loader.js` 檔頭 | 並行載入與 DOMContentLoaded 閘門的理由 |
| `assets/components/th-page-shell.js`／`th-data-table.js` 檔頭 | 文字節點合併造成的次像素反鋸齒差異 |
### 10.4 `hiking-site/` 的刪除前置條件（已驗證）

整個目錄排在階段 4 刪除。刪之前必須確認 `index-b.html` 與 `pages.css` 的
`.p-indexb-*` 區塊完全不依賴它。**用瀏覽器的請求紀錄驗證，不是 grep**——
CSS 的 `url()` 是相對於 CSS 檔位置解析的，寫 `../indexb-x.webp` 到底解到哪裡
只有瀏覽器知道。

| 檢查 | 結果 |
|---|---|
| `index-b.html` 執行期的請求總數 | 73 |
| 其中指向 `hiking-site/` 的 | **0** |
| 圖檔請求 | `indexb-journey.svg`、`indexb-mountains.webp`、`logo-mark.png` —— 全部在 `assets/` |
| 4xx／5xx | **0** |
| console error／warning | 1（Tailwind CDN 的 production 提醒，全站皆有，與本頁無關） |

圖檔確認是**複製**而非引用原位置，md5 逐一比對：

```
assets/indexb-mountains.webp  27ffc7d25384  ==  hiking-site/assets/mountains.webp
assets/indexb-journey.svg     31ce3a04a456  ==  hiking-site/assets/journey.svg
```

`hiking-site/assets/logo.png` 沒有被 `index-b` 使用（頁首改用共用元件 `th-header`
的 `logo-mark.png`），隨目錄一起刪即可。

原始碼裡剩下 4 處提到 `hiking-site`，**全部是註解**，刪目錄時一併更新措辭：
`index-b.html:9`、`components/index-b.js:3`、`assets/css/pages.css` 的區塊檔頭 2 處。

## 11. index-b 的 `!important` 與 alpha 色碼

### 11.1 現況

- **`!important` 18 處**，全部來自來源 `hiking-site/style.css`，照搬未動
- **含 alpha 的 8 位色碼 6 種**：`#233e4910`、`#244f4420`、`#2c435110`、`#436b5110`、`#ffffff60`、`#fffffff5`

原始 8 種中的 `#142a3b50`、`#182d3e88` 在 `dialog`／`#modal-content` 規則裡，
那兩條因為原生 `<dialog>` 未移植而已被丟棄。
### 11.2 為什麼不在階段 4 動它們

使用者裁決的遷移期界線是「**與改版前相同**」，而 index-b 的驗收基準是
`hiking-site/index.html` 本身。改掉 `!important` 或把 alpha 色碼收進 tokens，
兩者都會改變算出來的樣式，等於在遷移期做改善——**「改善不是遷移」**。

另有一個技術理由：這 18 處 `!important` 目前是靠 `:where(.p-indexb-body)`
零特異性前綴限定在單一頁面內。把它們拿掉需要重新推導整頁的層疊，
那是一件獨立的工作，不是刪除清單的一部分。
### 11.3 提案（**不在階段 4 執行**，供使用者決定要不要另立任務）

| 對象 | 提案 | 風險 |
|---|---|---|
| 18 處 `!important` | 逐處確認是否還有真的競爭對手（`:where()` 前綴已把外部競爭切斷），沒有的就拿掉 | 低——有 `hiking-site/index.html` 當基準可逐處比對 |
| 6 種 alpha 色碼 | 若某個 base 色已有 token，改寫成 `color-mix(in srgb, var(--x) 6%, transparent)`；否則新增 6 個 token 並記入 `decisions.md` | 中——`color-mix` 的算法與 8 位 hex 不保證位元相同，要逐處量 |

⚠ **8 位 hex 不可直接對應到不透明 token**。階段 3 踩過：把 `#ffffff60` 換成
`var(--white)` 會丟掉 alpha。當時的修法是排除 8 位 hex 不參與替換，
並用 `(?![0-9a-fA-F])` 的 lookahead 讓 `#ffffff` 不會誤蓋 `#ffffff60`。
## 12. 問題 b：從 shared.css「移」到 components.css 會不會反轉層疊

### 12.1 為什麼會問這個

`shared.css` 裡同特異性的兩條規則，後面那條勝出。搬到 `components.css` 之後，
如果兩條的先後被打亂，或其中一條留在 `shared.css`、另一條搬走，
勝負就可能反轉——而且**這種反轉在 console 上完全安靜**。
### 12.2 用什麼方法查

靜態分析先篩，瀏覽器實測定案：

1. **靜態**：找出所有「同特異性、且選擇器有可能同時命中同一個元素」的規則對，
   得到 8 組候選。
2. **瀏覽器**：對每一組，在 35 頁上跑 `querySelectorAll` 看**是否真有元素同時被兩條命中**
   （共現測試）。8 組裡有 7 組從未共現——它們只是選擇器長得像，實際上永遠不會撞。
### 12.3 結論：8 組候選裡只有 1 組真的會共現

```
移動者：.th-input, .th-select, .th-textarea   （shared.css L460，min-height: 42px）
覆蓋者：.th-textarea                          （shared.css L506，min-height: 96px）
覆蓋的屬性：min-height
共現頁面：apply-3.html（2 個元素）
```
兩條同特異性（各 1 個 class），後面那條勝出，所以 `<textarea>` 的實際最小高度是 96px。
其餘 7 組（`.th-chip.is-active` 對上 6 個別族的 `.is-active`、
`.p3-members-row .th-input` 對上 `.p3-members-row .row-del`）
在 35 頁上**從未有元素同時被兩條命中**——它們只是共用一個 token，實際永遠不會撞。
### 12.4 那一組不會反轉（已核對目的檔）

| | shared.css | components.css |
|---|---|---|
| `.th-input, .th-select, .th-textarea`（42px） | L460 | **L83** |
| `.th-textarea`（96px） | L506 | **L121** |

**目的檔已經是同樣的先後順序**（83 在 121 之前），而且 L460 的動作是「刪」
（§3 已核對宣告完全相同），不是「移」——所以這一組根本不會經歷搬遷。
`components.css` 已經把兩條都接手了，順序也對。

⇒ **問題 b 的答案：沒有任何規則會因為這次移動而反轉層疊。**
唯一一組真有共現的規則對，目的檔早已用正確順序接手。

但這個結論**只對已經比對過的目的檔成立**。§8.3 步驟 1 的 300 條要搬進
`components.css` 時仍可能製造新的反轉，所以下一節的三個防護要照做。
### 12.5 移動時的通用防護

1. **同一 class 家族一次搬完，不拆兩個 commit** —— 拆開的中間狀態就是「一半在前一半在後」。
2. **維持家族內的相對順序**（§4.2 第 2 點的 `.fc-aside` 就是實例）。
3. **每批搬完跑 `cascade_check.py`**（2026-09-10 已重寫，見下）。
### 12.6 `cascade_check.py` 的實際能力（R3：描述必須對照實作）

**第 2 版對它的描述是錯的。** 舊版只查固定 18 頁、**手寫的 8 組候選**選擇器，
只回答「這兩個選擇器有沒有命中同一個元素」，**沒有前後快照、沒有計算誰勝出**，
部分候選還用改名前的 `.p1-*`／`.p3-*`。它證明不了「搬移沒有反轉層疊」。

重寫後實際做的事：

| 項目 | 內容 |
|---|---|
| 涵蓋 | 全 36 頁 × 1440／900 兩寬度，**不預設候選**——每個元素每個屬性的勝出者都算 |
| 比什麼 | 「這個屬性最終由哪一條規則決定」＝ `樣式表\|選擇器\|值`，搬移前後各取一次快照 |
| 為何比來源不只比值 | 搬移最典型的錯誤是「兩邊都留一份」或「搬走了但目的檔沒放進去」：值完全相同，像素／computed／console 全過，但改一邊不生效且不報錯 |
| 跳過 Tailwind | 它的 `<style>` runtime 插在 head 尾端、永遠最後，搬移不改變相對位置，故其勝出的屬性必然不變 |
| 前提守衛 | 任一頁載入失敗即中止；元素紀錄數低於門檻即中止；**兩次快照的 DOM 形狀簽章不同即中止**（DOM 變了索引路徑就對不上，會產出大量假差異） |

**四個 bug 是靠負向測試才抓到的**，逐一記下來因為都會重犯：

1. **`document.styleSheets` 不含 `@import` 進來的樣式表**（它們是父表 `cssRules` 裡的
   `CSSImportRule`）。四層 CSS 全由 `index.css` `@import`，不處理這一支就**完全沒被走訪**，
   勝出者一律算成 `shared.css`。
2. **特異性要逐個逗號分段算**，取「元素實際命中的那一段」。整串一起算會虛高：
   `h2, .h2` 被算成 101 而壓過 `.th-card-title`(100)，但對 `<h2 class="th-card-title">`
   命中的是 `h2`，特異性只有 1。
3. **`pg.route("**/*")` 會拖慢 head-loader 的平行佇列**，造成部分頁面在等待時限內
   沒掛載完，被 DOM 守衛報成「DOM 變了」。只攔要改的那一支。
4. **不要等固定毫秒**。改成等 `#th-app` 的 `v-cloak` 被移除——機器負載一高，
   固定等待就會拍到「只有外殼」的半成品（實測 `notice_a10` 373 個元素變 15 個）。

**兩項負向測試（`py cascade_check.py selftest`）**：

| 測試 | 手法 | 結果 |
|---|---|---|
| 1. 值變了 | 攔截 `components.css`，追加一條 `.th-card-title { color: red }` | 偵測到 **12** 處，歸因正確（`components.css\|.th-card-title`） |
| 2. **來源換了、值相同** | 攔截 `components.css`，**整條拿掉** `.th-card-title` → 勝出者退回 `shared.css`，值一模一樣 | 偵測到 **84** 處，全部歸在「來源換了但值相同」 |

測試 2 才是這支工具存在的理由：那 84 處在像素、computed、console 上**完全看不出來**。
**沒有負向測試就不知道這支是不是啞的** —— 事實上它前三次跑都回報「零差異」，
而那三次全都是它自己壞了。
## 13. 問題 c：刪除後沒有「改版前」可比對，怎麼驗證

### 13.1 前提釐清：其實一直有基準

「沒有改版前」這個顧慮只在**最後一步（刪 `shared.css`）**成立。在那之前每一步都有基準：

- 步驟 1–9：參考端是 git worktree at `8db90de`（React 版），`.jsx` 與 `shared.css` 在那裡永遠存在。
- 步驟 10（移除 link）：**參考端改成「該步驟前的最後一個 commit」**，不再用 `8db90de`——
  因為此時 `shared.css` 已被大幅刪改，把它複製進舊 worktree（裁決 5 的做法）已經沒有意義。
- 步驟 11（刪檔）：參考端是步驟 10 完成後的 commit。

做法：開第二個 worktree 指向前一步的 commit，起在 8083 埠。
`verify1.py` 的參考 URL 改成 8083 即可，工具本身不用改。
### 13.2 三道獨立的閘門（缺一不可）

| 閘門 | 抓什麼 | 抓不到什麼 |
|---|---|---|
| 全頁像素比對，1440 ＋ 900 | 版面、字體、顏色、間距的任何可見變化 | 只在互動後或特定資料下才出現的分支（階段 3 的「 已填寫」前導空白就是這樣漏掉的） |
| 32 項 computed 屬性 | 像素比對看不到的計算值差異（`line-height`、`z-index`、`overflow`） | 沒有被列進 32 項的屬性 |
| console 零錯誤含 4xx | 檔案 404、JS 例外 | **Vue prod build 的樣板編譯失敗與元件解析失敗**（完全安靜，見 §10.1 的警告） |

三者的盲區不重疊，所以三件並行才是驗收。**這正是階段 3 的做法，刪除階段照用。**
### 13.3 刪除特有的第四道：規則層級的前後對照

像素與 computed 都是「結果」的比對，抓不到「規則沒了但剛好沒人用」與
「規則沒了而且有人用，只是那個狀態沒被走到」的差別。所以刪除階段另加一道：

```
刪除前：對 35 頁 × 兩狀態，記錄每個元素「勝出的宣告來自哪一條規則」
刪除後：同樣記錄一次，比對「來源規則」的集合
```

如果某個元素的勝出來源從「shared.css L1234」變成「components.css L56」，
而算出來的值相同——那是預期的（規則搬家了）。
如果變成「沒有任何規則」——那就是刪錯了，即使像素剛好沒變（例如被 Tailwind 蓋著）。

這一道用 `getMatchedCSSRules` 的替代方案實作：走 `document.styleSheets`
逐條 `matches()` 測試，再按特異性排序。**這支工具還沒寫**（⇒ 待確認清單第 3 項：
要不要為刪除階段補這支）。
### 13.4 不可逆風險的控制

1. **一步一個 commit**，35 頁的 link 移除是 35 個 commit——回滾的粒度就是一頁。
2. **每 5 步一個 checkpoint**：跑全 35 頁的三件驗收，不只驗當步改動的頁。
   理由：層疊是全域的，改 A 頁可能影響 B 頁（index-b 的全域洩漏就是這樣發現的）。
3. **`git rm` 前先確認使用者授權**——`styles/shared.css` 是使用者的未提交工作。

## 14. 待確認清單

| # | 事項 | 為什麼要你決定 | 我的建議 |
|---|---|---|---|
| 1 | `_stage2-test.html` 的 14 條 `.camp-*`（§8.2）：要改寫該頁的 markup，還是留 `shared.css` 當殘骸檔？ | 影響 `shared.css` 能不能真的刪掉。改 markup 動到你確認保留的驗收頁 | **改 markup**。驗收頁的用途是證明共用元件能獨立運作，換 class 名不影響那個用途；留殘骸檔會讓退場永遠差最後一步 |
| 2 | B 級 52 條（§2.4）：階段 4 一律保留不動，還是先補一次針對性互動探針把它們分完？ | 探針要造資料（`built: false`）、開手機選單、填 forest-camp-2 每一晚，是一件獨立的工作量 | **階段 4 一律保留**。它們留在 `shared.css` 不影響刪除，探針排在階段 4 之後 |
| 3 | 要不要為刪除階段補「規則層級前後對照」那支工具（§13.3）？ | 像素與 computed 抓不到「規則沒了但剛好沒人用」與「有人用只是沒走到」的差別 | **要補**。刪除是不可逆的，這是唯一能直接證明「沒有元素失去樣式來源」的方法 |
| 4 | 12 支仍有效的產生器（§7.2）：要不要整理進 `scripts/` 並提交？ | 它們現在只存在於 scratchpad，session 結束就消失 | **不強求**。真要保留的話是獨立任務；本清單的立場是不為它們延後刪 `.jsx` |
| 5 | `git rm styles/shared.css`（§9 步驟 11）需要你明示授權 | 那是你的未提交工作（`git status` 顯示 ` M`） | 到步驟 11 時我會停下來問，不自行處理 |
## 15. 本輪未做的事（明確聲明）

- **沒有執行任何刪除、移動、改名。** 唯一的檔案改動是 §10.1 的三支慣例檔改寫
  （`CLAUDE.md`、`CONTEXT.md`、`assets/components/_README.md`），那是缺口 1 要求的。
- §7.2 為了實測產生器，曾讓 `gen_index.py` 改到 `index.html`，**已 `git checkout` 還原**，
  工作區確認乾淨。
- `template/` 尚未改寫（是 §9 步驟 1）。
- B 級 52 條的針對性探針尚未跑（⇒ 待確認清單第 2 項）。

## 16. 自審（2026-09-10）

### S1. 缺口 1 掃出的同類檔案清單完整嗎？用什麼方法掃的？

**方法見 §10.3：瀏覽器實際請求紀錄，不是 grep。** 35 頁全部載入一遍，
收到 185 個路徑，沒被要走的 38 支就是「不影響任何現有頁面」的檔。

**完整性的依據**：候選集合是 `05_Prototype` 底下 11 組 glob 加上 repo 根目錄的 `*.md`，
逐檔比對是否在請求紀錄裡。分類後 **F 類（其他，要逐支確認）＝ 0 支**，沒有漏網的。

**這個方法有一個已知邊界**：只涵蓋「35 頁初始載入 ＋ 捲到底」。
只在互動後才被要走的檔會被誤判成孤兒。本專案目前沒有這種檔
（head-loader 把所有元件都排在初始佇列），但之後若引入延遲載入，
這支掃描要跟著加互動步驟。

**掃描本身踩過的坑已寫進 §10.3**：伺服器中途停掉時它宣稱「每個檔都是孤兒」
且完全不出聲；防護已改成「任一頁載入失敗就中止」。
### S2. 混族 12 條的刪／移，每一條的目的檔對應內容確實存在嗎？

**逐條查證，貼行號與宣告比對，不用「已有」帶過。**

**「刪」的 7 條 —— 目的檔存在且宣告完全相同：**

| shared.css | 選擇器 | @media | 目的檔 | 宣告比對 |
|---|---|---|---|---|
| L460 | `.th-input, .th-select, .th-textarea` | 無 | `components.css` **L83** | 10 項 vs 10 項，**完全相同** |
| L485 | `.th-field .th-select` | 無 | `components.css` **L104** | 1 項（`width: 100%`），**完全相同** |
| L489 | `.th-input:hover, .th-select:hover` | 無 | `components.css` **L107** | 1 項（`border-color: var(--slate-400)`），**完全相同** |
| L494 | `.th-input:focus, .th-select:focus, .th-textarea:focus` | 無 | `components.css` **L111** | 2 項，**完全相同** |
| L1659 | `.th-select, .th-input--date` | `(max-width: 768px)` | `components.css` **L1372** | 2 項，**完全相同**，`@media` 條件一致 |
| L5116 | `.p3-members-row .th-input, .p3-members-row .th-select` | 無 | `pages.css` **L2504**（已改名） | 3 項，**完全相同** |
| L5187 | `.p3-members-row .th-input::before, …` | `(max-width: 900px)` | `pages.css` **L2673**（已改名） | 5 項，**完全相同**，`@media` 一致 |

比對方式是把宣告以分號拆項、去空白、排序後比集合，不是比字串 ——
避免宣告順序不同被誤判成不同。七條都是逐項相同，不是「數量相同」。

**「移」的 6 條 —— 證明目的檔真的沒有：**

| shared.css | 選擇器 | 各 token 在四層 CSS 的條數 |
|---|---|---|
| L1296 | `.bulletin-section-head .th-section-title` | 兩個 token 都是 0／0 —— **都沒有** |
| L1665 | `.bulletin-filter-row .th-btn` | 兩個 token 都是 0／0 —— **都沒有** |
| L6143 | `.bulletin-tab.is-todo, .th-chip.is-todo` | `.bulletin-tab` 0／0、`.th-chip` 0／0；`.is-todo` 有 4 條但全是別族（`components.css` L1698,1702 的 `.th-linkrow.is-todo`；`pages.css` L240,332） |
| L6151 | 同上加 `:hover` | 同上 |
| L570 | `.th-chip.is-active` | `.th-chip` **0／0**；`.is-active` 有 8 條但全是別族（`components.css` L747,829,853,1938；`pages.css` L515,1823,1829,2409） |
| L5568 | `.fc-avail-section .fc-section-title` | 兩個 token 都是 0／0 —— **都沒有** |

**要補正審核說法的一點**：`.is-active` 在四層 CSS 有 8 條，但**沒有一條是 `.th-chip.is-active`**。
所以 L570 不只是「`.is-active` 要跟 `.th-chip` 綁在一起」的排版問題，
而是整個 chip 家族（L547 `.th-chip-row`、L553 `.th-chip`、L565 `:hover`、L570 `.is-active`）
目的檔一條都沒有，四條必須一起移。`.th-btn*`（13 條）、`.th-section-title*`（2 條）、
`.th-table-note`（1 條）同樣是 0 條，同理。
### S3. 刪除順序有沒有「做完 A 就無法驗證 B」的倒置？

把 11 步各標上「需要哪些驗證能力」與「會破壞哪些驗證能力」，依序模擬一遍，
檢查有沒有任何一步用到已被前面步驟破壞的能力。**倒置數：0。**

兩個被點名的先後關係：

**(a) 死規則清理（步驟 3）與 CSS 搬移（步驟 6–8）**
先清死規則是對的，而且**必須先清**：搬移的工作量與風險都跟規則數成正比，
先把 410 條（A 級 402 ＋ 第三類／.fc-* 判為刪的 8）確定沒人用的清掉，步驟 6–8 要搬的就只剩真的有人用的。
反過來會把死規則一起搬進 `components.css`，等於把垃圾換個地方放。
兩者不互相破壞驗證能力 —— 死規則刪掉後像素與 computed 都不會變
（這正是「死」的定義），所以步驟 3 的驗證不依賴步驟 6–8，反之亦然。

**(b) 刪 `.jsx`（步驟 2）與驗收工具失效**
§7.4 重查後結論改變：**沒有任何驗收工具會因為刪 `.jsx` 而失效**。
三件驗收全部走瀏覽器。會失效的是 12 支 `gen_*.py`，那是產生器不是驗收工具，
而且依 R1 不為它們延後。所以步驟 2 放在第 2 位沒有問題 —— 它唯一的前置是步驟 1
（改寫範本，因為 `template/page.html` 是 `Shared.jsx` 唯一的 `<script src>` 引用來源）。
### S4. 三層驗證在每一個刪除步驟上都適用嗎？

| 步驟 | 凍結基準 | 條件渲染狀態一起凍結 | 刪除前選擇器實證 | 缺哪一層 |
|---|---|---|---|---|
| 1 改寫範本 | **無**（範本沒有「改版前」） | 不適用 | 不適用 | **三層都沒有**，見 S6 |
| 2 刪 `.jsx` | 不需要（零引用） | 不適用 | 不適用 | 不需要 |
| 3 刪 410 條（A 級 402 ＋ 刪 8） | 前一 commit | 適用 | 適用 | 齊 |
| 4 `.th-notice-grid` 改名 | 前一 commit | 適用 | 適用 | 齊 |
| 5 移入 `base.css` | 前一 commit | 適用 | 適用 | 齊 |
| 6–8 搬移 | 前一 commit | 適用 | 適用 | 齊 |
| 9 B 級與 `_stage2` 14 條 | 前一 commit | 適用 | 適用 | 齊 |
| 10 移除 link | 前一 commit | 適用 | 適用 | 齊 |
| 11 `git rm` | 步驟 10 完成後的 commit | 適用 | **不適用**（檔案已空） | 缺第三層，但此時只剩空殼，不影響 |

**只有步驟 1 三層都缺**，這是 S6 的答案。
### S5. §8.1 總表的數字對得上了嗎？

**對上了：402 ＋ 52 ＋ 2 ＋ 46 ＋ 456 ＝ 958 ＝ `shared.css` 解析出的規則數。**

先前的 403／54／961 vs 953 是事故的副作用：`shared.css` 被退回 HEAD 版
（958→953 條），§8.1 在那個狀態下重跑，於是產出「合計 961、母體 953」
這種自相矛盾的表，而文件照樣寫了出來。

**根因不是數字算錯，是那張表沒有斷言。** 已在產生器裡補上
`assert tot == len(S.RULES)`，對不上就當場失敗、不產出文件。
`remain.py` 本來就有同樣的斷言，所以它在事故中有擋下來 —— §8.1 沒有，才漏過去。
### S6. 有哪一步是「執行後無法驗證是否正確」的？

**有一步：步驟 1，改寫 `template/page.html` ＋ `template/PageName.jsx`。**

理由：範本**不是任何頁面**。它不被載入（§10.3 的 C 類），所以三件驗收
沒有對象可跑；它也沒有「改版前」—— 現況是 React 版、改寫後是 Vue 版，
兩者本來就該不同，比對無意義。

第 1 版寫的驗證方式是「從新範本產一頁 `_tpl-test.html`，跑三件驗收，通過就刪掉」。
**這個方法不成立**：三件驗收要有比對基準，而 `_tpl-test.html` 是新產的，
沒有基準頁 —— 跟 React 範本產的頁比嗎？那兩者的 DOM 本來就不同。

**改法：把「範本正確」拆成可機械檢查的命題，逐條驗證，不做比對。**

| # | 命題 | 怎麼檢查 |
|---|---|---|
| 1 | 靜態四行的順序與內容與已遷移頁面一致 | 從 `notice.html`（結構最小的一頁）抽出 `<head>` 靜態四行，與範本逐字比對，必須完全相同 |
| 2 | `head.html` 經 loader 載入 | 範本含 `<script src="assets/includes/head-loader.js" data-page-init="…">`，形式與 32 頁一致 |
| 3 | 單一 `#th-app` 加 `v-cloak`，涵蓋 header／內容／footer／modal | 解析範本 DOM：`#th-app` 恰好 1 個且帶 `v-cloak`；`<th-header>`／`<th-footer>` 是它的直接子節點，且在 `<th-page-shell>` **之外** |
| 4 | 頁面初始化交給 `thPage()` | `template/PageName.js` 頂層恰好呼叫一次 `thPage(`，且不含 `ReactDOM`／`React.` |
| 5 | 頁面骨架接手，不是只換 head | 範本的 `<th-page-shell>` 帶 `trail`／`title`／`lead`；`PageName.js` 裡沒有任何 JSX 標籤 |
| 6 | 沒有把 React 帶回來 | 範本全文不含 `react`／`babel`／`text/babel`／`.jsx`（大小寫不敏感） |
| 7 | **真的能產出可用的頁** | 見下面「命題 7 的實際做法」 |

**命題 7 的實際做法（2026-09-10 已執行，22 項全過）。**
第 2 版寫的是「DOM 元素數 > 50」，**那個門檻自我反證**：
同一節自己引用的失敗案例（根層 `<template>` 變真元素）剩 78 個元素，會通過。
改為**逐項檢查具名的預期輸出**，每一項都是「這個東西在不在」，不是「總量夠不夠」：

| 群 | 檢查項 |
|---|---|
| 掛載真的發生 | `#th-app` 的 `v-cloak` 屬性**已被移除**（prod build 掛載失敗時 console 全靜，這是唯一可靠訊號）；`document.body.innerText` 無殘留 `{{ }}`；`th-header`／`th-footer`／`th-page-shell`／`th-page-nav`／`th-quick-nav` 五個自訂標籤在 DOM 中為 0（已被元件取代） |
| **初始化真的生效** | `thPage()` 的 `data` 有進到畫面（具名字串）；`navItems` 有渲染成頁內目錄；`app-boot` 沒有回報「宣告了 `data-page-init` 但 `thPage()` 從未被呼叫」 |
| `th-page-shell` 的具名輸出 | h1 標題文字相符、導言 `lead`、麵包屑 `.th-crumb` 含兩層、`updated` 日期、`#nav` 具名 slot 落到兩欄版面、預設 slot 的內容卡片 |
| `th-header` 的既有互動 | 語言下拉可開啟（`.th-lang-dropdown .th-lang-item` > 0）、Escape 可關閉；390px 手機選單可開啟（`.th-menupanel`）、開啟時 `body.th-noscroll` 上鎖、Escape 可關閉並解鎖 |
| 沒有把 React 帶回來 | **看實際發出的請求**，不是原始碼字串（範本註解裡就寫著「本頁沒有 React／Babel」，字串比對會被自己的註解判失敗）；另檢查 `script[type="text/babel"]` 節點數為 0 |
| 三件驗收中仍適用的 | console 零 error、零 warning（Tailwind CDN 的 production 提醒除外）、零 4xx／5xx |

**措辭更正**：第 2 版說「三件驗收在這一步都不適用」**過廣**。
`console`／4xx 不需要「改版前」基準，S6 自己也保留了它。
成立的說法是：**不能直接套用既有頁面的「前後比對」**（像素與 computed 需要基準，
而範本不是頁面、也沒有改版前可比）。console 這一件照跑。

**負向測試**：另建一頁 `data-page-init` 指向「載得到但沒呼叫 `thPage()`」的腳本，
確認斷鏈偵測真的會叫 —— 實測「掛載成功 True／外殼有渲染 True／資料進畫面 False，
斷鏈偵測有出聲 True」。沒有這道負向測試，那一項檢查可能是空的。
另掃全部 36 頁確認**零誤報**。
命題 1–6 是靜態檢查，寫成一支腳本；命題 7 走瀏覽器。
**七條全部通過才算步驟 1 完成**，不接受「看起來對」。

其餘 10 步都有可驗證的基準。步驟 11（`git rm`）雖缺第三層，
但此時 `shared.css` 已是空殼，前兩層足夠。

**S6 連帶要改的**：§9 步驟 1 的「驗證」欄原本寫「從新範本產一頁跑三件驗收」，
改為「§16 S6 的七條命題全部通過」。§10.2 末段的驗收方式同步改寫。
