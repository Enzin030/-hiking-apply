- [2026-08-24] 資料夾層級比照 `115嘉市災管/嘉市災管`：外層 `115登山一站式改版/` 只當容器（放 `00_Obsidian/`、`tasks.base` 與 OneDrive 共用的「楊庚翊」的檔案資料夾），`登山一站式/` 成為 repo root — 機關端往來文件是 OneDrive 共用資料夾（含 `desktop.ini`），不該被 repo 追蹤，把 repo 根往內收一層可從結構上避免。
- [2026-08-24] git dir 分離到 `D:\git-dirs\登山一站式.git`，repo 內 `.git` 改為 gitdir 指標檔 — OneDrive 同步 `.git` 目錄會產生 `config-LAPTOP-*`、`index-LAPTOP-*` 之類的衝突副本（本次遷移前已出現），把 git dir 移出同步範圍可根除。
- [2026-08-24] 舊雛形 `frontend/`、簡報產生器 `pptx_gen/`（含 node_modules）、舊規範 `gemini.md` 移入 `.scratch/`（junction 到 `D:\scratch-dirs\登山一站式`）— 這三者已非現行工作面，`.gitignore` 原本就註明「舊雛型先跳過，不追蹤」；`gemini.md` 的有效規則（繁中 commit、禁 Emoji、FontAwesome）先併入 `CLAUDE.md` 再移走，不直接丟棄。
- [2026-08-24] `登山一站式.rp`（Axure 原始檔，65MB）移入 `01_Raw_Input/` 並維持版控 — 歷史裡已有這個 blob，斷追蹤不會讓 `.git` 變小，反而失去誤刪偵測。
- [2026-08-24] `raw/*.csv` 維持在 repo 根的 `raw/`，不併入 `01_Raw_Input/` — 它們是 `components/RouteData.jsx`、`components/ConsentData.generated.jsx` 的 codegen 來源（見兩檔開頭註解），屬程式碼相依，不是機關提供的原始素材。
- [2026-08-24] 移除內層 `登山一站式/.git`（master@511dc3c）與外層 `inner-ui` remote — 該歷史已由 1e43775「整合登山一站式內層 Git 歷史」併入，511dc3c 即外層 `HEAD~2`，且無其他分支或 stash；`登山一站式` 成為 repo root 後 `inner-ui` 會指向自己。
- [2026-08-24] 雛形檔案（`index.html`、`apply-*.html`、`forest-camp-*.html`、`assets/`、`components/`、`styles/`、`design_system/`、`template/`）全部收進 `05_Prototype/`，repo 根目錄只留文件與資料夾骨架 — 比照 `115嘉市災管/嘉市災管/05_Prototype` 的作法，讓根目錄一眼看得出「哪些是交付文件、哪些是雛形」；整組一起搬，頁面之間的相對路徑不受影響（已用瀏覽器實跑 index／apply-1／forest-camp-1 驗證，無 404、無 console error）。
- [2026-08-24] `docs/` 整個移到 `.scratch/docs-舊規劃/`，退出版控 — 依使用者裁決。內含 `PRD.md`、`nationalpark-apply-implementation-plan.md`（679 行，舊系統 `nationalpark/HikeNationpark` 流程盤點）、`nationalpark-apply-db-request.md`（含 SQL 的機關資料索取清單）、`儀錶板.md`（後台儀表板規格含 RBAC）與三份 UI/UX 提案／簡報稿；後三者對應的雛形與簡報產生器已在 `.scratch`。正式規格一律改寫在 `02_Spec/`，舊草稿只作回查用。
- [2026-08-24] 前台 WBS 盤點不把頁尾連結視為功能 — 依使用者裁決；聯絡我們、隱私權宣告、資訊安全政策與資料開放宣告不列入本期缺口數。
- [2026-09-02] 登山須知的 31 項入口，階段一全部指向正式站絕對網址（`https://service.skyeyes.tw/hikenationpark/notice_a1~a10`、`notice_b1/b2/b3/b7/b4/b5/b6`、三份步道通訊點 PDF），階段二再逐頁換成本地新版詳細頁 — 原型把 20 個正式站既有入口誤標成「待確認」，使用者會讀成資料未完成；先恢復可操作性比等 17 張詳細頁全部改版完再上線務實。九九山莊是 `notice_b7.aspx`（排在 b3 與 b4 之間），已逐頁核對子頁標題，不得依畫面順序推算。
- [2026-09-02] 登山須知頁首「同一趟行程若跨越多個管理機關，須分別符合各機關規定」「進入山地管制區者另須申請入山許可證」標 `[待確認]` — 正式站 `notice.aspx` 無此文字，屬我方新增的業務規則；依 core.md §3.4 未取得來源前不得當成已確認規則，畫面上保留文字並標示，不改寫成模糊說法掩蓋。
- [2026-09-02] 四頁內容頁的檔名一律比照舊站 aspx（`guide.html`→`web_illustrate.html`、`trail-level.html`→`information_6.html`、`apply-teach.html`→`apply_teach.html`，`notice.html` 原就相符），元件檔同步改名（`Guide.jsx`→`WebIllustrate.jsx`、`TrailLevel.jsx`→`Information6.jsx`） — 依使用者裁決，方便新舊頁一對一對照；四頁當時皆未進版控，改名成本低。`index.html`／`apply-1~3.html`／`forest-camp-*.html` 已進版控且寫在 CLAUDE.md 關鍵路徑，維持現名不動。
- [2026-09-02] 尚未建置的頁面一律不給 `#` 假連結，改出非連結的「待建置」標記（`TodoLink` 元件＋`.th-todo-link`） — Header 工具列與主導覽、Footer 兩組清單、FAQ 內的操作入口都適用；看起來可點卻沒反應比明說未建置更糟。連帶把主導覽字級由 18px 降為 16px：六個項目加三個標記後，18px 在 1280 容器內塞不下同一行。
- [2026-09-02] 頁首「更新日期」（本頁）與頁尾「最後更新日期」（全站，抄自正式站的 2026-03-23）語意不同，不對齊 — 依使用者裁決；頁尾值由 `Shared.jsx` 十頁共用，硬改會蓋掉有來源的內容。實際上線時該值的維護方式標 `[待確認]`。
- [2026-09-02] 「兩種許可的差別」表保留但逐格標 `[待確認]` — 入園許可的法源（國家公園法第十九條）舊站原文有寫，可留；兩列的核發機關與入山許可的法源是比對登山須知頁機關分組推得的，加警示 Callout 並在儲存格內標示，不整段刪、也不當成已定案資訊發布。
- [2026-09-02] 「兩種許可的差別」表的法源與核發機關改為已查證，撤下 `[待確認]` — 查全國法規資料庫確認：國家公園法第 19 條「進入生態保護區者，應經國家公園管理處之許可」（核發機關為條文明文）；山地管制區與入山許可在**國家安全法第 6 條**，不是雛形原本寫的第 5 條 —— 111-06-08 全文修正前才是第 5 條，現行第 5 條是入出境檢查職權，網路資料多半還停在舊條號。核發機關依國家安全法施行細則第 31 條為「內政部警政署或該管警察局、警察分局、分駐所、派出所或保安警察第七總隊派駐國家公園分隊、小隊」，不只警政署一級，故不再簡寫成「警政署」。表下加出處註記。
- [2026-09-02] 主導覽在 1280px 以下改收合式選單（`th-menubtn` ＋ 右側滑出面板），取代原本 wrap 成兩行的降級 — 六個項目加三個「待建置」標記後，1280 容器內只容得下一行 16px 文字。順帶讓手機也有了導覽入口（原本 <1024 完全沒有主導覽，只有右下角「快捷選單」浮動鈕，那是另一個東西）。
- [2026-09-02] 重構「公佈欄」為新聞與公告中心（`news.html` / `News.jsx`），整合 4 大 Tab（最新消息、違規名單、檔案下載、常見問答），採用單層 Header 導覽 + 頁內頂部 Segmented Control 切換；麵包屑與標題對齊 `apply-1.html`，發布單位維持 6 個標準單位，發布日期支援起訖區間查詢，無底卡包覆，所有樣式統一歸併至 `styles/shared.css`。
- [2026-09-02] 17 張詳細須知頁改採「機器轉檔」而非人工謄寫 — 內容約 4.4 萬字、含 20 張表格，人工抄寫必然掉字。`.scratch/outputs/build-notice-detail.py` 直接讀正式站 HTML 轉成 `NoticeDetailData.jsx` 的結構化資料（展開 rowspan／colspan 並記錄合併位置、保留每個 `<a href>`、依 `<br>` 切行、容器層裸文字不得略過），版型由單一 renderer `NoticeDetail.jsx` 負責。驗收方式是 Playwright 逐字元比對本地頁與正式站，17 頁全部 0 差異；日後正式站改版重跑腳本即可。
- [2026-09-02] 詳細須知頁的內文連結用 `--national-800`（#166534）而非品牌 sage `--national-700`（#587a68） — sage 與內文 `--fg-2`（#475569）明度太接近，長文裡看不出是連結；先試過 `--btn-blue` 但與整站綠色調不協調（使用者裁決）。未新增顏色 token。
- [2026-09-02] 臺中市登山活動管理自治條例連結改指 `law.taichung.gov.tw`，與正式站不同 — 正式站用的 `lawsearch.taichung.gov.tw` 整個網域已失效（連根目錄都 404），法規站搬到新網域。正式站自己也連到死網址，此處刻意不照抄。新網址已抓回內容確認是該法規的法規內容頁。
- [2026-09-02] 公佈欄四頁籤改為共用同一套骨架（篩選卡 → 區塊標題 → 表格 → 分頁），比照正式站 `hike.taiwan.gov.tw` 的 news_0／5／6／7 四頁 — 原本最新消息是卡片列表、違規名單是查詢卡＋表格、檔案下載是表格、常見問答是 accordion，四種版型各長一樣。表格改用 `bulletin-*` 命名並以 `columns` 定義驅動，四頁只差欄位不差版型；表頭沿用品牌 sage `--national-700`，不照抄正式站的深藍。篩選改「草稿→按查詢才套用」與正式站表單行為一致（原本即時篩選會讓查詢鈕變裝飾），分頁為真實 client-side 分頁（pageSize 10）。FAQ 捨棄 accordion 改表格＋彈窗、個人違規查詢卡保留但樣式統一為使用者裁決。
- [2026-09-02] 登山路線開放狀態（`open.html` / `OpenStatus.jsx`）的資料採機器轉檔，不手抄 — `.scratch/outputs/build-open-status.mjs` 在正式站 `open.aspx` 實跑「全部機關」查詢，取回 11 欄 × 479 列並逐列抓 `linenote.aspx` 的備註，輸出 `components/OpenStatusData.jsx`；正式站改版重跑腳本即可。驗收數字：太管處 21／雪管處 32／玉管處 18／自然保留區 16／自然保護區 6／野生動物保護區 2／國家步道(山屋/營地) 4／警政署入山 380。
- [2026-09-02] `OpenStatusData.jsx` 不與 `RouteData.jsx` 做 join，接受 71 列（三處國家公園）內容重疊 — join key 只有路線名稱，而正式站的名稱有標點變體（如「玉山前峰單日往返(塔塔加 - 玉山前峰 - 塔塔加 )」右括號前多一個空白），本專案零 build、沒有東西會在載入時報 join 失敗，錯了不會被發現。兩檔各自獨立維護，來源都寫在檔頭註解。
- [2026-09-02] 機關篩選鈕與表格的機關欄位不是 1:1，資料同時存 `orgLabel`（表格顯示的 8 種）與 `filterKey`（對應 6 顆鈕） — 「林業及自然保育署自然保護區域」一顆鈕實際涵蓋三個相鄰 GUID（尾碼 a85/a86/a87），對應自然保留區／自然保護區／野生動物保護區三種 `orgLabel`，正式站點該鈕實測共 24 列。按鈕本身只送 a85，轉檔時需補上另兩個，否則 8 列的 `filterKey` 會是空的、篩選漏筆。
- [2026-09-02] 本頁載入即列出全部 479 列（分頁呈現），刻意不照抄正式站「按查詢前顯示尚無資料」 — 進頁看到空表格是舊站要修的體驗問題，不是要保留的行為。
- [2026-09-02] 路線下拉改由 479 列的主路線衍生（432 條），不照抄正式站的路線下拉 — 正式站下拉 447 條全在表格內，但表格另有 25 條（外籍提前變體、部分警政署路線）在下拉裡選不到，是正式站自己的資料落差；改為衍生後每一列都篩得到。
- [2026-09-02] 備註欄的兩種正式站雜訊在轉檔時就地處理：備註全文「無」正規化為空字串（渲染端據此不出備註鈕）；「部分關閉原因」中的 `asdasd` 等測試字串剔除並在 console 列出（4 列，皆為太管處奇萊系統路線） — 逐列轉檔會把生產環境的垃圾資料原樣帶進雛形。
- [2026-09-02] 路線現況保留正式站圖例的四種狀態（本日開放／本日關閉／依規檢附登山經驗證明／雪季），但實資料目前只出現前兩種 — 後兩種在 `STATUS_META` 留定義、圖例照樣呈現，不為了展示而捏造樣本列（core.md §3.4）。同理「可否申請」與「路線現況」是獨立兩欄，實資料存在「可申請＋本日關閉」的組合（奇萊主北峰線），不合併成單一 badge、不「修正」。
- [2026-09-02] 狀態膠囊獨立成 shared.css 的 `.th-flag`（is-yes／is-no／is-open／is-closed／is-proof／is-snow），不留在頁面層 — 篩選卡、表格、分頁都是沿用既有元件，狀態膠囊才是本頁唯一新造的原件，也是宿營地與床位查詢那 8 張頁最需要複用的那塊；留在 `open.css` 並叫 `open-flag`，下一頁只能複製一份。
- [2026-09-02] 查詢型頁面版型沿用公佈欄那套 `bulletin-*`（篩選卡／chip／表格／分頁／彈窗），不另造一套 — 依 core.md §7 不重造既有元件；`bulletin-` 這個前綴對非公佈欄的頁面語意不符，屬命名債，待「宿營地與床位查詢」8 張子頁一起改版時再一次改名，不在本次動已交付的頁面。
- [2026-09-02] 分頁元件 `BulletinPager` 由 `News.jsx` 移入 `Shared.jsx` 並改為視窗式頁碼（目前頁前後各兩頁＋首末頁，中間 `…`） — 路線開放狀態 479 筆共 24 頁，原本把頁碼全部列出會比表格還寬；公佈欄僅 2 頁，行為不變。
- [2026-09-02] 難度等級說明由 `Information6.jsx` 抽成 `components/TrailLevelData.jsx`，供步道分級表與本頁的難度彈窗共用 — 正式站 `linelevel.aspx` 彈窗與 `information_6.aspx` 表格是同一組文字，兩頁各抄一份必然走鐘。
- [2026-09-02] 「功能」欄的「申請」統一連 `apply-1.html`，不帶路線參數 — `Apply1.jsx` 目前不讀 URL 參數，要做預選路線得先與 `RouteData.jsx` join（見上），成本與風險都不在本頁範圍；「介紹」對應的路線介紹頁尚未建置，依既有決策出「待建置」標記不給 `#`。
- [2026-09-02] 宿營地與床位查詢的 11 張正式站子頁（`bed_0/1/3/4/5/6/7/8/9/10/11.aspx`）收成單一 `campsite.html`，上層機關 Tab ＋ 次層類別子選 — 依使用者裁決。不做 11 張獨立頁（導覽列要掛 11 個子項目），也不做入口頁（多一層點擊）。本次只實作雪霸宿營地（`bed_1`），其餘 Tab 與子選出「待建置」標記不給假連結。
- [2026-09-02] 月曆格以餘額為主角（大字＋狀態色），其餘計數收進點格後的明細彈窗 — 依使用者裁決。正式站每格直接堆 3～7 行「標籤 數字」，雪霸 7 項時列高破百、行動版幾乎不可用；欄位一項不減，只是改成兩層呈現。
- [2026-09-02] 餘額只分「尚有餘額」與「已無餘額」兩級，不做「剩少量」之類的中間級距 — 正式站只提供餘額數，未提供每日承載量，算不出比例；級距門檻是業務規則，未取得前不編造（core.md §3.4）。
- [2026-09-02] 月曆的月份切換做成「待建置」而非可點按鈕 — 雛形資料是單月快照，切月沒有資料可換；做成點了沒反應的按鈕比明說未建置更糟（比照 2026-09-02 的待建置連結決策）。餘額的快照日期同時標在畫面的警示 Callout 與資料檔檔頭，不假裝即時查詢。
- [2026-09-02] `CampsiteData.jsx` 的計數改存與 `labels` 同序的值陣列，明細網址由 `orgid`＋`node_id`＋`sdate` 在渲染端組出 — 原本每天各存一份標籤與完整網址，檔案 593KB，超過 `@babel/standalone` 的 500KB 門檻會在 console 出 deoptimise 警告並拖慢載入；改成精簡結構後 91KB。
- [2026-09-02] `shared.css` 的 `.th-page-inner` 由 `max-width: 1280px` 改為 `max-width: min(1280px, 100%)` 並加 `min-width: 0` — 內容頁的 `.th-page` 是 grid（`content.css`），這個元素是 grid item，只寫 1280px 時會被內容的 max-content 撐開再夾在 1280，宿營地頁的「林業及自然保育署」長 Tab 讓 390px 下整頁橫向溢出到 627px。夾回 100% 後 Tab 列的 `overflow-x: auto` 與表格卡片化才輪得到。這是既有版型的潛在問題，路線開放狀態頁沒有這麼長的 min-content 才沒發作。
- [2026-09-02] 查詢型頁面的區塊標題改用既有的 `.th-section-title`（與申請頁同一套左色條），不再用 `.bulletin-section-title` — 後者已於公佈欄改版時移除（`.bulletin-section-head` 現在是純右靠的筆數列），路線開放狀態頁因此有一段時間標題沒有樣式、被擠到右側。新增 `.bulletin-section-head .th-section-title` 一條規則把它推到最左並去掉下邊距，兩個既有 class 都不改寫。
- [2026-09-03] 依使用者指示，公佈欄違規名單頁面（`news.html?tab=violation`）拿掉「個人違規與停權紀錄查詢」卡片區塊與相關 logic/CSS，回復公佈欄四頁籤單一標題/篩選卡/表格的純粹版型。
- [2026-09-03] Header 工具列與選單增加語言切換下拉選單，支援「繁體中文」、「English（英文）」與「日本語（日文）」三種語系切換。
- [2026-09-03] Header 的 Logo 圖片統一使用與 `index.html` 一致的國家公園署 base64 標籤檔；工具列「警特報」項目改為外連至中央氣象署警報專區（`https://www.cwa.gov.tw/V8/C/P/Warning/FIFOWS.html`）。
- [2026-09-03] 首頁與申請流程以外的頁面，頁首統一為查詢型頁面（`news.html?tab=news`）的版型：`Breadcrumb` ＋ `.th-page-inner > h1.th-page-title`，`Shared.jsx` 的 `PageHero`（照片橫幅 `.th-hero`）改名並改寫為 `PageHead`，導言與更新日期改放 `h1` 底下（`.th-page-lead`／`.th-page-meta`）— 全站原本兩套頁首並存，照片橫幅只出現在 5 支內容頁，標題字級、左色條與麵包屑位置都跟其餘頁面對不上。`content.css` 的 `.th-hero*` 一併移除（無其他使用者）。
- [2026-09-03] `open.html`／`campsite.html` 的 `<main>` 加 `th-page-plain`，還原 `shared.css` 的純外框 `.th-page` — 這兩頁為了 `.th-level`、`.th-inline-link` 而載入 `content.css`，卻連帶吃到內容頁專用的 `.th-page` grid（`padding: 32px`），標題因此比麵包屑多縮 32px，與 news 對不齊。修的是 CSS 撞名，兩頁 JSX 結構本來就與 news 相同。
- [2026-09-03] RSS 訂閱頁（`rss.html` / `Rss.jsx`）的改版核心是「補上正式站沒有的訂閱網址清單」，而非重排版面 — 正式站 `RssWeb.aspx` 通篇只有麵包屑、標題與「何謂RSS?」「如何訂閱RSS」兩段文字，**一個可訂閱的網址都沒列**，看完仍不知道要貼什麼進閱讀器。改版以「可訂閱的頻道」表格（頻道／內容／訂閱網址＋複製鈕／對應頁面）為主體，說明文字退居其後。版型取內容頁那套（`th-page has-nav` ＋ `SectionCard` ＋ `PageNav`），不套公佈欄的 `bulletin-*` 查詢骨架 — 本頁沒有列可篩、可分頁，篩選卡與分頁沒有東西可操作。
- [2026-09-03] 五個頻道中只有「最新消息」給網址，其餘四個標「待建置」 — 正式站全站唯一的 RSS 端點是 `news_0.aspx` 標題右側連出的 `rss.aspx`，其他頁籤與路線開放狀態沒有對應 feed。不編造 `rss.aspx?type=…` 之類的參數化網址（core.md §3.4）；頁面上出現的每個網址都是實際抓取驗證過的。
- [2026-09-03] `rss.aspx` 實測回傳全站的「系統維護中」頁而非 XML（HTTP 200，同時間 news_0／5／6／7 皆正常，故為該端點自身失效），照實在表格內標註而非隱藏或改連別處 — 與臺中市自治條例那筆（正式站連死網址、我方改連新網域）不同：那筆有可用的替代網址，這筆沒有，能做的只有據實呈現。各頻道的供應範圍與更新頻率標 `[待確認]`。
- [2026-09-03] 「如何訂閱」改寫為現行做法（擴充套件／網頁版閱讀器／行動裝置 App），捨棄正式站原文的 IE 外掛與 Firefox Sage — 兩者皆已停止維護，照抄等於教使用者裝不存在的東西。「何謂RSS?」的定義段落逐字保留並註明出處，只動操作教學、不動定義。
- [2026-09-03] `Shared.jsx` 的 `HEADER_UTILITY` 加 `external` 旗標 — 原本有 url 就一律 `target="_blank"`，那是為外連的警特報寫的；RSS 接上站內 `rss.html` 後會在新分頁開自家頁面。內部連結不帶 target／rel。
- [2026-09-03] 麵包屑不再於 trail 首位放文字「首頁」（`news`／`open`／`campsite` 三頁移除） — `Breadcrumb` 元件本身已渲染首頁圖示連 `index.html`，兩者並存等於首頁出現兩次；正式站 `news_0.aspx` 實測就是「首頁圖示 > 公布欄 > 最新消息」。只改呼叫端的 trail 陣列，不動 `Breadcrumb` 元件——`index.html` 另有一份內嵌副本（第 848 行），改元件會讓兩份行為分岔。
- [2026-09-03] 麵包屑層級改對齊正式站 `site_map.aspx` 的網站導覽：一級選單在正式站沒有自己的頁面時（登山申請、公佈欄、宿營地與床位查詢）維持純文字不給連結，有頁面的中間層級才給 href。申請流程四頁（`apply-2`／`apply-3`／`forest-camp-1`／`2`）的中間層級「登山線上申請」抽成 `Shared.jsx` 的 `APPLY_CRUMB` 常數並指回 `apply-1.html`，四頁共用一份，不各寫一次。
- [2026-09-03] 麵包屑與 h1 沿用專案既有用語「公佈欄」「登山線上申請」，不改成 `site_map.aspx` 的「公布欄」「各項線上申請」 — 前者是全站數十處的既有命名，後者若只改麵包屑會與同頁 h1 打架；兩筆用語差異列為待使用者裁決，本次不動。
- [2026-09-03] `Apply2.jsx` 申請前摘要頁的 `style={{ maxWidth: 680 }}` 由 `.th-page-inner` 移到新的頁面層 class `.p2-summary-narrow`（`apply2.css`），只夾住 Stepper 以下的內容 — 原本掛在 `.th-page-inner` 會讓標題區只有 680 而麵包屑仍是 1280，頁首左緣對不齊（實測 h1 與麵包屑分別起於 112 與 112 才算齊，修前不齊）；同時解掉 core.md §7 的 inline style 違規。標題與 Stepper 改為與其他申請頁同寬。
- [2026-09-03] 全站 LOGO 收斂為單一來源 `05_Prototype/assets/logo-mark.png`（綠山＋藍水＋紅圈，88×88 RGBA）— 原本同一個站跑著兩顆不同 logo、共 5 處副本且 4 處是內嵌 base64：Header（`Shared.jsx`、`index.html`）用的是**沒有**藍水與紅圈的舊版 mark，首頁 Footer／hero 用完整版 mark，內頁 Footer 卻用含中文字的盾形標誌 `assets/logo.png`。5 處全部改為引用同一支檔案，換 logo 從此只需替換一個檔。
- [2026-09-03] Footer 的 logo 由盾形標誌改為綠山 mark — Footer 外層是 `w-20 h-20 bg-white rounded-full` 的白色圓底容器（`Shared.jsx`），盾形標誌是 301×289 的橫向構圖，塞進 64px 高時中文字全糊、四角擠出圓形，而旁邊本來就有同樣文字的 `<h3>`，等於重複；正方形的 mark 才貼合圓底。
- [2026-09-03] `index.html` 是刻意的自足單檔（`<title>` 標「單檔版」、不引用任何本地 CSS／JSX，hero 背景亦為內嵌 jpeg），因此它的三處 logo **維持內嵌 base64**，但改由 `assets/logo-mark.png` 機械產生（三處位元與該檔完全相同）— 一度改成 `src="assets/logo-mark.png"` 會讓它第一次產生本地檔案相依、失去單檔性質。日後換 logo 的正確做法：換掉 `assets/logo-mark.png`，再把它 base64 編碼回填 `index.html` 的三處。
- [2026-09-03] 首頁實際渲染的頁首是 `index.html` 自己的 `HomeHeader`；同檔內另有一份 `Header`，是 `Shared.jsx` 那份的內嵌複本（第 962 行 `Object.assign(window, { Header, ... })`，旁邊註解寫明「僅首頁使用本檔的 HomeHeader；申請頁沿用 Shared.jsx 的 Header」）— 它在首頁不被渲染，所以它的 logo 一直停在舊版也沒人發現。本次一併對齊，元件本身未動。
- [2026-09-03] 快捷選單（`ExperienceNav`）補齊全站 15 頁 — 依使用者指示。原本只有 8 頁有，申請流程（`Apply1/2/3`，其中 `Apply2` 內含兩個子頁）、林場露營（`ForestCamp1/2`）、公佈欄、首頁都沒有。統一置於各頁 `<Footer />` 之前。
- [2026-09-03] `ExperienceNav` 的樣式由 `content.css` 搬到 `shared.css` — 它現在是每頁都有的元件，而 `content.css` 只有內容頁載入；不搬的話申請流程、公佈欄、林場露營、首頁拿到的是沒有樣式的裸元素（`position: static`、寬度撐滿、鈕變成一行字）。這類「元件在 Shared.jsx、樣式卻在頁面層 CSS」的錯配，程式碼看起來完全正確，只有實跑才看得出來。
- [2026-09-03] `index.html` 由單檔版改吃共用資源（既有卡「首頁單檔版改吃共用元件」的主體） — 原本它內嵌一份 `Header`／`Breadcrumb`／`Stepper`／`Footer` 複本（223 行）與一份 `design_system` ＋ `shared.css` 的整份 CSS 複本（712 行），改為 `<script src="components/Shared.jsx">` ＋ 兩個 `<link>`，內嵌 CSS 只留首頁專屬的 95 行（hero／跑馬燈／置中頁首／功能鈕／focus）。首頁實際只用到內嵌複本裡的 `Footer`，另三個元件是死碼。
- [2026-09-03] 上述改動順帶修掉兩處已經走鐘的複本：內嵌 `Footer` 的五個頁尾連結還是 `#` 假連結（違反 2026-09-02 決策，改吃 `Shared.jsx` 後自動變成「常見問答→news.html?tab=faq」＋四個「待建置」標記）；內嵌 `.th-lang-*` 是舊版，留著會蓋掉 `shared.css` 的新版，故不保留。1440 與 390 目視比對，除快捷選單與頁尾標記外版面無變化。
- [2026-09-03] `forest-camp-2.html` 直接開啟會 crash（`start` 參數缺漏時 `new Date("")` → Invalid time value）是既有行為，本次未處理 — 它是林場露營流程第 2 步，正常入口是 `forest-camp-1.html` 送出後帶參數導頁。記在此處備查。
- [2026-09-03] 「公佈欄」全站改為正式站用字「公布欄」（`Shared.jsx` 主導覽、`News.jsx` 麵包屑／aria-label／公告內文、`news.html` title、`OpenStatus.jsx`／`WebIllustrate.jsx`／`shared.css` 註解、`template/1.html`），共 13 處 — 依使用者裁決照 `site_map.aspx`；正式站 `news_0.aspx` 的麵包屑實測亦為「公布欄」。**唯一不改的是 `NoticeDetailData.jsx` 第 423 行的「\ 公佈欄 \ 檔案下載」**：該檔是正式站 17 張須知頁的逐字轉檔產物、驗收方式是與正式站逐字元比對 0 差異，正式站內文自己寫「公佈欄」與其導覽不一致，是正式站的問題，改了會讓比對失效。
- [2026-09-03] 「登山線上申請」**維持不改**，不採用 `site_map.aspx` 的「各項線上申請」 — 實測正式站 `apply_1.aspx`：頁面 h1 是「登山線上申請」、麵包屑是「首頁 > 登山申請 > 登山線上申請」，只有主選單與網站導覽把該項稱為「各項線上申請」。也就是「各項線上申請」是**選單項名稱**、「登山線上申請」是**頁面名稱**，正式站兩者本來就不同名，我方現況與正式站一致。若日後主導覽做成兩層下拉，子選單才用「各項線上申請」。
- [2026-09-03] 申請前摘要頁改為內容全寬，不保留 680 窄欄 — 頁首修好後窄欄變成靠左、右側空出約 536px，讀起來歪；置中又會讓內容左緣（約 380）與 h1（112）對不上。全寬後麵包屑／h1／Stepper／注意事項卡／按鈕全部同寬 1216、左緣一致，與 `apply-1`／`apply-3` 同一套。三行注意事項實際文字約 500px 寬，全寬不會產生過長行。`.p2-summary-narrow` 一併移除。
- [2026-09-03] 山屋流程兩頁共用的 `CABIN_DATA`、`FC_STEPS`／`FcStepper` 與日期工具（`formatDateInputValue`／`addDaysToDateValue`）抽成 `components/ForestCampShared.jsx` — 原本 `forest-camp-2.html` 為了取 `CABIN_DATA` 而載入整支 `ForestCamp1.jsx`，該檔檔尾自帶 `ReactDOM.createRoot(...).render(<ForestCamp1App/>)`，`#root` 因此被連續 render 兩次（先第一步、再被第二步覆蓋），console 固定出 createRoot 警告。不放進全站的 `Shared.jsx`：`fc-stepper` 的樣式只在 `forest-camp.css`，兩頁專用的東西不佔全站共用檔。
- [2026-09-03] `ForestCamp2App` 的 `start` 由空字串預設改為今天（`formatDateInputValue(new Date())`），與第一步一致 — 直接開不帶參數的 `forest-camp-2.html` 時 `addDays("")` 產生 Invalid Date、後續 `toISOString()` 拋 RangeError，整頁白畫面。`route` 本來就有 fallback，`start` 沒有。雛形的每一頁都要能單獨用網址打開檢視，不能只在走完前一步時才活著。
- [2026-09-03] **全站樣式收斂為單一 `styles/shared.css`** — 原本八支分頁 CSS（`apply1`／`apply2`／`apply3`／`forest-camp`／`campsite`／`notice`／`open`／`content`）全數併入，30 支 HTML 的 `<head>` 只剩一個 `<link>`。分頁 CSS 之間**沒有 class 名稱衝突**（各用 `p1-`／`p2-`／`p3-`／`fc-`／`camp-`／`open-` 前綴），但同一種卡片／輸入框／篩選膠囊／表格被換名字重畫了六次——重複的是視覺不是名稱，所以只合併檔案不會產生一致風格，必須連元件一起收斂。
- [2026-09-03] **`.th-page` 原本有兩份互相矛盾的定義**：`shared.css` 是純區塊（靠 `.th-page-inner` 置中）、`content.css` 是 `display:grid` ＋ `max-width:1280` ＋ `padding:32px`，全站靠「哪些頁有沒有載 `content.css`」才沒撞在一起。合併前若不先解掉，後定義會全域覆蓋、申請頁整批爆版。解法：`.th-page` 只留純區塊語意，兩欄 grid 移到新的 `.th-page-body.has-nav`。`.th-page-plain`（原本只為了還原 `content.css` 的 grid 而存在）連同 `plain` 參數一併移除。
- [2026-09-03] **頁面外殼收斂為 `Shared.jsx` 的 `PageShell` 一支**，`PageHead` 移除 — 在此之前查詢／申請頁走 `Breadcrumb ＋ main.th-page > .th-page-inner`、內容頁走 `PageHead ＋ div.th-page.has-nav > .th-page-main`，兩套骨架並存。15 個頁面外殼（含 `Apply2` 的兩個）全部改用 `PageShell`，參數為 `trail／title／lead／updated／stepper／nav／bare`。
- [2026-09-03] 表單控制項統一為 `.th-field`／`.th-label`／`.th-input`／`.th-select`／`.th-textarea`／`.th-field-hint` — 原有三套（`.p3-input` 圓角 8px、`.fc-input` 固定高 42 邊框 1.5px、`.bulletin-input` 內距 9/14），高度、邊框粗細、focus 環都不同。統一取 `min-height: 42px`、1px 邊框、`--r-md` 圓角、3px focus 環；`.th-btn` 也加 `min-height: 42px`，篩選列上按鈕與輸入框才齊平。另外 `.p1-chip`／`.bulletin-chip` → `.th-chip`，`.p1-empty`／`.bulletin-empty` → `.th-empty`，`.bulletin-search`／`.p1-search-input` → `.th-search`，`.bulletin-btn` → `.th-btn`。
- [2026-09-03] 表格統一為 `.th-table`，長清單加 `.th-table--zebra` — 原本公布欄／開放狀態／宿營地用 `.bulletin-table`（深綠底白字表頭＋斑馬紋），內容頁用 `.th-table`（淺色表頭）。依「以內容頁版型為準」的裁決，表頭一律採淺色版本，斑馬紋保留為變體。窄螢幕的堆疊卡片規則**只套 `--zebra`**：內容頁的參照表沒有 `data-label`，堆疊後會變成沒有欄名的字串串。
- [2026-09-03] `index.html` 從 315 行降到 33 行 — 內嵌的 `<script type="text/babel">`（178 行）抽成 `components/Index.jsx`（同名舊檔已成死碼、無任何頁面引用且內容過期，一併取代），內嵌 `<style>`（95 行）併入 `shared.css` 的「首頁」段，`<head>` 改為與其餘 30 頁同一套。
- [2026-09-03] **首頁刻意保留自有的置中頁首 `HomeHeader`，不套 `Shared.jsx` 的 `Header`** — 那是 landing page 版型（照片 hero ＋ 置中站名 ＋ 跑馬燈 ＋ 左右雙卡片），與內頁的 sticky 導覽列是不同用途，不是版面不一致。其餘共用元件（`Footer`、`ExperienceNav`）仍走共用。
- [2026-09-03] `template/` 新增 `page.html` ＋ `PageName.jsx` 作為現行骨架範本（單一 `shared.css`、`PageShell` 用法、共用 class 清單）。舊的 `template/1.html`（678 行、自帶一份 Tailwind config 與內嵌樣式）**未刪除**，但它已與現行寫法脫節，從它起手的新頁面會再長出分頁 CSS，待裁決是否移除。
- [2026-09-03] 舊範本 `template/1.html` 移入 `.scratch/frontend-舊雛形/template-1.html`，不直接刪除 — 比照 2026-08-24「舊雛形、簡報產生器、舊規範移入 `.scratch/`」的前例；`.scratch/` 不進版控，git 歷史仍留得住原檔。`template/` 底下只剩 `page.html` ＋ `PageName.jsx`，新頁沒有第二個起手處可選，也就不會再長出分頁 CSS。同時把前一 session 的比對暫存 `f_inline.tmp`／`f_shared.tmp` 移出 `05_Prototype/`（兩者本就在 `.gitignore` 內）。
- [2026-09-07] **`applySearch.aspx` 與 `apply_3.aspx` 併為單頁 `applySearch.html`** — 原任務卡寫「applySearch 只是轉導頁」，實抓正式站後確認並非如此：它是「請選擇操作方式」的四選一入口，四顆按鈕分別連 `apply_3`（進度查詢）／`apply_2`（異動及取消）／`apply_4`（線上繳費）／`apply_5`（線上退費），`apply_3` 才是查詢表單頁。取捨在「新頁檔名比照舊站 aspx」與本卡「不另做一張」之間，經使用者裁決採單頁：入口四張卡照舊站呈現，「申請進度查詢」就地展開表單與結果區，另三項出待建置標記等各自的卡接上。頁內標題採導覽列名稱「申請進度查詢／繳費／異動／取消」。
- [2026-09-07] **`apply_5.aspx`（國家公園線上退費）目前沒有對應任務卡** — 前台缺口盤點把繳費與退費併在「繳費與退費查詢頁」（`apply_4`）一張卡，但正式站是兩支獨立頁面、兩個入口按鈕。退費頁的規格歸屬待決定：併入 `apply_4` 那張卡，或另開一張。
- [2026-09-08] `--home-panel-edu`／`--home-panel-apply` 列為將來收斂 tokens 的候選 — 這兩個是首頁雙卡片面板標頭的單點專用色（原本寫死在 Index.jsx 的 inline style），為了同時滿足「§7 禁 inline style」與「§6.1.1 禁 pages.css 出現 hex」才收成 token；它們不屬於既有色階，全站只有首頁兩處使用
- [2026-09-08] 複製 CSS 前先確認規則存在於 HEAD — 階段 3.0b 曾把使用者當時未提交的 `.th-expcard` 修改複製進 `components.css` 並提交，造成「定版複製 vs 未提交原檔」的分裂；已撤除，規則寫入 v4 §6.4.2
- [2026-09-08] **`popstate` 列為將來改善候選，遷移期不加** — React 版 `news` 只在初始化讀一次 `?tab=`（接受 key 或索引），並在 `changeTab` 呼叫 `pushState`，**沒有 popstate 監聽**，所以按瀏覽器上一頁時網址會退、頁籤不會跟著退。Vue 版照搬同一行為。補 `popstate` 是修一個既有缺陷，屬「改善不是遷移」，要等階段 3 全部遷完再單獨處理（同類還有 `open` 的分頁狀態未進網址）。
- [2026-09-08] **head-loader 平行化後 page-init 必須等 `DOMContentLoaded`** — body 底部的資料檔是 parser-inserted，head 那 22 支平行化後是「動態插入 ＋ async=false」佇列，規範沒有規定兩個佇列的相對順序。舊的序列版有 24 段串聯、約 1.5 秒安全邊際所以看不出來；平行版壓到約 200ms 就翻車（資料檔延遲 800ms 實測：`open`／`apply-1` `ReferenceError`、`notice_a1` 掛載失敗白畫面）。會炸的原因正好是計畫的通則「大宗資料常數不進 `data()`」——page-init 在頂層就讀 `window.XXX`。解法是 page-init 等 `DOMContentLoaded` 才 append，並下 `rel=preload` 讓下載仍並行。**不把資料檔搬進 loader**，那會改掉掛載契約。
- [2026-09-08] **四層 CSS 的 `@import` 鏈維持不動，不改成 head.html 的四個 `<link>`** — 兩個理由各自成立：(1) 實測掛載完成中位數 1694ms vs 1454ms，四個 link 反而慢 240ms，因為掛載是被 `includesLoaded`（JS）擋住而不是 CSS，CSS 提早 548ms 就緒買不到任何東西，卻多了四支 render-blocking 樣式表跟 CDN 搶頻寬；(2) 放進 `head.html` 就變成動態插入，會落在 Tailwind runtime `<style>` 之後，翻掉已實測（99px vs 12px）的 cascade 順序。`@import` 鏈本身只有 2 層串聯，不是瓶頸。
- [2026-09-09] **共用元件自帶的正確行為不算擅自改善** — forest-camp-1 改用階段 2 定版的 `th-date-picker`，該元件會給 input 一個 `id`、label 一個對應的 `for`，原 `ForestCamp1.jsx` 自己包的那層兩者都沒有（點 label 不會聚焦欄位）。這是本頁唯一的 computed 真實差異。裁決：保留。使用階段 2 定版的共用元件本身就是遷移目標，元件自帶的正確行為不視為在遷移中夾帶改善。同類情形（元件比原頁面多做了對的事）比照辦理。
- [2026-09-09] **`.th-notice-grid` 的改名封鎖已解除，排階段 4** — 原本不能改名是因為 `applySearch.html` 尚未遷移；兩頁（notice、applySearch）現在都已遷移，依 §6.2 可改為 `.p-notice-grid`。遷移期不動，`pages.css` 與 `scan_66.py` 的白名單維持到階段 4 一併撤除。
- [2026-09-09] **forest-camp-2 的畫面文案與驗證邏輯不一致，屬需求問題不修** — 畫面寫「各設施每晚至少填入 1 個單位」，程式判的是「該夜**所有設施數量相加** ≥ 1」（單一設施填 1 即可通過）。原 `ForestCamp2.jsx` 就是這樣，遷移照搬未改。要改的是文案還是規則由業務端確認，不在遷移範圍。
- [2026-09-09] **靜態 inline style 一律收成 class，動態的保留** — 判準：值是固定的就收（`ForestCamp2.jsx` 五處 → `.p-fc2-*`，比照 `Index.jsx` → `.p-home-*` 的前例）；值是依資料算出來的就保留 `:style`（`ForestCamp1.jsx` 的可用量長條寬度與狀態色，收成 class 會改變行為）。先前 forest-camp-1／2 全部保留 inline 是我的取捨錯誤，已改正。
- [2026-09-09] **原碼既有的 falsy 缺陷遷移期一律照搬，不順手修** — 界線是「與改版前相同」。目前累積三筆，與 FC2 的文案不一致同一批送業務端：(1) `apply-3` 的 `routeData?.diff || 3`（難度分級有第 0 級，`diff === 0` 會顯示「第 3 級」，`open.html` 的彈窗已因此改用 `!== null`）；(2) `apply-3` 的 `routeData?.days || 2`（`days` 為 0 落到預設 2）；(3) `forest-camp-2` 的畫面文案寫「各設施每晚至少填入 1 個單位」但程式判的是「該夜所有設施相加 ≥ 1」。
- [2026-09-09] **`mounted()` 裡不能量版面（第八類靜默失敗）** — `[v-cloak]{display:none}` 要等 Vue 掛載後移除屬性才解除，而 `mounted()` 執行時屬性還在，整個 `#th-app` 仍是 `display:none`，`getBoundingClientRect()` 全回 0。實測 `apply-2` 的 scroll-spy：19 條條文的 top 全讀成 0、全部滿足 `top <= 160`，「目前條款」停在最後一條（React 是第 0 條）；像素比對抓不到（`.is-active` 的差異落在初始捲動範圍外）。修法：首次量測排進 `requestAnimationFrame`（React 的 `useEffect` 在 paint 後才跑）。**源頭是 v-cloak 這個改動本身**，不是頁面寫錯。已寫進 `assets/app-boot.js` 的掛載契約。
- [2026-09-09] **campsite 的 11 組檢視不是對等結構，共用件一律走頁面區域元件** — 實測：5 個是包住 `NodeCalendarView` 的薄殼（共 104 行、零自有狀態）、3 個有狀態月曆、1 個表格、2 個純資料表。11 支共用件的消費端**只有 campsite 一頁**，依 §4.2「擁有者是元件、退場條件是最後一個消費端」，單一消費端不升為全站共用，改走 `createApp options.components`。例外是 `DayModal`／`ForestryDayModal`：與既有的 `th-modal` DOM 完全同構（只差 `body-class`，而該 prop 存在），比照 `open.html` 直接改用共用元件。
- [2026-09-09] **整頁專用樣式表搬進 pages.css 前必須先作用域化** — `hiking-site/style.css` 是為單一頁面寫的完整樣式表，含 `:root`／`body`／`*`／`button`／`a`／`main` 等全域選擇器；在原資料夾只影響那一頁，直接搬進 `pages.css` 會打到全部 32 頁（實測 `notice_b3` 對 React 版高度 3523→3635、75 筆 computed 差異，`.bulletin-badge` 的 `!important` 還會鎖死全站機關徽章的顏色）。作法：`<body class="p-indexb-body">`，CSS 每條規則前綴 `:where(.p-indexb-body)`。**用 `:where()` 而非一般後代選擇器**，因為它不貢獻特異性，每條規則的層疊順位與在原檔中一致，不會反過來蓋掉頁面自己的 `.p-indexb-*`。`audit_copy` 抓到其中一條（`.bulletin-badge`），其餘是另外掃「選擇器未以頁面前綴起頭」找出來的。
- [2026-09-09] **index-b.html：來源 209 行 app.js 有 99 行由既有共用元件取代，不移植** — `th-header` 已內建語言下拉、行動選單與 Escape 關閉（來源 132–208 行）；`th-quick-nav` 就是來源那段快捷選單的出處（來源檔內註解自己寫明是手抄自 `Shared.jsx` 的 `ExperienceNav`，來源 100–122 行）。另外 `openStage()` 在來源即死碼（點階段實際呼叫 `openQuickMenu`），`sitemap`／`search` 兩個彈窗的觸發鈕在來源自有頁首上，改用 `th-header` 後沒有觸發點，一併不移植。
- [2026-09-09] **`th-quick-nav` 的開關用 template ref 觸發，不替共用元件加 prop** — index-b 點階段要開快捷選單，但該元件的 `open` 是內部狀態、沒有對外 API。用 `this.$refs.quicknav.open = true` 比替共用元件加一個只有一頁要用的 prop 乾淨，也不動共用元件。
- [2026-09-10] 訂立三種任務模式（遷移／清理、改畫面、開新頁） — 遷移已完成，遷移期的「看到可以改善也不要改」「不為統一而動 DOM」兩條界線標明只適用模式 1，改畫面模式改以使用者指定的「不能動什麼」當安全網
- [2026-09-10] **背景分頁會讓驗收產生偽陽性** — 自動化開的分頁 `document.hidden === true`：rAF 完全不觸發（實測 3 秒未執行），排進 rAF 的首次量測不跑、把 resolve 放進 rAF 的等待腳本永不完成（實測掛到 CDP 45 秒逾時，回報成「renderer 沒有回應」）；`window.scrollTo` 搬得動捲軸但不發 scroll 事件（實測 scrollY=3500、listener 收到 0 個事件），apply-2 的 scroll-spy 因此停在條文 01——而條文 01 剛好是正確初始值，看起來完全正常。改用 `computer` 的 scroll 動作即正常（scroll-spy 跳到條文 05）。對策：截圖前等 `window.thApp && !document.querySelector("#th-app[v-cloak]")`，捲動用真實輸入動作。已寫入踩坑總表第三之三節與 CLAUDE.md 模式 2 驗收 (a)。
- [2026-09-10] **th-date-picker 改用 flatpickr（模式 2，理由是外觀）＋ AA 補債清單** — 原生 `input[type=date]` 的日曆圖示、年/月/日 排版、展開的月曆彈窗都不可控，跨瀏覽器差異明顯（Safari 連日曆圖示都沒有），功能上原生夠用（單選 ＋ min/max），純為外觀換。flatpickr 4.6.13 由 CDN 載入、integrity 釘版，零 build step；主題一律用 tokens 覆寫。**代價是無障礙從「免費」變成「要自己補」**：原生 input 的鍵盤與螢幕閱讀器支援來自作業系統層，換掉之後全部要自己實作。這正是本元件檔頭 2026-09-07 那段預言的情況（「自建彈窗日曆正是最容易欠下 AA 債的元件」），該段原文保留在檔頭供回溯，**不要刪**。**本輪不補，記成清單**：（1）**方向鍵移動無效** — 連按 ArrowRight／ArrowDown，高亮日始終停在今天、值不變；（2）**Escape 無法關閉彈窗** — 按下後 `.flatpickr-calendar.open` 仍在；（3）**Enter 選取後焦點掉到 `<body>`**，沒有還給 input；（4）**Tab 直接跳出到下一個控制項**，焦點不會進入彈窗內部；（5）**input 完全沒有 `aria-*`**（無 `role`、無 `aria-expanded`、無 `aria-haspopup`）；（6）**彈窗完全沒有 `aria-*`**（無 `role="dialog"`／`"application"`）；（7）**日格只有 `aria-label`**（如「八月 30, 2026」），沒有 `role="gridcell"`、沒有 `tabindex`、沒有 `aria-selected`。**成因判斷**：第 1、2 項很可能是 `allowInput: true` 造成 — 鍵盤事件被文字輸入框吃掉，而「可直接鍵入」與「鍵盤導航」在 flatpickr 這個設定下互相衝突。**這是取捨不是 bug**，要補的話得先決定哪一邊優先。**債的規模**：這筆債從 1 處擴大到 **4 處正式頁面消費端**（forest-camp-1 ×1、apply-3 ×1、news ×2；另有 `_stage2-test.html` ×3 屬驗收頁）。補的時候是**改一支元件、影響 4 頁** — 這正是當初包 wrapper 的價值，換底層或補 aria 都只動 `assets/components/th-date-picker.js` 一個檔。 **（8）[2026-09-14 追加] `th-todo-link` 的「待建置」字樣是 CSS `::after` 產生的**（`components.css` `.th-todo-link::after { content: "待建置" }`），螢幕閱讀器是否念出因軟體而異，使用者只聽得到項目名稱、不知道它不可點。消費端遍及全站（頁首、footer、sitemap、快捷選單以外的各待建置項），補的時候同樣是改一支元件；sitemap 頁是無障礙檢測重點，檢測前應先處理。
- [2026-09-11] **`th-date-picker` 的 `bare` prop 讓元件逼頁面配合，可接受** — flatpickr 的日曆圖示需要一個定位基準，而 `<select>`／`<input>` 這類 replaced element 吃不下偽元素，所以元件內部多包一層 `.th-fp-wrap`。代價是 flex item 從 input 變成 wrapper，原本掛在 input 上的尺寸（apply-3 的 `flex: 1`、news 的 `width: 165px`）要轉移到 wrapper、input 改吃 `width: 100%`。**裁決：不重想 bare 的設計。** 理由：包一層 wrapper 是採用第三方彈窗元件的必然代價；影響面只有兩處頁面 CSS；尺寸轉移後實測寬度 383／165／165 與改動前一致。
- [2026-09-11] **`th-date-picker` 的 input 屬性抽成 `inputAttrs` computed，可接受** — 有 `.th-field` 與 `bare` 兩個 template 分支，若各寫一份 input 屬性，兩份遲早漂移，而**那種漂移 prod build 不會出聲**（樣板編譯失敗與屬性不一致都不報錯）。代價是讀樣板時看不到 input 有哪些屬性，要跳到 computed 看。**裁決：防漂移的價值大於可讀性的損失。**
- [2026-09-11] **`_island-test.html` 的 10 處字體／顏色變化是「預期中的修正」，不是回歸** — 階段 4 步驟 5 把元素層 reset 移進 `base.css` 之後，`cascade_check` 報該頁 3 個元素的 `color`／`font-family` 由「無規則」變成 `var(--fg-1)`／`var(--font-sans)`／`var(--fg-2)`（@1440 與 @900 各 5 處）。**因果**：`design_system/colors_and_type.css` 是被 `styles/shared.css` 第 1 行 `@import` 帶進來的，而 `_island-test.html` **只 link `index.css`、沒有 link `shared.css`**，所以它從來沒載到那 12 條元素層規則，`<h2>`／`<p>` 一直是瀏覽器預設樣式 —— 那是階段 1 建立驗收頁時的疏漏。搬進 `base.css`（由 `index.css` `@import`）之後，任何 link `index.css` 的頁面都會拿到，這正是四層架構應有的行為。**裁決：接受，不補 link `shared.css` 去維持舊外觀。** 該頁的用途是「證明共用元件能在不載 `shared.css` 的情況下獨立運作」，而階段 4 結束後 `shared.css` 根本不存在 —— 它現在拿到 `base.css` 的 reset 正是它未來的正常狀態；補 link 會取消這頁存在的意義，而且下一步就要刪 `shared.css`，補了也白補。其餘 35 頁「值也變了」為 0，DOM 形狀簽章一致。
- [2026-09-11] **現行 DB 的 272 表規整成 `03_Schema/`，參照嘉市災管做法但不轉換識別字** — 來源是 `01_Raw_Input/現行tableSchema.csv`（3535 欄）。結構照嘉市災管：一表一份 md ＋ `_fields/` 一欄位一筆記 ＋ `00_目錄.base` 提供視圖，表文件的欄位段用 `![[00_目錄.base#單表結構]]` 嵌入而非另存表格（存兩份必然不同步）。**三點刻意與嘉市災管不同**：(1) **不做 PascalCase 轉換**——嘉市那邊在設計新表名所以轉，這裡的 `AI_ID`／`CRTDATE`／`___filesSizeCount` 是現行 DB 的真實識別字，轉了會產生 DB 裡不存在的名字，因此逐字保留，也不產 `TableSnake`／`FieldSnake`；(2) **產生器是合併式不是重建式**——嘉市的 `gen-schema-field-notes.js` 因為「先刪光再產生」而必須退休，這裡 `scripts/gen-schema-from-csv.js` 一開始就設計成：CSV 覆寫 DB 事實欄位（型別／長度／預設值／允許空值／鍵值／Seq）、人工欄位（中文名稱／備註／外鍵指向）既有值非空就保留、表文件正文整段保留、任何情況都不刪檔，孤兒筆記只列報告；(3) **表狀態分類標為推測**——`狀態`（現役 228／系統框架 16／疑似備份 16／檢視 8／疑似暫存 4）純由表名特徵推得，frontmatter 一律帶 `狀態來源: "命名推測"` 與 `狀態依據`，**不得據以刪表**，實際使用查證未做。**1566 個空的欄位中文名一律留空不猜**（§3.4），外鍵資訊匯出檔沒有所以全空，空白本身就是待辦清單（`00_目錄.base` 的「待補中文名」視圖）。`.gitignore` 的 `*.base` 加了 `!03_Schema/00_目錄.base` 例外（原本只放行舊名 `schema.base`）。
- [2026-09-11] **搬移時若規則的「承載元素」已經改變，把它掛到正確的元素上是維持行為，不是改善** — 階段 4 步驟 8 把 `.bulletin-toolbar .th-input--date { width: 145px }` 搬進 `pages.css` 並改名為 `.p-news-toolbar` 之後，它從「排在 components.css 之前」變成「之後」，開始壓過 `.th-fp-wrap .th-input--date { width: 100% }`，於是 input 145px 而 wrapper 165px，多出 20px 空白。**判準：問「哪一種做法讓畫面不變」。** 那條規則的意圖是「news 工具列的日期欄位 145px 寬」；flatpickr 化之後承載寬度的元素由 input 變成 `.th-fp-wrap`，145px 留在 input 上已經是**描述一個不再負責寬度的元素**。改掛到 wrapper（`.p-news-toolbar .th-fp-wrap { width: 145px }`，input 改 `width: 100%`）讓畫面與改版前相同 —— 那是維持行為；保持原樣才是改變行為。**這不違反「看到可以改善也不要改」**：那條界線是「與改版前行為相同」，此處正是為了達成它。實測 wrapper／input 皆 145px，cascade_check「值也變了」由 4 降為 0。
- [2026-09-11] **`___filesSizeCount` 的分類更正：系統框架 → 疑似暫存** — 原本的規則把 `^___` 和 `aspnet_`／`sysdiagrams` 綁在同一條「ASP.NET／SQL Server 內建表」，但三底線前綴不是任何框架的產物，是人手打的命名慣例（排在清單最上面／標記臨時）。該表 4 欄（`id`／`filesize`／`f_id`／`iscompose`「1是合併0未合併」），`f_id` 在 `applylist_files`／`applylist_lchgfiles`／`node_files`／`FileManagementDetail` 都是主鍵名，看起來是某張檔案表的大小統計，但**指向哪一張無法從匯出檔判定**，標 `[待確認]`。
- [2026-09-11] **疑似備份的 16 張表移到 `03_Schema/_backup/`，目錄視圖不顯示** — 使用者要求。表文件換資料夾，但**欄位筆記仍平鋪在 `_fields/`**：Bases 靠屬性過濾而不是資料夾，所以改為每份欄位筆記帶 `表狀態`，欄位層的三個視圖（所有表結構／待補中文名／外鍵關聯）用 `表狀態 != "疑似備份"` 濾掉；表層的目錄與疑似停用視圖用 `狀態 != "疑似備份"`，另開「備份表」視圖單獨看。產生器同步認得兩個位置，分類規則之後再變動時會把正文搬到新位置並回報舊檔（不自動刪）。
- [2026-09-11] **表中文名接 `01_Raw_Input/現行表描述.csv`，只填有值的 142 張** — 欄位那份匯出檔沒有表層級名稱。描述檔 248 筆、142 筆有值、106 筆是字串 `"NULL"`（不是空字串，照字面收會產生一堆叫 NULL 的中文名）。**它少收 24 張表，而那 24 張幾乎全是檢視**（`vw_aspnet_*` 六張、`*_view` 七張、`vw_NCDR_Warning`，加四張中文名的查詢用表）——等於獨立佐證了狀態分類裡的「檢視」那一類；frontmatter 用 `描述檔收錄` 記這件事。收到的是「描述」不是嚴格名稱（`ApplyBanTime` →「禁止申請時間(抽籤時間)」），照收不改寫。表文件 H1 改成 `# 表名【中文名】`，但**只在標題還是產生器原樣時才更新**，人工改過就不動。
- [2026-09-11] **表狀態改為「描述檔優先於命名推測」，因為 `_bak` 規則誤判了 6 張現役表** — 純命名推測把 `applylist_bak`（入園申請資料表草稿）、`apply_teams_bak`（入園申請隊員草稿）、`apply_schedule_bak`（申請入園行程資料表草稿）、`tb_scheduleOther_bak`（其他路線草稿資料表）、`booking_bak`（宿營地居住資料表）、`Spnperrormember_hislog`（歷史關注紀錄）全判成備份，搬進 `_backup/` 還從目錄藏起來——**而前四張正是草稿編輯功能在用的表**。新判定順序：描述含「草稿」→ `草稿表`；描述非空但名字像備份／暫存 → `現役`（依據欄把那句描述抄進去）；都沒有才走命名推測。結果：疑似備份 16→10，新增草稿表 5（含 `Case_MainTemp`），現役 228→230。**獨立佐證**：`applylist_bak` 85 欄是 `applylist` 223 欄的子集（只多 `CaseId`），缺的 139 欄全是審核意見／抽籤／繳費／出入園實際紀錄——備份會是完整複本，子集只可能是草稿。**教訓**：命名特徵只在沒有其他資訊時才可用，機關自己寫過描述的地方，推測規則不得覆蓋它。
- [2026-09-11] **出園回報（apply_6）的「申請編號」是哪一欄未定，四個候選全部給** — 頁面標籤寫「(一站式/入園)申請編號」，暗示兩種編號都收，而 `applylist` 裡有四個候選欄：`serial`（隊伍識別碼）、`CaseSerial` 與 `TCaseSerial`（**兩欄中文名都叫「一站式案件編號」**）、`org_caseno`（原始申請編號）。匯出檔看不出哪一欄對應頁面輸入，也沒有唯一約束。處置：測試 SQL 四欄全部輸出並加一段填答率／長相對照與「CaseSerial 是否等於 TCaseSerial」的探測，標 `[待確認]`，不挑一欄當答案。2026-09-11 以 WebFetch 實抓兩支頁面，確認 `02_Spec/07_草稿編輯.md`、`02_Spec/08_國家公園出園回報.md` 的欄位描述與現況一致。
- [2026-09-11] **`applylist.actout` 的「已出園」是 2 不是 1；`actin = 1` 不是出園回報的前提** — 實跑值域：`actout` 全表只有 NULL 與 2（NULL 1,484,176／2 22,292），**一筆 1 都沒有**；`actin` 是 NULL／0／1。原本照直覺寫的 `actout = 1` 條件會永遠撈到 0 筆，看起來像「沒有已出園的資料」而不是條件寫錯——**這種錯誤不會報錯，只會安靜地回空集合**。另一個反直覺點：有 14,433 筆 `actout = 2` 但 `actin` 是 NULL，代表出園回報不要求先有入園註記，所以「可測出園回報」的條件只能是 `actout IS NULL`，不能加 `actin = 1`。已寫進 `03_Schema/_fields/applylist__actout.md` 與 `applylist__actin.md` 的備註。
- [2026-09-11] **國籍欄位存的是中文字串「中華民國」／「國外」，不是 0/1** — `applylist.apply_nation`／`leader_nation`、`applylist_bak.apply_nation`、`apply_teams.member_nation` 四欄皆然。**`apply_teams.member_nation` 的欄位中文名寫「國家別 本國0 外國1」，與實際存的值不符**——照欄位中文名寫條件會全部落空。另有少量髒資料：空字串、NULL，以及 4 筆 `請選擇`（沒選就送出）。已寫進四份欄位筆記的備註。
- [2026-09-11] **實測到的值域寫回 `03_Schema/_fields/*.md` 的「備註」欄，不另開文件** — 備註是產生器認定的人工欄位，重跑不會被 CSV 洗掉（見 `03_Schema/README.md`），而且在 Bases 的「所有表結構」視圖直接看得到。這是規整 schema 的第一個實際回收：查一次值域，之後所有人都不必再查。
- [2026-09-11] **出園回報的「申請編號」確定是 `applylist.serial`** — 四個候選欄填答率實測：`serial` 1,506,465／1,506,468（99.9998%）、`org_caseno` 32,867（2.2%）、`CaseSerial` 129、`TCaseSerial` **1 筆**。頁面標籤寫「(一站式/入園)申請編號」是給使用者的說法，DB 端只有 `serial` 在用；`CaseSerial`／`TCaseSerial` 是幾乎沒啟用的一站式串接欄位（兩者中文名相同、差異仍未確認）。**判定方法是填答率而不是欄位中文名**——光看中文名會以為「一站式案件編號」才是一站式那個。四欄的實測數字已寫進各自欄位筆記的備註。
- [2026-09-11] **`apply_4`／`apply_5` 是排雲山莊專用的繳費／退費頁，不是通用繳費頁** — 實抓頁面標題：`apply_4.aspx`＝「列印繳費說明單/線上繳費(**排雲**)」（查詢鍵：申請編號 ＋ 隊伍名稱 ＋ 驗證碼）、`apply_5.aspx`＝「線上申請退費(**排雲**)」（查詢鍵：身分證字號 ＋ 手機號碼 ＋ 驗證密碼，正好對上 `applyrefund` 的 `af_sid`／`af_mobile`／`af_chkpassword`）。雛形 `th-quick-nav.js` 把 `apply_4` 標成「繳費／退費（含退費日期）查詢」，**漏掉了「排雲」這個限定**——那是整個功能的適用範圍，不是修飾語。資料串法：`applylist` →`a_id`→ `booking` →`node_id`→ `node`（`name` 含「排雲」），規費主檔是 `campmanagefees`（`b_id` 對 `booking`），`paytype = 4` 是線上繳費、`status = 1` 已繳費；退費明細 `campmanagefeesDetail`。排雲山莊在 `node` 有多筆（交接文件 A16：3 排雲山莊、219 專3、296 專3(不收費)、377 排雲山莊()），所以查詢一律用 `name LIKE N'%排雲%'` 而不寫死 node_id。退費前置條件（頁面原文）：**必須先取消入園**，資格為原案的申請人／領隊／隊員任一員，申請案保留 2 年。
- [2026-09-11] **`campmanagefees` 的 `paytype`／`status` 實際值域比欄位註解多，`refunds` 形同未使用** — 欄位中文名寫 `paytype` 1 郵政匯票／2 金融機構轉帳／3 現金／4 線上繳費，**實測有 1~8**（2→24,395、4→15,221、5→990、1→705、6→580、7→326、3→201、8→2），5・6・7・8 無來源標 `[待確認]`。`status` 註解只寫 1 已繳費／0 未繳費，**實測有 -1（41 筆）與 2（14 筆）**，同樣無來源。`refunds`（退費金額）**全表沒有一筆 > 0**，退費金額實際記在 `campmanagefeesDetail` 與 `refundsData`——照欄位中文名寫退費條件會完全落空。這是第三次遇到「欄位中文名與實際存的值不符」（前兩次是 `apply_teams.member_nation`、`applylist.actout`），值域一律以實跑為準。已寫進三份欄位筆記備註。
- [2026-09-11] **用名稱找山屋節點必須同時限 `node.type = 'H'`** — `node.name LIKE N'%排雲%'` 會抓到 356「排雲登山服務中心」，那是 `type = G`（登山口／服務中心）不是住宿節點，跟規費完全無關。排雲的住宿節點有四筆且都是 `type = H`：3 排雲山莊（`chk=2` 部份關閉）、219 排雲山莊專3、296 排雲山莊專3(不收費，費用 0)、377 排雲山莊()。**一個山屋在 `node` 表有多筆是常態**，所以查詢用名稱 LIKE 而不寫死 node_id，但要配 type 過濾。已寫進 `node.type` 的欄位備註。
- [2026-09-11] **`applylist.status` 的實際值域大於欄位中文名所列的 0~8** — 現行程式 `apply_4.aspx.cs:224-240` 的 switch 用到 **0／1／4／9／10／11／12／17**，程式碼註解：1 初審完成待補件、4 核准入園、9 初審完成待繳費、10 初審完成待補件&繳費、11 複審完成、12 備取、17 初審完成。欄位中文名寫的是「0待處理 1補件 2書件通過 3宿營地不足 4已通過 5退件 6待抽籤 7路線承載量不足後補 8取消」，**兩套編碼對不起來**（同一個 1，一邊是「補件」一邊是「初審完成待補件」；9 以上完全沒列）。今天第四次遇到欄位中文名與實際不符（前三次：`apply_teams.member_nation`、`applylist.actout`、`campmanagefees.paytype`/`status`/`refunds`）。**通則：欄位中文名只能當線索，值域一律以程式碼或實跑為準。** 已寫進欄位備註。
- [2026-09-11] **`apply_4.aspx`（排雲繳費）的查詢只打 `applylist`，與 `campmanagefees` 無關** — 查詢條件是 `(serial = @serial or CaseSerial = @serial) and teams_name = @teams_name and orgid = @org`，`@org` 在程式裡寫死為玉山 `c951cdcd-b75a-46b9-8002-8ef952ec95fd`。規費是進頁後才由 `booking` join `node.localmoney`／`fmoney` 即時算的，`campmanagefees` 只在實際繳費時才寫入。**所以「測試機的 campmanagefees 幾乎沒資料」不影響能不能進查詢畫面**——先前照 `campmanagefees` 撈測試資料的方向是錯的。進去後要看到繳費資訊還要過四關：status 在那 8 個值內、booking 掛在有收費的 node、該 node＋該住宿日在 `Ballot` 有 `chk=1` 公告（或 `node.IsDraw <> 1`）、算出的金額 > 0。
- [2026-09-11] **刪除 `05_Prototype/hiking-site/`（整個目錄，6 個檔）** — 它是 index-b.html 的移植來源，移植已於 2026-09-09 完成。直接理由是**它自己 `link ../styles/shared.css`**（`index.html:25`，執行期實測 46 個請求裡 1 個要 `shared.css`、1 個要它 `@import` 的 `design_system`），不刪就無法完成步驟 11。評估：消費端 **0**（repo 內 4 處提及全是註解）；繼任者 `index-b.html` 已完成，73 個請求 0 個指向該目錄，圖檔是**複製**不是引用（md5 逐一比對）；留著的成本是 17 處 `!important`、自成一套 `--green`／`--navy` 色票、5 個與四層撞名的 class。**否決「只拿掉那行 link」**：`style.css` 是假設 `shared.css` 在底下寫的，拿掉會讓它外觀垮掉，留一個沒人連到、壞掉又沒人會發現的頁面——那正是本輪在清的那種狀態。原始碼可由 git `624985e`（它最後一次變動的 commit）取回，4 處註解已改為寫明該 commit。
- [2026-09-11] **刪除 `05_Prototype/design_system/colors_and_type.css`** — 唯一消費端是 `shared.css` 第 1 行的 `@import`，後者一刪就沒人載。內容已完全被四層接手：12 條元素層規則於步驟 5 進 `base.css`，`:root` 的 94 個變數 `tokens.css` 全數涵蓋且共有項數值一致。留著會變成色票的第二個真源——下一個人改色票可能改到它然後發現沒效果。**「不在 `styles/` 底下」是清單 §8.3 前置條件的疏漏，不是它該留的理由**；該條件已擴為「連帶檢查所有被 `shared.css` `@import` 的檔」。
- [2026-09-11] **第十類靜默失敗「死掉的指示殘留在活的檔案裡」與它的四類分流** — 刪檔／改名後，指著舊東西的說明留在原地且是祈使句。三件驗收全部無效（註解不影響像素、computed、console），孤兒掃描也抓不到（它問的是寄主活不活著，而寄主是活的）。處置分四類：**甲 活的指示**（照做會壞）必改；**乙 指錯地方的出處**（「外觀沿用 shared.css 的 .X」）改成真正的落點、**逐支查證不一律替換**（同批改名有些進 `components.css`、有些進 `pages.css`）；**丙 歷史敘述**保留內容、把「所以**現在**勝出」改成過去式（歷史可以留，不能留成現況）；**丁 不動**——另案目錄、決策紀錄，以及「**例子過期但結論有效**」那種（`head.html:32` 講動態插入在靜態 link 之後，只是舉例用了 `shared.css` 的節點序號，一律替換會把一句對的話改錯）。本輪處理 57 支檔案。完整敘述見知識庫踩坑總表第三之四節。
- [2026-09-11] **步驟 9 的母體以「四層裡找不到的 41 條」為準，而不是乾跑實測的 19 條** — 兩個數字都是實測：乾跑（原樹 vs 移除 link 的副本）量到 19 條規則、1,620 個屬性真的失去勝出者；靜態比對（選擇器＋@media 在四層找不到）得 124 條，經字面消費端測試剩 41 條。取 41 的理由是**乾跑看不到條件渲染的分支**（彈窗、切頁籤、依資料才出現的狀態），19 是下界不是全集。保守方向：多搬 22 條的代價是四層多 22 條，漏搬的代價是某個分支的樣式無聲消失。
- [2026-09-11] **§8.2 的 13 條 `.camp-*` 取 (a)：改 `_stage2-test.html` 的 markup，不搬那批規則** — 那批既不能刪（驗收頁在用）也不該進 `pages.css`（campsite 已改名 `.p-campsite-*`，再放一份 `.camp-*` 就是第二個真源）。驗收頁的用途是證明共用元件能獨立運作，改 class 名不影響那個用途；選 (b) 則會讓 `shared.css` 永遠差最後一步退不掉。
- [2026-09-11] **token 收斂的成本判斷更正一：「換風格要改每一支元件」不成立** — 19 支 `assets/components/th-*.js` 的程式碼裡**零個 hex**，元件只輸出 class 名，顏色全在 CSS。換風格真正要動的是 `components.css`／`pages.css` 裡寫死的外觀值，不是元件檔。這把工作的形狀從「碰 19 支元件」改成「碰 2 支 CSS」。
- [2026-09-11] **token 收斂的成本判斷更正二：兩層結構已部分存在，這是補完不是從零建** — 盤點時 94 個 token 裡已有 6 個是純 `var()` 轉指（`--fg-1: var(--slate-800)`、`--bg-2: var(--slate-50)` 等），正是要的形狀。重算方法也要記：**用「值的形狀」分類，不用「名稱前綴」**——前綴是命名慣例、值的形狀才是結構事實；改用值的形狀後，A 的範圍由猜測的 49 個修正為 52 個純色字面，其中 29 個本身就是原色（不必再包），真正要補語意層的是 23 個。
- [2026-09-11] **token 語意層只給「換風格時會跟著變」的值** — 色彩、字級、圓角加語意層；間距（846 處 px）、z-index、transition 不加。判準是問這個值在換風格時會不會變：`--national-700` 會，`padding: 12px` 不會。多一層間接的代價（讀 CSS 要多跳一次）只對會換的東西值得付。
- [2026-09-11] **A2：新增原色階梯，只建實際用到的格** — 新增 17 個原色（red 5／amber 3／green 3／blue 2／mist 3／white 1），語意 token 20 個改指原色。值一個都沒變，色票沒變，變的只是引用層級。每格註明來源（`#dc2626 = Tailwind red-600`）。**不補滿 50–900**——造沒人引用的架子與零引用 token 是同一個問題。**6 個單一用途品牌色不包**（`--bg-gray`／`--footer`／`--btn-blue`／`--home-panel-edu`／`--home-panel-apply`）：值只服務一個語意，再包一層是零合併、雙倍間接。
- [2026-09-11] **零引用 token 刪 3 個，`--font-serif` 保留（與原裁決不同）** — 刪 `--fg-on-dark-2`／`--grad-mist`／`--grad-card-overlay`（四層 CSS 與 markup 皆 0）。`--font-serif` 雖然四層零引用，但 `pages.css` 的 `.p-indexb-*` 區塊有一個**同值的本地 `--serif` 被用了 7 次**——serif token 有需求、只是定義在錯的地方，刪了會讓將來收斂那個區塊時沒有東西可接。`--national-500`／`--slate-900`／`--fs-h1` 是階梯中間的格，全留。另記：「零引用」判定一度只掃了四層 CSS，漏掉 markup 裡的 `var()`（`--fs-3xs` 在 markup 用 3 次）。
- [2026-09-14] **C（縮小版）：無對應 token 的字面色，只收消費端 ≥2 的** — `#bfdbfe`×5（info 提示框框線 → `--blue-200` ＋ `--info-border`，補完 info 三件組）、`#f0f7f3`×2（兩個元件的選中項底色 → `--mist-75` ＋ `--bg-selected`）、`#fff8f8`×2（日曆已額滿格 → `--red-25` ＋ `--danger-bg-subtle`）。**計數依消費端不依出現次數**：`#1f2d26` 出現 2 次但只有 `.th-home-header` 一個元件，維持字面。另：B 的正則只抓六位數 hex，**漏了 `#fff` 三位數寫法 43 處**，C 重算時才發現，併入本批以 B 的規則處理（background→`--bg-1`、color→`--fg-on-dark`）。
- [2026-09-14] **單點專用色，未收斂（32 種）** — 只有一個消費端，包進 token 等於把雜物放進設計系統的階梯：`#1f2d26`（`.th-home-header`×2）、`#1a3244`／`#233c4a`／`#335c49`／`#9fb6ad`／`#c5d7d2`／`#f8faf8`（components.css 首頁區塊）、`#1e40af`／`#c2410c`／`#fff7ed`／`#ffedd5`／`#5f6f3c`／`#f5f8ec`／`#dde5c4`／`#f0f7f9`／`#cfe3ea`（components.css 各機關徽章色）、`#2f4a72`、`#b6cbd3`／`#dbe7ed`、`#7e22ce`／`#e9d5ff`／`#f3e8ff`／`#faf5ff`／`#fee2e2`／`#fff5f5`（pages.css:217–235 狀態膠囊）、`#92400e`、以及 `.p-indexb-*` 移植帶進來的 `#233e4910`／`#244f4420`／`#2c435110`／`#436b5110`／`#ffffff60`／`#fffffff5`（8 位含 alpha）。**機關徽章那一組（agency-*）若設計師要換色，會是一批一起換**，屆時再評估是否成組收斂。
- [2026-09-14] **E（頁面覆寫元件 24 條）獨立一輪，等設計稿** — 提升成元件變體是設計問題不是引用層級問題，形狀受設計師影響，現在做可能白做。清單：`.th-table.p-rss-table` 12 條（640px 以下表格轉卡片，應成 `.th-table--stacked`）、`.th-input` 3 條（apply-3）、`.th-fp-wrap` 2 條（news 與 apply-3 **已達二次即提升**）、`.th-todo-link` 2 條（open）、`.p-news-toolbar` 下的 `.th-search`／`.th-input--date`／`.bulletin-filter-row` 3 條、`.fc-section-title` 1 條（fc1）、**`:where(.p-indexb-body) .bulletin-badge` 1 條——換風格時會被遺忘的典型：整組徽章改色寫在頁面層，不會跟著元件 token 走**。另記兩件同屬 E 的重複：日曆額滿格 `.camp-cal-cell.is-full` 與 `.p-campsite-cal-cell.is-full` 是同一條規則的兩份；`.p-indexb-*` 自帶一個與 `--font-serif` 同值的本地 `--serif`。
- [2026-09-14] **「規格」一詞固定指 `02_Spec/` 內帶「功能路徑」的 37 份規格檔** — 使用者先前一直說「八份規格」，實際不只八份，當時據此推出的「規格有、雛形沒有」結論都建立在不完整資訊上。往後對話中講「規格」一律指這 37 份（不含 `00_系統功能設計說明.md` 與 `README.md`）；比對規格與雛形時以全部 37 份為母體。**數字更正**：同日 Claude Code 先報為 38 份，是把 `ls 02_Spec` 的 39 個項目扣掉 `00_` 時漏扣 `assets/` 資料夾；以 `grep -l 功能路徑 02_Spec/*.md` 實數為 37。同日另一個 session 為規格檔加上架構圖編號前綴（見 `02_Spec/README.md`），份數不變。
- [2026-09-14] **網站導覽頁（`sitemap.html`）的收錄範圍** — 骨架＝正式站 `site_map.aspx` 的 10 組，順序與名稱照抄（「玉山抽籤日期查詢」「RSS訂閱」、半形「/」）；另加「網站資訊」一組放正式站 footer 的 5 項（導覽頁本該涵蓋 footer 連結，是正式站導覽頁的缺漏）。**不列**：快捷選單（浮動元件不是頁面）、`index-b.html`（首頁 B 案未定案）、`_stage2-test.html`（回歸證據）、流程內與列表內頁面（照正式站做法）。**[待確認]** 歷史消息、線上申請退費（排雲）、山域事故統計儀表板：規格有、正式站導覽頁沒有，待業務端回覆是否補進正式站導覽，未回覆前不列。頁首工具列「網站導覽」由待建置改連本頁。
- [2026-09-14] **功能名稱的斜線用半形「/」** — 與正式站一致，且全形「／」會被螢幕閱讀器念出來。本輪只改「網站導覽頁上的功能名稱」在各頁的可見文字（`申請進度查詢/繳費/異動/取消` 共 7 處），其餘全形斜線（註解、內文、資料檔）未動，是否擴大範圍待使用者決定。
- [2026-09-14] **申請流程以雛形三頁式（選路線 → 申請須知與同意書 → 單頁表單）為準，回頭補規格** — 雛形的三頁式是改版後的設計，不是逆向錯誤；用舊站四步驟（行程登記 → 基本資料 → 確認 → 完成）去改雛形等於把改版成果退回去。佐證：`02_Spec/00_系統功能設計說明.md` 的流程圖也是三步驟。**更正（同日）**：三頁式對應的是尚未撰寫的 `05_各項線上申請.md`（README：`apply_1` 選機關／路線 → `apply_1_2` 同意條文 → `apply_1_3／1_4／1_5` 各機關申請表），不是 `07_草稿編輯.md`；05 寫出來時再註明「本節為改版後設計」，**07 不動**。舊站有而雛形沒有的（草稿、送件驗證碼、確認頁、完成頁、附件上傳）另列功能缺漏清單，另案處理。
- [2026-09-14] **宿營地查詢補「請選擇」與月份切換；當日申請名單明細頁排進統一建頁批次** — 對照正式站 `bed_6.aspx` 實抓：不帶 `node_id` 時下拉停在「請選擇」並顯示承載量總表；帶 `node_id` 時預選該地點並出現年份／月份下拉與月曆。雛形目前預設選第一個地點、總表與月曆並列、無月份切換。明細頁是新頁面，不在本次補。
- [2026-09-14] **`00_系統功能設計說明.md` 的頁面網址是新版目標命名，不修正成舊站網址** — 細部規格記錄舊站現況（`news_0`／`apply_4`／`bed_6`），總說明描述新版目標（`news.aspx`／`pay.aspx`／`sitemap.aspx`），兩者是不同時間點。實測這批新網址**只出現在總說明**，repo、`.scratch/docs-舊規劃`、`04_Handover`、`03_Schema`、知識庫專案頁與 WBS 卡片都沒有（以已知字串做對照組確認搜尋有效）；是否為既定命名待確認，雛形檔名要照它們來前須先定案。例外：`MountainDashboard.aspx` 是正式站**現有**網址（快捷選單實際連結），不是新命名。
- [2026-09-14] **02_Spec 改序號檔名後，內部連結改指新檔名，不動檔名** — 總說明 72 個 wikilink 改成 `[[01_最新消息|最新消息]]`（保留顯示文字），快捷選單 21 個 markdown 連結改成 `09_登山須知.md` 形式。名稱不同但舊站網址唯一對得上的一併改（網站使用說明→30_本站使用說明、玉山單日往返查詢→17、宿營地及山屋查詢→11、氣象資訊→26、線上繳費／退費(排雲山莊)→06b／06c、林業署自然保護區域→21）。對不到檔案的 5 個不猜：玉山未中籤抽籤日期查詢、四份玉山繳費說明。
- [2026-09-14] **待辦：全形「／」全站文案規範（獨立任務，暫不做）** — 導覽功能名稱已改半形（7 處，見同日「功能名稱的斜線用半形」），其餘是各頁自己的文案，不是導覽名稱，不併入。**設計稿到位後文案可能再動一次，屆時一次處理。** 位置（以「剝除註解後仍含全形斜線」的啟發式計數，共 52 處，未逐行確認是否真的可見）：`components/campsite.js` 19、`applySearch.html` 5、`_stage2-test.html` 5、`components/apply-search.js` 3、`components/RouteData.js` 3、`components/notice.js` 2、`components/NoticeDetailData.js` 2、`components/CampsiteYushanData.js` 2、`assets/components/th-quick-nav.js` 2、`apply-3.html` 2、`web_illustrate.html` 1、`open.html` 1、`components/rss.js` 1、`components/news.js` 1、`components/OpenStatusData.js` 1、`assets/components/th-header.js` 1（實為行尾 `//` 註解，啟發式誤計）、`assets/app-boot.js` 1。另有註解內 370 處不在範圍。
- [2026-09-14] **[待確認] 「雪霸路線登山口查詢」vs「雪霸路線查詢」** — `components/campsite.js` 的 `CAMPSITE_VIEWS["shei-pa:route"].trail` 寫「雪霸路線登山口查詢」，正式站導覽頁、頁首與規格功能路徑皆為「雪霸路線查詢」。使用者詢問業務端中，回覆前不改。（對照：`yushan:lotdate` 的「玉山抽籤日期」與正式站 bed_8 頁自己的麵包屑一致，確認不改。）
- [2026-09-14] **待辦：首頁功能列「RSS」是 `href="#"` 假連結** — `index.html` 首頁自有頁首的功能列，`rss.html` 已存在。與其他待建置項一起處理（同列的「網站導覽」已於本日改連 `sitemap.html`）。
- [2026-09-14] **設計師交付的 `01_Raw_Input/prototypep_ref/前台web` 與我方是同一套設計系統的兩個分支，不是兩套系統對接** — 設計檔 `css/tokens.css` 是從我方同一份 token 檔分出去的（連英文註解「The signature 587a68 is a muted, lichen-y green」都相同）：色票、圓角、陰影、行高、字型**全部同值**。差異只有字級整體大一階，以及它為無障礙 AA 新增的少數 token。**這改變成本判斷**：換皮大多是改我方既有 token 與元件樣式，不必建立對照層，也不必引入它的技術棧（Bootstrap 5／jQuery／FA5 等一律不引入，只當視覺規格用）。有畫面可依據的只有三頁：首頁、`news.html`、`route.html`（33 個面板全是空狀態，只有兩層頁籤骨架），所以本輪換皮實際只做 `news.html`；只有 CSS、三頁都沒用到的元件（stepper／sidebar／footbar／facts／itinerary 等）不列入範圍，等設計師補畫面。設計檔 README 列的 `information.html`、`_template.html` 不會交付，也不需要。
- [2026-09-14] **字級整體放大一階（A 批），`--fs-nav`／`--fs-2xs`／`--fs-3xs` 不動** — 使用者裁決照設計檔改 `tokens.css` 9 格：display 32→34、h1 28→30、h2 24→26、h3 20→22、lg 18→19、md 15→16、sm 14→15、xs 13→14、xxs 12→13，全站生效。例外：`--fs-nav 16px` 維持（「18px 在 1280 容器內塞不下」是實測約束，不是偏好）；`--fs-2xs 11px`、`--fs-3xs 10px` 保留（設計檔下限 13px 是因為它沒用到這兩級，不代表不該有）。同批新增 token：`--border-ui #7b8799`（互動元件邊框，白底 3.64:1；`--slate-300` 只有 1.48:1，送 AA 檢測必被打回）、六組機關徽章 `--badge-<機關>-fg／-bg／-border`（設計檔的底框色原是寫死 hex，收成 token）、`--field-h 44px`、`--header-pad-y 5px`。與設計檔同值異名者沿用我方名稱（pin→`--red-*`、panel→`--home-panel-*`、`--fg-on-dark-2`→`--slate-300`）。改後全站 37 頁在 1440／390 寬重掃：無水平溢位、無 console error，僅 7 頁頁首高 +1px。徽章改為**按機關配色**（我方原本的藍／橘看起來像狀態標示，容易誤讀），實際套用在 C 批。
- [2026-09-14] **news.html 依設計檔換皮（C 批），共用元件的改動範圍與影響頁** — 查詢區由扁平工具列改**霧綠底卡片**（標籤在欄位上方、欄位與按鈕同排等高 `--field-h`、新增「清除」鈕）；新增**列表工具列**（左「共 N 筆，第 x/y 頁」`role="status"`，右「RSS 訂閱」膠囊；設計檔的「歷史消息」本站沒有該頁，不放假連結）；置頂由單一圖釘圖示改**紅色「置頂」膠囊＋整列霧綠底**（WCAG 1.4.1 不以顏色單獨表意）。查詢卡命名用 `.p-news-*` 而非設計檔的 `.th-search`——我方 `.th-search` 已是「帶放大鏡的關鍵字輸入框」（apply-1、open 在用），同名會撞；等第二個列表頁要用再依「二次即提升」升到 `components.css`。**共用元件同批改動與影響頁**：`.th-table` 表頭改霧綠底／深色字／升一級字級、列 hover 改霧綠（影響 open、applySearch、campsite、information_6／_8、web_illustrate、accident-dashboard、rss、_stage2-test）；`.th-input／.th-select／.th-textarea／.th-btn-ghost` 邊框改 `--border-ui`（AA 1.4.11，影響所有含表單的頁）；`.bulletin-badge.agency-*` 改機關識別色並補 police／npa（影響 news、news_0_1、open、index-b）；`th-table-pager` 改置中、按鈕 38px、上一頁／下一頁改角括號圖示並保留 `aria-label`（影響 news、open）；`.th-crumb` 上下內距 16→10px（全站）。**不在本批**：窄版表格形態（維持我方一筆一卡）、手機主選單模式、兩層頁籤、登山路線介紹頁、只有 CSS 無畫面的元件。
- [2026-09-14] **B 批的副作用：`index-b.html` 會出現兩個 skip link** — 該頁自己有 `.p-indexb-skip`（指向 `#services`），又用共用的 `th-header`（新的 skip link 指向 `#main`），而該頁沒有 `th-page-shell`、也就沒有 `#main`，那一條是死連結；同理該頁沒有 `#AC`。使用者指示不動 `index-b.html`，故**保留現狀並記錄**：要處理時的選項是（a）該頁改用 `th-page-shell`，或（b）`th-header` 加一個關掉 skip link 的 prop。
- [2026-09-14] **news 查詢區不採設計檔的霧綠底卡片——使用者主觀決定，不得「修正」回去** — 使用者明說「不喜歡那個綠底」，不是實作錯誤。查詢區維持我方的**扁平工具列**（無卡片外框、無霧綠底），但**標籤位置、欄位高度 44px（`--field-h`）、按鈕排列照設計檔**。這是整份設計裡**唯一**刻意不對齊的地方，其餘一律以設計檔為準。之後若有人拿設計檔比對而發現「查詢區沒照做」，那是刻意的，請先看本條。
- [2026-09-14] **`th-header` 新增 `skipLink` prop（預設 true），`index-b.html` 傳 false** — B 批把 skip link 放進 th-header 後，`index-b.html` 會有兩條：它自己那條指向 `#services`（有效），元件那條指向 `#main`，但該頁沒有 `th-page-shell`、也就沒有 `#main`，是**死連結**——鍵盤使用者按了焦點會掉到頁首或消失，AA 檢測會抓到。使用者裁決：用 prop 關掉，**不改那頁去套 `th-page-shell`**（會動到版面，而該頁明定不動）。該頁保留自己那條 skip link；`#AC` 導盲磚仍缺（它沒有 th-page-shell），屬該頁的已知缺口。
- [2026-09-14] **設計檔對齊批次 1：th-header 的結構與鍵盤行為改寫（窄版抽屜 → 就地展開）** — 使用者裁決「換皮＝結構也對齊」，逐項比對後分五批，本批只做結構與鍵盤，視覺數值照搬設計檔 CSS、字級尺寸排批次 2。內容：(1) 窄版（≤900px）由「右側滑入抽屜」改為**頁首往下就地展開**、子選單就地手風琴，`.th-menumask／.th-menupanel／.th-menunav／.th-menuutil／.th-menu-lang-*` 一併刪除（唯一消費端是 th-header；`body.th-noscroll` 保留給 th-quick-nav）。抽屜宣告 `role="dialog" aria-modal="true"` 卻沒有 focus trap，是名不副實的 AA 缺陷；**改成非 modal 之後那個問題不存在，不是修好、是不需要了**。(2) 下拉改完整的 Disclosure Pattern：↓ 開啟並聚焦第一項、↑↓ 循環、Home／End、Esc 關閉並把焦點還給按鈕、Tab 移出自動關閉、桌機 hover 展開並延遲 220ms 收合（WCAG 1.4.13 可停留）；**移除 focusin 自動展開**——那會把子項全部插進 Tab 序列（宿營地一項多 11 站）。(3) 補 `aria-controls`（語言、主選單、窄版導覽三處原本全缺）、子選單 `aria-current="page"`、語系項目 `lang` 屬性與 `aria-current`、外連補「（另開新視窗）」報讀文字。(4) 頁尾標題 h3／h4 → h2／h3，消除 h1→h3 跳階。(5) 頁首骨架（`.th-header-inner／.th-brand／.th-utility／.th-nav`）由 Tailwind 搬進 components.css——窄版要靠 `.th-header.is-nav-open` 切換，那不是 utility class 表達得了的狀態；**數值一律沿用改版前的實測值**。
- [2026-09-14] **主導覽斷點 1280 → 900（與設計檔同），901–1280 之間頁首會變高** — 原本 xl（1280）以下才收合，是因為 `--fs-nav` 註解記的「1280 容器內塞不下同一行」。實測：901–1279 之間我方不會溢位，但整個右側區塊（工具列＋主導覽）會換到站名下方，頁首由 92px 變成 **164px**；設計檔同寬度是 110–136px，差距來自我方站名與標誌仍是舊尺寸（標誌 64px、站名 24px／800；設計檔為 56px、22px／700），**批次 2 對齊尺寸後應會縮回接近設計檔**。901 以下與 900 以上都無水平溢位，主導覽在 ≥901 完整可見、≤900 收進就地展開的選單。
- [2026-09-14] **設計檔對齊批次 2：全站樣式對齊** — 數值全部取自設計檔 `前台web` 的 tokens.css／components.css，未自創、未新增色票。頁首上下內距 12px → `--header-pad-y`（5px），頁首高由 92 降到 87px（設計檔 85）；標誌 64 → 56px、站名 24px/800 → 22px/700 並取消「臺灣登山申請」那一段的放大（設計檔站名是單一字級），窄版階梯改照設計檔（≤900 標誌 44／≤600 36、字 19／≤360 32、字 16）；輔助列 15 → 14px 並補 24×24 可點區；語言鈕顯示「語言」而非目前語系（目前語系改由選單內的勾號與 `aria-current` 表達，另補只朗讀的「（目前：繁體中文）」）；頁首搜尋改膠囊型、邊框 `--border-ui`、送出鈕改實心品牌綠配白圖示；主導覽項目上下 4px 內距並補**作用中底線**（3px，以 `calc(-1 * var(--header-pad-y) - 1px)` 貼齊頁首下緣，與設計檔同一條公式）；麵包屑中間層級改為可點——**以主選單資料反查該區塊的落點頁，查不到就維持純文字**（設計檔那裡是 `href="#"` 死連結，不照抄）；頁尾連結補 24px 可點區、版權帶文字 `--slate-400`（4.04:1）→ `--slate-300`（6.97:1）；跑馬燈連結補 24px、「更多」鈕 hover 改 `--home-panel-edu` 並加陰影與箭頭位移（`--national-700-hover` 在照片背景上只暗 1.39 倍，幾乎看不出來）；頁面底部留白 80 → 40px；卡片內距 24px → 22px 24px。
- [2026-09-14] **901–1280 之間頁首高度：由 164px 降到 126px（設計檔 110px）** — 批次 1 記的 164px 有兩個原因，批次 2 都處理了：(1) 站名尺寸未對齊；(2) `.th-header-inner` 整塊 `flex-wrap: wrap`，右側區塊（輔助列＋主導覽）會整個掉到站名下方。改為 **≥901 不換行、由 `.th-nav` 自己換行**（設計檔的行為），≤900 才 wrap。實測：1280+ 為 87px、1024–1100 為 126px、901 為 163px（該寬度主導覽被擠成三列），全段無水平溢位。仍高於設計檔的 110/136，差在我方主導覽項目較多、字串較長，**不再為此調整**；要再壓低就得動導覽項目本身，那是內容決定不是樣式。
- [2026-09-14] **news 查詢區改回扁平工具列（落實同日的主觀裁決）** — C 批當時做成了設計檔的霧綠底卡片，與使用者「不要那個綠底」的裁決不符，批次 2 一併改回：`.p-news-filter` 只留 `margin-bottom`，標籤位置、欄位高度 `--field-h`、按鈕排列仍照設計檔。
- [2026-09-15] **設計檔對齊批次 3：語意與無障礙標記** — (1) `base.css` 補全域焦點框（`:focus` 與 `:focus-visible` 並寫，2px `--national-700`，白底 4.77:1）；`.th-input／.th-select／.th-textarea` 與頁首搜尋框原本寫了 `outline: none` 卻沒有等效替代，補上貼邊的明確外框。(2) `transition: all` 與無屬性簡寫全數改為明列屬性（components.css 6 處、pages.css 16 處）——`all` 會把 outline 一起動畫化、焦點框延遲出現；實測樣式表中 `transition-property: all` 殘留 0。(3) `<html lang>` 由 `zh-TW` 改 `zh-Hant-TW`（36 支已追蹤頁面）；**flatpickr 不受影響已實測**，它的語系是 JS 明指 `flatpickr.l10ns.zh_tw`，與 html lang 無關，改後月份與星期仍是中文。(4) 首頁站名改 `h1`（class 不動、外觀不變），7 頁的標題跳階全數修掉（apply-2／apply-3 的區塊標題、news_0_1 的「相關附件」、notice 四頁的 `th-doc` 文件標題 h3→h2），全站 0 跳階。(5) 表格補 `scope="col"` 與 caption：`th-data-table`、`p-news-table` 加 `caption` prop（沒給就由欄位標題組出「…，欄位為 A、B、C」，不留空），`open`／`web_illustrate`／`_stage2-test` 的自寫表格逐一補；已追蹤頁面 th 無 scope 歸零。(6) 裝飾性圖示以腳本補 `aria-hidden`（apply-1 的 342 個是真的，不是重複計算：77 張路線卡各數個圖示），全站未標記由 864 降到 25，**剩下的都是 flatpickr 自己注入的 svg**（第三方，設計檔同樣未處理）。(7) 可點區：apply-1 清除鈕、apply-3 刪除列鈕補 24×24；apply-3 的兩個 radio 是 13×13 但**包在 202×43 的 label 內**，實際點擊目標是 label，不需放大。
- [2026-09-15] **待辦：另一個 session 的 11 支未提交新頁尚未套用批次 3 的無障礙處理** — `accident-dashboard`、`contact`、`information_2`／`_3`／`_4`／`_8`、`mail`、`mail_1`、`opendata`、`privacy`、`security` 當時還是未追蹤檔，**刻意不碰**。它們的 `<html lang>` 仍是 `zh-TW`、表格缺 caption（contact 1、information_2 5、information_3 3、mail 1）、裝飾性圖示缺 `aria-hidden`（約 20 個），information_2／_3 另有一個小於 24px 的連結目標。**那一批提交後要補跑同一組處理**（腳本在 session 暫存區的 `b3.py`，`add_aria_hidden` 與 `fix_lang` 可直接重用）。
- [2026-09-15] **`index.lock` 孤兒檔處理（使用者單次授權，R2 不因此放寬）** — 2026-09-15 00:29:30 出現 0 bytes 的 `D:\git-dirs\登山一站式.git\index.lock`，非本 session 產生，導致批次 3 無法入 index。依使用者授權的順序處理：先查行程（21 個存活的 `git.exe` 全是 `--no-optional-locks` 的唯讀指令 rev-parse／log／diff／symbolic-ref，啟動時間 2026-09-14 10:30–17:11，**該旗標本身就不會建立 index.lock**；2026-09-15 之後啟動的 git 行程 0 個、寫入型指令 0 個），再以獨佔開啟測試確認無人持有 handle，判定為孤兒後刪除。**路徑層級的 `git checkout`／`clean`／`reset --hard` 仍一律禁止（R2 不變）**。
- [2026-09-16] **設計檔對齊批次 5：驗證三項，並修掉驗證中發現的落差** — (1) **flatpickr 覆寫逐條核對**，發現 4 條與設計檔不符並對齊：日曆外框 `--slate-200`（白底 1.3:1）→ `--border-ui`（3.64:1，WCAG 1.4.11）、星期列 13.5px／`--fg-3` → 14px／`--fg-2`（7.58:1）、今天由淺綠框改品牌綠框＋綠字＋粗體、**上／下月日期 `--fg-4`（2.56:1）→ `--fg-3`（4.76:1）**——那是可點的一般文字，原值不符 1.4.3；停用日期維持 `--fg-4`（WCAG 對停用元件不要求對比）。(2) **320px reflow**：apply-3 整頁溢位 37px，原因是 grid 項目的 `min-width` 預設 auto、欄軌被壓不下去的子元素撐開，補 `.p-apply3-layout > * { min-width: 0 }` 後歸零；其餘頁全數通過。(3) **200% 縮放**：通過。**掃描器以 `document.documentElement.style.zoom` 模擬會誤報**——媒體查詢不會因此重新觸發，窄版規則不生效，表格就會「溢位」；真實瀏覽器縮放會改變 CSS 視窗寬度。改以等效的 360px 實測確認，information_6 等頁皆無溢位。(4) 5-14 無障礙說明頁依使用者裁決不做（新頁屬模式 3）。
- [2026-09-16] **麵包屑與頁首圖示對齊（批次 6）：以設計檔的「實際呈現」為基準，不是它的 CSS 原始碼** — 我方 `.th-crumb i { font-size: 10px; color: --slate-300 }` 真的生效（FA6 CSS 版維持 `<i>`），設計檔寫了同一條卻**從未生效**（FA5 的 JS 版把 `<i>` 換成 `<svg>`，選擇器打不到），他們畫面上是繼承來的 14px／`--fg-3`。改成繼承後兩邊一致（首頁圖示 16×14、分隔符號 9×14／--fg-3；分隔符號差 2px 是 FA6 字身較寬）。同批把頁首的地球、選單鈕、搜尋鈕由 Phosphor 換成 FA solid（與設計檔同一顆），下拉箭頭 `fa-chevron-down` → `fa-angle-down`。**教訓已寫入知識庫**：`shared/tech/vue3-global-build-遷移踩坑.md` 第十類（第三方套件改寫 DOM 標籤讓 CSS 全部失效）與「一之二、對照設計檔時基準是畫面呈現」。
- [2026-09-16] **維持 Font Awesome 6，不整站改用設計檔的 FA5** — 決定性理由是 FA5 的 **JS 版**會把 `<i>` 換成 `<svg>`，所有針對 `i` 寫的 CSS 一次失效且不報錯（設計檔自己的麵包屑就是活證據）；我方 `components.css` 有多條對 `i` 的規則（`.th-crumb i`、`.th-block-title i`、`.th-card-head i`、`.th-sidebar-head i` 等）。其餘理由：我方 108 種 FA6 圖示要逐一改名（改錯只顯示空白、console 不報錯）；FA5 JS 版沒有 icon 字型可供 CSS `::before`（設計檔為此另載整包 bootstrap-icons）；FA6 才是現行版。作法改為**只對齊看得出差異的個別圖示**。
- [2026-09-16] **`.th-select` 的箭頭改線條式 chevron，與頁首語言鈕統一（批次 7）** — 原為兩道 `linear-gradient` 疊出的實心三角。仍不用 SVG data URI（data URI 的顏色只能寫死，吃不到 token），改成每道 gradient 只畫一條 1.6px 斜線、兩道拼成 ∨。顏色收斂為區域變數 `--th-select-caret`，hover／focus／disabled 只換那一格，不再重寫四遍 gradient。
- [2026-09-16] **待辦：全站 61 處 Phosphor 圖示是否統一到 FA** — 分布於 12 支檔（forest-camp-1／2、apply-1／2／3、th-forest-camp-shared、th-quick-nav、RouteData 等），都是內容型圖示（山、樹、帳篷、建築、盾牌…）。**設計檔沒有對應畫面，換了等於用猜的**，故不動；要處理時需先有設計依據。
- [2026-09-16] **待辦：兩層頁籤元件與 WAI-ARIA tabs 鍵盤行為** — 另一個 session 已把 campsite 的 `.p-campsite-tab*` 提升為 `.th-tabs／.th-tab`（單層頁籤的視覺提升），但設計檔的**主頁籤＋膠囊次頁籤兩層結構**與 `tabs.js` 的鍵盤行為（← → Home End 移動並切換、`aria-selected` 與面板 `hidden` 同步、tablist 只佔一個 Tab 停留點）尚未實作。等那批提交後再排。
- [2026-09-16] **`index.lock` 改為常態程序（R2 增補，使用者裁決）** — 寫進本專案 CLAUDE.md 的 R2：先以獨佔開啟測試確認無行程持有，再確認沒有寫入型 git 指令在跑（唯讀指令不算，帶 `--no-optional-locks` 的本來就不建立 lock）；兩項都成立＝孤兒可刪，回報中記下建立時間與大小；任一項不成立即停下回報。實測本專案這個 lock 會反覆出現（2026-09-15 00:29、01:11，2026-09-16 09:24），**每次都要重新確認**。R2 其餘不變：路徑層級的 `git checkout`／`clean`／`reset --hard` 仍一律禁止。
- [2026-09-16] **61 處 Phosphor 圖示維持不換（使用者裁決）** — 三個理由：(1) **那些是內容型圖示**（山、樹、帳篷、建築、盾牌），代表的是業務語意不是介面元件；介面圖示統一有價值（頁首那三顆已於批次 6 對齊），內容圖示沒有人會注意到來源不同。(2) **沒有設計依據**——設計檔沒畫那些頁面，換了就是從 `ph-tent` 猜一個 `fa-campground` 對上去，那是推測不是規格。(3) **造型語言不同**（Phosphor 細線、大圓角、開放輪廓；FA solid 實心封閉），逐顆換完可能比現在更不協調。**盤點表已產出**：`_knowledge/projects/國家公園入園擴充/圖示盤點-Phosphor.md`（12 支檔、61 處、26 種圖示，逐一標語意與 FA6 free 候選），供設計師補畫面時一次對照、也是將來要減少字型依賴時的範圍。**FA6 覆蓋度實查結果**（對 6.4.0 的 2,452 個 free 類名）：沒有任何一個語意在 FA6 完全找不到候選，所以「換到一半變成混用三套」的風險不成立；但**登山領域的山屋、登山口、水源、避難所、生態保護區在 FA6 與 Phosphor 都沒有專用圖示**，兩邊現況都是拿通用圖示代用（山屋＝房子、生態保護區＝樹），要精確表達得另找圖示集或自製 SVG——那是比換字型更大的決定。另註：`index.html` 首頁**自有頁首**的地球與放大鏡（2 處）仍是 Phosphor，批次 6 只改了 `th-header`。
- [2026-09-16] **[待辦] 02_Spec 缺「各機關收費基準」一塊** — 正式站的檜谷／天池／嘉明湖收費基準在 `notice_b4`／`b5`／`b6` 三頁，但**那三頁的費率沒有寫進任何一份規格**：`09_登山須知.md` 只列出連結與頁名，`11_林業及自然保育署宿營地查詢.md` 全份沒有收費或床位數（唯一的業務數值是天池營地帳篷 4*4 建議 8 人／3*3 建議 6 人與檜谷四人帳篷），`21_林業及自然保育署區域申請及抽籤查詢.md` 一個業務數值都沒有。**後果是那次核對在規格層留不下痕跡**——2026-09-16 依 b4/b5/b6 核對的落點是雛形常數 `forest-camp-1.js`（費率分級、向陽山屋 70 床、人數上限 1〜12），規格層沒有對應物可標「已核對」。處置：另立一份「各機關收費基準」規格，或在 `11` 補一節收費基準。**這不是標記問題，是規格缺了一塊。** 同類缺口另有 `06b` 的外國人單價（已標 `[缺口]`）與觀高山屋平日 800／假日 1200 元（`notice_a1` 有載，本份未收錄）。
- [2026-09-16] **`[待確認]` 與 `[缺口]` 是兩個標記，不可互相代用** — `[待確認]` 是「有值但不確定對不對」，`[缺口]` 是「根本沒有這個值」。混用會讓下一個人以為只要去查證就好，而缺口查證解決不了——要取得一個值填進來。判準與實例寫進 `02_Spec/README.md`「三個標記的差別」。同節並訂：**法條條次一律實查法規原文**（全國法規資料庫），查得到就直接更正並註明查證日期，查不到才標 `[待確認]`；我方自行盤點的統計數字（`24` 的 146／28／118 筆、`35` 的年度筆數）不適用溯源規則，出處在文件自己裡面。
- [2026-09-16] **判「已核對」的兩條界線** — (1) **數字相同但語境不同，算查不到**：`16_玉山宿營地查詢.md` 的「排隊遞補逾 60 人建請另擇期」，`notice_a1` 上確實有「60人」，但那是玉山線單日往返的每日承載量（「每隊1至12人，每日60人」），與排隊門檻無關 → 標 `[待確認]`，不得援引。硬湊來源等於把一個示意值換成另一個示意值，而且從此看起來像有依據，比原本更難發現。(2) **正式站本身有誤時記錄差異但不修正**：`34_聯絡我們.md` 的個資告知，正式站寫「個資法第15條……僅利用於……」，但第 15 條規範的是「蒐集或處理」、「利用」是第 16 條（全國法規資料庫實查）。條次照抄不改——改了就與正式站不一致，下一輪比對會判成我方的錯；差異記在核對狀態區塊，寫法「與正式站一致，但正式站可能有誤：<差在哪>」。實查的結果有三種去處：我方抄錯→改我方、正式站有誤→記錄不改、查不到→標 `[待確認]`。兩條都寫進 `02_Spec/README.md`。
- [2026-09-16] **[待辦] `06b_線上繳費(排雲).md` 是範圍缺漏不只是缺值** — 「線上繳費」實際涵蓋**排雲山莊與觀高山屋兩處**（佐證：`06a` 的狀態卡片寫「待繳費（排雲山莊／觀高山屋）」、`16` 的「排雲山莊/觀高山屋規費查帳」、`notice_a1` 兩處費率都有載），但本份只寫排雲。缺的不是一個數字而是**一整個宿營地的費率結構與流程分支**——觀高是平日 800／假日 1200 的**兩段費率**，與排雲單一 480 元的結構不同。檔名與標題的「(排雲)」沿用正式站頁名，**內容範圍不該跟著它縮**。補的時候要連 2026-09-16「繳費頁不做山屋選擇器」那筆一起解（`apply_4.aspx` 查詢頁沒有山屋欄位、另有 `apply_4t.aspx`，兩支與兩處的對應仍 `[待確認]`），否則會補出一份沒有分流的兩處費率。
- [2026-09-16] **登山須知頁刪除我方新增的三項內容（取代 2026-09-02 的 `[待確認]` 裁決）** — 刪除頁首導言「本站整合國家公園署、林業及自然保育署、警政署等機關之登山申辦規定。」、頁面更新日期 `2026-09-01`，以及警示區（跨機關規定、入山許可證＋「待確認」徽章＋來源說明）。理由：正式站 `notice.aspx` 的頁面本體只有標題、安全影片按鈕與五個機關清單，三項皆不存在；官網的「更新日期」與「乃整合…」說明位於**頁尾**，我方 `th-footer` 已有對應內容，刪除後資訊未流失且與官網結構一致。2026-09-02 那筆「畫面上保留文字並標示」的裁決自本日起失效，業務來源到齊也不要直接加回頁面，先確認要放頁面還是頁尾。
- [2026-09-16] **[待確認] 山屋計價單位語意不一致（檜谷「每人每晚」vs 其他「每床」）—— 標註不改** — 官網 `notice_b4` 檜谷山莊收費基準寫「假日：每**人**每晚收費新台幣300元整／平日250元」，而 `notice_b5`（天池）是「480元/每**床**」、`notice_b6`（嘉明湖・向陽）是「每**床位**收費600元」。雛形 `TH_CABIN_DATA` 統一用 `unit: "床"` × 數量 × 單價，於是同一個 FC2 數量 stepper 在檜谷代表**人數**、在其他山屋代表**床數**，語意不同但 UI 完全一樣。要修需加 `chargeBy: "person" | "bed"` 欄位，或把檜谷的 `unit` 改成「人」（較誠實但會讓四間山屋 UI 不一致）。**本輪不改**（使用者 2026-09-16 裁決）：僅在此記錄，避免下一個人以為是漏改。相關：同日裁決資料模型只做「假日／非假日二價」，不做三類型（會動到 `facilities[].id`＝`alloc` 二維矩陣的鍵，屬 campsite 等級工程）與計價單位。
- [2026-09-16] **瓦拉米山屋自 `TH_CABIN_DATA` 移除，改用向陽山屋** — 實測「瓦拉米」只出現在 `notice_a1`（玉山登山須知）與 `notice_a3`（玉山緊急聯絡資訊），洽詢單位為「玉山國家公園管理處南安遊客中心」，在 a1 中與排雲、圓峰、庫哈諾辛並列為**玉山國家公園**的抽籤宿營地。雛形原記 `manager: "花蓮林區管理處"` 是錯的機關，且問題不只是費率——`forest-camp-1/2` 是**林保署山屋申請流程**，而瓦拉米走玉山的入園申請與抽籤，整組放在錯的流程裡。改用**向陽山屋**（70 床，與嘉明湖同屬 `notice_b3` 指定宿營地點、共用 `notice_b6` 收費基準），可讓「同一份收費基準管兩間山屋」的真實結構在雛形裡呈現，且本來就在 b1～b7 範圍內。
- [2026-09-16] **[待確認] 申請期限類規則完全沒有寫進卡控，只存在顯示文字** — 掃全部 `.js` 後確認：「入園前 5 天至前 2 個月」「住宿日前 60 日至前 5 日」「外籍前 35 天至前 4 個月」這類期限規則，只出現在 `components/NoticeDetailData.js` 的顯示文字裡，**沒有任何一處變成日期卡控**（雛形的日期限制只有 `:min="todayStr"` 不可選過去、結束日不早於開始日，皆為通用 UI 約束）。**這是好事**：雛形不會讓人填出一個系統會拒絕的申請，也不會因期限規則過期而誤導。但要記下來，因為之後真的要做期限卡控時，那批顯示文字就是清單來源——屆時須先確認各機關期限（玉山、太魯閣、雪霸、林保署四套各不相同）並逐條標註來源。
- [2026-09-16] **寫死數值的掃描產出要分三類，不要一律當成問題** — 掃 `.js` 裡的業務數值時，結論必須分：**(a) 與官網一致→不動**（例 `apply-3.js:152` 的 `members.length >= 12`，與 `notice_a1`「每隊 1 至 12 人」相符）、**(b) 無依據的示意值→標註或改**（例 `forest-camp-1.js:78-82` 可用量長條的假資料公式與 50%／20% 顏色門檻）、**(c) 與官網矛盾→必改**（例 `forest-camp-1.js:142` 原 `Math.min(20, …)` 人數上限，`notice_b3` 明寫 1〜12，已改）。「寫死」不等於「錯」——同一次掃描的 39 處命中裡，真正該改的只有 1 處，另有 1 處該標註，其餘皆為 UI 機制（`PAGE_SIZE`、字數門檻、`maxH`）。不分類就會把對的東西一起改掉。
- [2026-09-16] **[待辦] TH_CABIN_DATA 的顯示文字欄位缺來源，與第 2 層（6 張 notice_a* 的 79 段）一起處理** — 費率與承載量已於 2026-09-16 對齊正式站（commit 93d2be1），但**顯示文字欄位未處理**。逐條拿去官網 b1～b7 ＋ a1/a3 全文比對的結果：**(A) 與官網直接矛盾，必改**——嘉明湖 notice「山屋提供睡袋（另計費），自帶睡袋可享折扣」，而 `notice_b3` 明寫「山屋床位抽籤→只提供床位 **沒有包含睡袋及供餐**」且要求「自備餐飲、營帳、睡袋」；嘉明湖 notice「入住日前 14 天可申請取消」，而 b3 的退費是「起算日前 5 日前全額／前 4 日 50%／前 3 日內不退」，**沒有 14 天這個級距**。**(B) location 全部無依據**——嘉明湖寫「9.5K」但 b3 沿線據點是「8.4 公里處」（矛盾）；三間山屋的海拔 3,310m／2,860m／2,230m 官網皆無；天池「22K」、檜谷「7.5K」官網無（只有「能高越嶺」「北大武」等詞可查到）。向陽「4.3K」則有 b3「4.3公里處：向陽山屋」依據。**(C) notices 9 條只有 1 條可查到**（向陽的「一般申請：住宿日前 60 日至前 5 日」出自 b3），其餘 8 條為我方自撰：天池「採先到先得制／建議提前 7 天／提供熱食需預約餐食」官網完全未見；檜谷「開放日期前 60 天受理」官網未寫（b1 是「行程前 30 日由系統自動抽籤」）。檜谷「無對外供餐，請自備糧食」**與 b1 一致**，是唯一對的一條。**(D) manager**：臺東／南投林區管理處官網可查到；**屏東林區管理處官網查不到**（b1/b4 未載管理機關名），屬我方推測。**處理原則**：顯示文字不影響互動所以可以晚做，但它跟費率一樣「看起來像已確認的規則」，不可只留在檔頭註解裡自生自滅。
- [2026-09-16] **斷言的語料要含還是要排除註解，取決於斷言在問什麼** — 問「還有沒有人用它／有沒有人在說它」（改名、刪檔的善後）→ 語料**要含**註解與文件，死指示就寄生在那裡。問「舊識別字是否已從程式碼消除」（改完自驗）→ 語料**要排除**註解，因為註解會正當地引用舊識別字當歷史敘述。本日同一輪因此誤判三次（`assert "pricePerNight" not in s`、`"walami" not in s`，而新寫的檔頭正好在說明「原為單一 pricePerNight」「原本的 walami 屬玉山國家公園」——那些註解是該留的）。修法是斷言前先剝區塊註解、HTML 註解與行註解。同型的坑先前已在 CSS 宣告比對上記過一次（「比對前先剝掉宣告區塊裡的註解」）。兩種選擇的失敗表現不同：該含沒含＝漏抓（安靜）；該排沒排＝誤判（會叫，而且叫得很像真的）。已補進 `_knowledge/shared/tech/vue3-global-build-遷移踩坑.md` 三之四節。
- [2026-09-16] **重試迴圈不要用 `2>/dev/null` 吞掉錯誤訊息** — 本日提交第 4 層時 `git commit` 連續 6 次失敗，因為迴圈把 stderr 導掉，看起來完全像 `index.lock` 問題（當時確實也有一個滯留 7 分鐘的孤兒 lock，更強化了誤判），於是走完孤兒判定並刪了 lock。實際原因是 **commit message 檔案根本沒被寫出來**（前一個指令在 heredoc 之前就 `exit 1` 了），錯誤訊息是 `fatal: could not read log file … No such file or directory`。改成保留 stderr、把第一行印出來之後，一次就看到真因。**形狀與踩坑總表六之 5「整段 try 會把局部失敗放大成全面失敗」相同**：重試迴圈會把一個確定性錯誤偽裝成暫時性衝突，而且偽裝得很好——它連續失敗的樣子與搶鎖一模一樣。判準：**重試迴圈必須顯示最後一次（或每一次）的實際錯誤**，不得只回報「重試 N 次皆失敗」。
