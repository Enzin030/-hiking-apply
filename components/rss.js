/* ============================================================
   rss.js — RSS 訂閱頁的初始化（原 Rss.jsx）
   ------------------------------------------------------------
   版面全在 rss.html，本頁沒有資料與互動，只做 thPage 登記
   （頁面宣告了 data-page-init，不呼叫 thPage 會在 console 報錯）。

   2026-09-14 依正式站改版：
   - 拿掉「訂閱網址」卡，FEEDS 資料與 p-rss-feed-url（複製網址）元件一併移除
   - 「如何訂閱 RSS」第 2 步要在句中放 RSS 圖示，th-step-list 的 body 只吃純文字
     （無 body slot，塞 HTML 字串會需要 v-html），故步驟改寫在 rss.html，
     直接沿用 .th-steplist／.th-steprow 系列 class，SUBSCRIBE_STEPS 移除
   - 曾加回頂端 TOP 按鈕，因與快捷選單按鈕位置衝突，使用者裁決先不做，已移除
   ============================================================ */

thPage({});
