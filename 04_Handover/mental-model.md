# mental-model — 一頁心智模型

標的：`\\10.0.0.51\d$\WEB\nationalpark\HikeNationpark`　產出：2026-09-08
**先讀這頁，再讀 `cases.md`。** 以下五件事一旦理解錯，清單裡大半題會答錯。

**前提——證據分三類**：`.cs` 邏輯＝系統實際會做什麼（事實）；機關原文
（`notice_a*`／`notice_b*` 靜態文字、`attention` 表）＝規則實際是什麼（事實）；
`.cs` 裡的中文註解與寫死字面值＝**某人當時的理解，不是事實**。前兩類回答不同問題，第三類不可引用。

---

## 1. 這套 code 裡沒有一套「申請規則」可以讀

規則拆成四種載體：**數值在 DB**（`RuleSet`／`Fixedclimb`／`snowset`）、**判斷在三份獨立實作**、
**部分寫死成會過期的日期**、**完整敘述只在機關原文**。沒有一處是完整的。
接手要做的不是修規則，是**從零實作卡控**。
**會答錯**：Q22–Q25、Q26–Q31、Q60。
出處：`apply_1_4.aspx.cs:8907`、`:8929`；`apply_1_3.aspx.cs:3026-3075`。

## 2. 「三家」不是設定差異，是三份獨立的程式

玉山 `apply_1_4`＋`YuShanFun.cs`；雪霸 `apply_1_3`（規則內嵌在頁面）；
太魯閣 `apply_1_5`＋`tarokoapplyControl/`＋`App_code/taroko/`。再乘上 `en/`、`jp/`
兩份**整套 code 的複本**（不是語系資源）。判準因此不同——「長程縱走」太魯閣只看
`linetype=="1"`，雪霸還多兩組節點推導。
**會答錯**：Q15、Q27–Q29、Q33、Q39、Q43–Q45、Q48。
出處：`tarokoModeCode.cs:169-170` vs `apply_1_3.aspx.cs:2658-2716`。

## 3. 入口檢查是 `Referer` 字串比對，不是驗證

全流程**沒有登入**。申請表唯一的守衛是「`UrlReferrer` 是否含 `apply_1_2.aspx`」——
可偽造，缺少時反而擋住正常使用者。`apply_1_49.aspx` 連這道都沒有。
**會答錯**：Q1–Q6。
出處：`apply_1_4.aspx.cs:105-116`；`apply_1_49.aspx.cs`（無 `UrlReferrer`）。

## 4. 前端擋 ≠ 後端擋；下拉清單不是伺服器端檢查

同意書全勾只在 `OnClientClick`，`btnagree_Click` 沒讀 `chk[]`、沒存紀錄。
封園日同理：雪霸的排除只發生在產生清單時，送出時那道檢查是空轉的。
**會答錯**：Q7、Q8、Q19。
出處：`apply_1_2.aspx:60` vs `.cs:141-197`；`apply_1_3.aspx.cs:771` vs `:5605`。

## 5. 寫死的日期會到期，而且沒有任何機制會發現

封園窗停在 2022-04（`web.config:44-45`）、2023-10（`CheckBlockDay.cs:136-137`）、
封閉日清單是 2025 農曆年（`:22`）、路線公告 `< 20260101`／`< 20241227`
（`apply_1.aspx.cs:306`、`:317`、`:328`）。**今日全部已失效，且不報錯。**
`pdftype`（名為 PDF 設定）被當環境旗標，值 `1` 使一整段日期保護被跳過。
**會答錯**：Q16–Q21、Q52。

---

# 待問人清單

## 給機關（業務規則）

1. **同意書預設勾選的效力**。`attention.selectchk='1'` 的條文載入即勾選，
   且系統不留同意紀錄（`apply_1_2.aspx.cs:70-77`、`:141-197`）。這在法律上是否成立？
2. **`chk` 與 `selectchk` 的業務語意差別**（`apply_1_2.aspx.cs:62`、`:70`）。
3. **太魯閣三種路線群的不同截止時刻與星期規則**（`notice_a4.aspx`：一般 5 天 15:00 前、
   單攻 3 天、錐麓 1 天且週六至翌週一須週五 15:00 前）——系統擋、人工審、還是沒擋？
   `RuleSet` 只有「天」與「月」兩欄，承載不了（Q24、Q25）。
4. **錐麓古道「每天每一人限制申請一隊 12 人」**（`notice_a4.aspx`）在何處實作？
5. **玉山外籍提前申請的名額與國籍搭配比例**（`notice_a1.aspx`）目前是否仍適用？
6. **封園（過年停機）現在如何執行**？程式端已失效三年（Q15–Q18），實務上怎麼做？

## 給原廠商（實作意圖）

7. **`apply_1_49.aspx` 是什麼**？可否下架？它與 `apply_1_4` 只差 12 行、
   驗證完全相同、但無入口守衛（Q1）。
8. **`apply_1_3_1` 等 8 處 `IndexOf("apply_2.aspx")` 重複兩次**——原本要允許的第二個來源是哪一頁（Q6）？
9. **`ucDatePicker` vs `ucDatePicker2`**（625 行差 2 行，差在 `De_EnCode`）應該用哪一個（Q49）？
10. **status `17`** 在同一支檔案裡既稱「初審完成」又稱「備取」（`YuShanFun.cs:2251`、`:2361`），何者為準？
11. **抽籤已公告的 node＋date 上，抽籤「之前」送出的案件，`afterstatus` 會不會被回填？**
    已確認 `afterstatus` 為 null 屬正常口徑（null＝排隊預約，三處寫法一致），
    且七格統計只在 `ballot` 有 `publictime` 時才被呼叫——所以「上百萬筆消失」是誤讀，**不是問題**。
    但抽籤程序本身在 `manasystem/`（後台，本輪未讀），若它沒回填舊案，
    那些案件在同一日期的六格裡仍會只落在「排隊預約」。這一題要問，範圍比原先小得多。
12. **抽籤前 status 17 那格缺 `IsNotRecovery` 排除條件**（`:2257`），同函式其他格都有——刻意或漏寫？
13. **`status = 7`** 在玉山歸「排隊預約」、在太魯閣當「候補」篩選（`YuShanFun.cs:2300` vs `taroko_waitinglist.cs:91`），是否同一語意？
14. **共同承載量**除了硬編碼的 `climblineid in (28,109)`（`taroko_waitinglist.cs:76-79`）之外還有哪些組合，有沒有被實作？
15. **`c_id <> 62`** 為何被寫死排除在路線查詢外（`apply_1.aspx.cs:223`）？
16. **玉山篩選時 `fm.ord between 100 and 1000`**、其餘機關 0–9999（`apply_1.aspx.cs:151-152`）的用意？
17. **`web.config` 這份對應哪個環境**？`pdftype=1`（本機）、`WSUrls` 指向 `localhost:804`、
   `customErrors=Off`、`debug=true`（Q51、Q52、Q55）。
18. **`isusedproxy` 等四個 key 重複且無 `<remove>`**（`web.config:47-50`、`:70-73`）是否有意（Q54）？

## 待決策（使用者）

19. **是否連線資料庫**補齊 `[缺口]` 的數值（`RuleSet`／`Fixedclimb`／`snowset`／`attention`）。
    本輪唯讀分析未連線，所有數值一律只寫表名與欄位名。
20. **是否續掃未讀區塊**：`apply_2`～`apply_6`、`bed_0`～`bed_11`、林業署與警政署三條流程、
    `App_code` 未讀者、`NationPark/`（後台 19,233 檔）。見 `cases.md` 末段的收斂說明。
    **2026-09-11 更新**：`NationPark/`（後台）已有**功能與畫面層級**的替代資料——
    106 年後台操作手冊，盤點見 `2026-09-11-106年操作手冊可用內容.md`（功能地圖在 §3、索引在 §6）。
    那份是 9 年前的手冊，**不能替代讀 code**，但足以知道後台有哪些功能、每個規則值由誰維護，
    因此「要不要掃後台 19,233 檔」這個決策現在有比先前更多的資訊可以下。

## 由 106 年操作手冊新產生（2026-09-11）

出處全部是 `01_Raw_Input/10604_系統操作手冊(前台／後台).pdf`（版本 10604001＝106 年 4 月），
完整依據與頁碼見 `2026-09-11-106年操作手冊可用內容.md` §8。
**手冊已 9 年，證據位階低於當前截圖與 DB 實測**，以下皆為待確認，不是斷言。

### 給原廠商

21. **「住宿方式」三選項（床位／營帳／遮蔽物）對到 `node` 的哪個欄位**？`bedtype` 是否曾有值 2？
    （後台 印 150；牽動 `rules-vs-code.md` 的 `bedtype` 那列，若是三值則 code 的二分法會把「遮蔽物」併入營位）
22. **後台審核進度九種名稱與 `status`／`afterstatus` 碼的對應**——尤其後台把後補分成
    「宿營地不足後補」與「路線承載量不足後補」兩種，而前台把 `3`／`6`／`7` 三碼都叫「排隊預約」（後台 印 53）。
23. **草稿保留 30 日這個值寫在哪**？後台「規則設定」沒有這個欄位，106 年時是 7 日（前台 印 153）。
24. **節點管理與宿營地管理是同一批欄位的兩個維護入口**，兩邊寫入是否同一張表？
    與「排雲山莊三筆同名」是否有關（後台 印 30-36／149-152）？

### 給機關

25. **「備取床位數 50／備取營帳數 12」**自 106 年至今兩個觀測點皆為此值，機關是否仍要這個數字？（後台 印 28）
26. **雪霸管制路線的三條硬規則**——隊伍最少 3 人（含）以上、領隊不得兼隊員、
    全員上傳登山經驗與簽署聲明書——現況是否仍然生效？（後台 印 153）
27. **前台多語範圍**：手冊有完整英文版章節（前台 印 181-231）但無日文版章節，
    日語只在頁首語言切換與後台三語欄位。115 案要支援到什麼程度？
28. **行動版算不算驗收維度**？手冊每個功能都有成對的「(電腦版)／(行動版)」截圖，
    而 `02_Spec/` 現有 34 份規格**全部未記行動版**（2026-09-11 實查）。
29. **颱風期間管理**（後台 印 165-169：聯繫表匯出、實際人數與聯絡狀況回填）這條機關端流程，
    115 案是否納入範圍？雛形與 `02_Spec/` 目前完全沒有。
30. **「立即申請入山證」轉送警政署「警政E網通」的現況**——手冊裡那張圖自己就標註「(待更換)」（前台 印 170）。
